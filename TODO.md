# TODO

## Trace Authority

- This file, `/root/repo/fustor/TODO.md`, is the only canonical goal trace for the current fs-meta work.
- The active owner is the side named by the current first raw failing boundary. Record any Capanix-owned evidence here by absolute path only if fresh raw evidence moves ownership across repos.

## Final Target: Active-Three fsmeta-stable

- Target cluster: exactly three active Capanix/fs-meta participants: `10.0.82.144` / `panda144`, `10.0.82.145` / `panda145`, and `10.0.82.146` / `panda146`.
- Active logical roots are exactly `nfs-144`, `nfs-145`, and `nfs-146`.
- Owner identity follows the app-visible host object that makes a root visible to the runtime, not the NFS server IP.
- Capanix remains the authority for bind/run realization and runtime accepted scopes. fs-meta consumes accepted scope evidence and must not fabricate sink/source/scan ownership from local grants, stale cache, or status-only projections.
- `/api/fs-meta/v1/status` must not publish a smaller complete total when owner fan-in is partial. It must aggregate all active owner evidence or explicitly mark the response degraded, partial, or failing.
- Final success is measured by correctness, stable ownership/materialization, complete/non-partial `/status` evidence, and stable index growth, not by CPU-efficiency targets.

Expected active-three deployment topology:

| host | logical group | host object | expected source/scan owner | expected sink owner |
| --- | --- | --- | --- | --- |
| `10.0.82.144` / `panda144` | `nfs-144` | `panda144::nfs-144` | `panda144` | `panda144` |
| `10.0.82.145` / `panda145` | `nfs-145` | `panda145::nfs-145` | `panda145` | `panda145` |
| `10.0.82.146` / `panda146` | `nfs-146` | `panda146::nfs-146` | `panda146` | `panda146` |

Final acceptance criteria:

- `cnxctl cluster status` shows exactly three active peers: `panda144`, `panda145`, and `panda146`.
- `cnxctl config dump` shows `fs-meta.__cnx_runtime.app_scopes` includes exactly the three active NFS logical roots plus only legitimate bootstrap/listener scopes.
- Authenticated `/api/fs-meta/v1/status` returns HTTP 200 only for complete evidence, or otherwise carries explicit degraded/partial/error evidence.
- After steady-state convergence, each active node has exactly one current fs-meta runtime process and no live draining predecessor for the same app identity.
- `/status` shows one-to-one `sink.primary_host_ref_by_group` for all three active NFS groups.
- `/status` shows one-to-one `sink.debug.scheduled_groups_by_node`, `source.debug.scheduled_groups_by_node`, and `scan.debug.scheduled_groups_by_node`; no node claims all three sink groups.
- Each sink statecell contains only its scheduled group plus bounded legitimate handoff-retained state.
- All three sink groups accumulate live nodes under their expected owners.
- Source concrete roots for all three groups have current owner evidence and completed current-audit evidence: `last_audit_started_at_us != null`, `last_audit_completed_at_us != null`, and completion belongs to the current revision/generation.
- Sink groups for all three roots leave `pending-materialization` and become trusted/materialized: `initial_audit_completed=true`, `materialization_readiness=ready`, `service_state=serving-trusted`, and `trusted_observation_readiness=true`.
- Stable dashboard/API totals, including `/api/fs-meta/v1/status` totals, do not flash backward between complete and partial aggregates.
- Complete/non-partial authenticated `/api/fs-meta/v1/status` evidence shows global live-node index entries continue stable forward growth and exceed `200,000,000`; any lower or stalled sample is acceptable only when explicitly marked degraded, partial, or failing.

## Active Gate: Fustor Source Admission Release Repair Pending Compatible Build/Redeploy

- Active owner: `fustor`.
- Current first raw boundary: clean-log official active-three redeploy/restore installed the current `panda145` compatible artifacts, app deploy succeeded, roots apply succeeded, and accepted-scope followup now exposes the three active NFS scopes to source/scan/sink. Post-followup manual rescan still fails readiness because source-status rejects receive-armed proof after same-route generation refresh: the latest sample reports HTTP 503 with `route_activation_gaps={"nfs-146":{"panda146"}}` and `route_receive_armed_gaps={"nfs-144":{"panda144"},"nfs-145":{"panda145"}}`, while `/status` is HTTP 200 and shows one-to-one sink ownership.
- Closed current seam: `POST /index/rescan` current-roots target selection no longer synchronously waits on live source-worker logical-roots/grants snapshot RPCs before preflight. `fs-meta/app/src/api/handlers.rs` now reads cached/app-authoritative roots/grants through `cached_logical_roots_snapshot_with_failure()` and `cached_host_object_grants_snapshot_with_failure()`; readiness remains proven by bounded current-roots roots-control replay, source-status runtime-scope fan-in, and scoped/generic delivery proof. The obsolete live worker `manual_rescan_logical_roots_snapshot_with_failure` fallback path was removed from `fs-meta/app/src/workers/source.rs`.
- Fresh aibox evidence: `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260615153330-gate.log` used complete source snapshots for `/root/repo/fustor` and `/root/repo/capanix`, preserved sibling layout, built `capanix_worker_host`, `libfs_meta_runtime.so`, and `fsmeta`, passed focused manual-rescan/current-roots validation, and passed `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1`.
- Aibox artifact hashes from that snapshot: `capanix_worker_host=bcd6160b808f934399d7177df48eecaac72c397318fcca9ff56f3688aa0efc55`, `libfs_meta_runtime.so=39a6945db5a71dec0d73a2168e57f5d5e4909953f676401627439047a7681502`, `fsmeta=003c94f8549e0ec980774a8ca0e6b88252e84485f6a10eb030ca2db934653b00`. These aibox ELFs are validation artifacts only; do not deploy them to the CentOS 7 cluster.
- Compatible builder evidence: complete local `fustor` and sibling `capanix` snapshots were uploaded to `panda145:/home/wanghuajin/fsmeta-build/current/fullsrc-20260615154344/`. Log `/tmp/panda145-compatible-builder-20260615154344.log` shows the focused manual-rescan/current-roots tests and `cargo fmt --check -p fs-meta-runtime` passed on `panda145`, release `fs-meta-runtime` and `fs-meta-tooling` built there, and artifact verification passed: `libfs_meta_runtime.so=9c0b22447cc298514e799f03100d484c5a4bdc2133838cec5480ae2c1180c633` with max `GLIBC_2.16`; `fsmeta=a4df79f3f0a53ca5ca497ec77a7931d43d57e07b7e3f849f5092eeee14c5ec38` with max `GLIBC_2.16`.
- Official deploy evidence: `/tmp/official-stage-20260615154344.log`, `/tmp/official-redeploy-20260615154344.log`, and redacted `/tmp/official-restore-20260615154344.log` show the `panda145` runtime/tooling artifacts staged to and installed on `panda144`, `panda145`, and `panda146`, clean-log active-three `capanixd` restart completed, official resources were re-announced, and the fs-meta app was redeployed. `/tmp/official-roots-apply-20260615154344.log` shows the official active-three roots apply succeeded. Manual rescan was later accepted with HTTP 200 `{accepted:true}` after convergence and roots apply.
- Previous official post-rescan evidence: session `15818`, collected after a conservative local wait ending `2026-06-15T16:42:13+08:00`, showed `login_status=200`, `status_http_status=200`, `status_partial=null`, `status_degraded=null`, `status_error=null`, `blocking_repair_lanes_count=0`, `runtime_artifact_sha256=9c0b22447cc298514e799f03100d484c5a4bdc2133838cec5480ae2c1180c633`, and one-to-one schedules for sink/source/scan: `panda144 -> nfs-144`, `panda145 -> nfs-145`, `panda146 -> nfs-146`.
- Prior official growth evidence: session `1468`, after a local 300s wait, showed `login_status=200`, `status_http_status=200`, `status_partial=null`, `status_degraded=null`, `status_error=null`, `runtime_artifact_sha256=9c0b22447cc298514e799f03100d484c5a4bdc2133838cec5480ae2c1180c633`, `trusted_observation_readiness=false`, sink primary owners still one-to-one, and `sink_live_nodes=18049` up from `11414`.
- Latest official stall evidence: session `56828`, collected at `2026-06-15T16:56:26+08:00` after another local 300s wait, again shows `login_status=200`, `status_http_status=200`, `status_partial=null`, `status_degraded=null`, `status_error=null`, `runtime_artifact_sha256=9c0b22447cc298514e799f03100d484c5a4bdc2133838cec5480ae2c1180c633`, `trusted_observation_readiness=false`, and sink primary owners still one-to-one: `nfs-144 -> panda144`, `nfs-145 -> panda145`, `nfs-146 -> panda146`.
- Latest materialization/growth state: `sink_live_nodes=18049`, unchanged from the prior 300s sample; sink groups are `nfs-144 live_nodes=1291 serving-degraded pending-materialization`, `nfs-145 live_nodes=10123 serving-degraded pending-materialization`, and `nfs-146 live_nodes=6635 serving-degraded pending-materialization`. All three source logical roots have `active_members=1` and `matched_grants=1`, but `last_audit_started_at_us=null` and `last_audit_completed_at_us=null` for all three roots. This is the current first raw boundary for diagnosis.
- Diagnosis evidence: authenticated `/status` diagnostic sampling after the stall showed `repair_lanes` already contained `source-scan-audit-admission-release state=completed`, sink debug contained stream receive readiness and received events on all three owners, and no status partial/degraded/error marker was present. Current active source worker logs on the three nodes showed `UpdateLogicalRootsDeferScanAudit` and `source.rescan scoped endpoint accepted and enqueued`; only the facade node logged `release_source_scan_audit_after_sink_scope_ready_if_needed ... epoch=1`, while the other source workers had no `OpenScanAuditAdmissionAndTriggerRescanWhenReadyEpoch`/admission-open log. This localizes the defect to fustor marking owner-scoped release-route collection complete from non-target source-status evidence.
- Closed source admission owner-provenance seam: `release_source_scan_audit_after_sink_scope_ready_on_owner_route` now requires owner-route release evidence to have a matching event origin and target-owner proof from scheduled source/scan groups, source primary refs, or active group-primary concrete roots before returning success; otherwise the owner route remains retryable timeout/pending evidence instead of completing falsely.
- Closed sink worker clean-log seam: `fullsrc-20260615172542` reached trusted/materialized active-three state but failed because sink worker control-frame failure logs rendered `CnxError::Timeout` through Display, producing one generic `operation timed out` blocker line. `fs-meta/app/src/workers/sink.rs` now renders timeout control-frame failures through the existing clean `timeout` classifier, preserving the error return while avoiding the forbidden generic text.
- Fresh aibox evidence for current closure: `fullsrc-20260615173742` used complete source snapshots for `/root/repo/fustor` and `/root/repo/capanix`, preserved sibling layout, built `cnxctl`, `capanix_worker_host`, `libfs_meta_runtime.so`, and `fsmeta`, passed focused source admission validation, passed the sink timeout clean-log unit, passed `cargo fmt --check -p fs-meta-runtime`, and passed `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1`.
- Aibox `fullsrc-20260615173742` final status evidence: clean-log blocker counts were zero on all three nodes, facade `state=serving`, rollout `state=stable`, readiness planes all true including `trusted_observation_readiness=true`, sink groups `nfs-144`, `nfs-145`, and `nfs-146` were each `serving-trusted` with `initial_audit_completed=true` and `materialization_readiness=ready`, sink/source/scan schedules were one-to-one by owner, and all three source roots had current audit start/completion timestamps. Aibox artifact hashes: `cnxctl=85477bdde468ce3f5fae6d607cc6e196913dab6ba339e2a6d75d1f2cd828f1a2`, `capanix_worker_host=d8616ef1258739ef2b12c3b81835e7724bec6f2b828afd16744b0ca0dfbbc44d`, `libfs_meta_runtime.so=4e537e76f7e0383dfd6fa9865f268a8e0b02662ea0df7920657a825f9ca8d3ea`, `fsmeta=19048e13e47d9b0cc2d99bfb9a5479494a44a465a89c756abb3daed2f6522f1a`. These aibox ELFs are validation artifacts only; do not deploy them to the CentOS 7 cluster.
- Compatible builder evidence: complete local `fustor` and sibling `capanix` snapshots were uploaded to `panda145:/home/wanghuajin/fsmeta-build/current/fullsrc-20260615174605/`. Logs `/tmp/panda145-compatible-builder-20260615174605.log`, `/tmp/panda145-compatible-builder-20260615174605-rerun-env.log`, and `/tmp/panda145-compatible-builder-20260615174605-rerun-worker-env.log` show the final corrected builder environment passed focused source admission validation, `scan_audit`, sink timeout clean-log unit, and `cargo fmt --check -p fs-meta-runtime`; release `fs-meta-runtime` and `fs-meta-tooling` built there, and artifact verification passed: `libfs_meta_runtime.so=c4db16ece12f9a19404069d3a8114c745454a526007fd36b3ca43ba43e3df734` with max `GLIBC_2.16`; `fsmeta=fcc9561106fa6d4bc83c97f00fd68e5317df1f556fd64a914595a54f63ca85f0` with max `GLIBC_2.16`.
- Official restore evidence for current artifacts: installed runtime/tooling hashes are `libfs_meta_runtime.so=c4db16ece12f9a19404069d3a8114c745454a526007fd36b3ca43ba43e3df734` and `fsmeta=fcc9561106fa6d4bc83c97f00fd68e5317df1f556fd64a914595a54f63ca85f0`. `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615180618` shows official roots preview matched 3 roots with zero unmatched roots, roots apply returned `roots_count=3`, and manual rescan was accepted before the accepted-scope followup gap was discovered.
- Closed official runtime-config invocation gap: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615181138/post-wait-summary.json` showed authenticated `/status` failed closed with HTTP 503, `sink_schedule_gaps={"nfs-144","nfs-145","nfs-146"}`, and config diagnostics showed source/scan/sink were still bound only to `__fsmeta_empty_roots_bootstrap`. Root cause was an operator invocation/config sequence gap: `fsmeta roots apply` was run without `--config`, `--socket`, and `--domain-id fsmeta-stable`, so the CLI did not submit its scope-only accepted-scope followup.
- Official scope-followup evidence: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615181314-scope-followup/roots-apply-followup.json` shows `runtime_apply.mode=management_api_with_accepted_scope_followup`, `relation_target_apply=scope_only_followup`, `roots_count=3`, and `app_scope_count=4`. `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615181335-post-followup-config/config-dump.json` shows `app_scopes` now include exactly `nfs-144`, `nfs-145`, `nfs-146`, and `fs-meta-tcp-listener`; source/scan/sink unit contexts are bound to all three NFS scopes.
- Latest official raw boundary after scope followup: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615181726-source-repair-single-sample` shows `/status` now returns HTTP 200 with no partial/degraded/error marker, sink primary owners are one-to-one (`nfs-144 -> panda144`, `nfs-145 -> panda145`, `nfs-146 -> panda146`), sink schedules are one-to-one, and source roots have active members/grants but no audit timestamps yet. Manual rescan still returns HTTP 503; its error reports source/scan schedules are one-to-one, but scoped delivery target proof is rejected with `route_activation_gaps={"nfs-146":{"panda146"}}` and `route_receive_armed_gaps={"nfs-144":{"panda144"},"nfs-145":{"panda145"}}`.
- Local fustor diagnosis for latest boundary: current official logs show each source owner spawned a new scoped source-rescan proxy endpoint after retiring the old generation-stale endpoint, then repeatedly logged `source status endpoint manual-rescan route not receive-armed`. The local root cause is fustor's manual-rescan readiness comparing ready proof against the latest raw activate generation in `last_control_frame_signals`, while runtime `RuntimeUnitGate` intentionally keeps a stable semantic generation across identical activate refreshes so receive-armed endpoints are not invalidated by fencing-only refresh. `fs-meta/app/src/api/handlers.rs` now accepts a ready generation older than the latest activate for the same route/group when no later deactivate exists; a later deactivate still invalidates the proof.
- Fresh aibox evidence for latest fix: `fullsrc-20260615182741` used complete source snapshots for `/root/repo/fustor` and `/root/repo/capanix`, preserved sibling layout, built `cnxctl`, `capanix_worker_host`, `libfs_meta_runtime.so`, and `fsmeta`, passed the manual-rescan semantic-generation focused tests, passed the sink timeout clean-log unit, passed `cargo fmt --check -p fs-meta-runtime`, and passed `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1`.
- Aibox `fullsrc-20260615182741` final status evidence: clean-log blocker counts were zero on all three nodes, facade `state=serving`, rollout `state=stable`, `trusted_observation_readiness=true`, sink groups `nfs-144`, `nfs-145`, and `nfs-146` were `serving-trusted`, sink/source/scan schedules were one-to-one by owner, all three source roots had current audit start/completion timestamps, and source control signals contained node-scoped `ready unit=runtime.exec.source route=source-manual-rescan.<node>:v1.req` markers. Aibox artifact hashes: `cnxctl=1fedb4d753842dccd87b82212e4b63bdb4b2a7f8822d61330dae472d8ad002db`, `capanix_worker_host=5a0173e75755e67421ea43886c516833b9b0ee85b59d828fd1ed2d3942c9cf93`, `libfs_meta_runtime.so=837c2be93ab035b221ccdcf904b1364545701228be71adce825f1b4dd8b87aa4`, `fsmeta=dd7cf1f81303645929f95680941a895ed4c045f42f8439886f60996bc7ac59a2`. These aibox ELFs are validation artifacts only; do not deploy them to the CentOS 7 cluster.
- Compatible builder evidence for latest fix: complete local `fustor` and sibling `capanix` snapshots were uploaded to `panda145:/home/wanghuajin/fsmeta-build/current/fullsrc-20260615182741/`. Log `/tmp/panda145-compatible-builder-20260615182741.log` shows the manual-rescan semantic-generation focused tests, sink timeout clean-log unit, and `cargo fmt --check -p fs-meta-runtime` passed on `panda145`; release `fs-meta-runtime` and `fs-meta-tooling` built there, and artifact verification passed: `libfs_meta_runtime.so=9eae1d2a82a9a36d96d192a7948abc50e2f92f238d330c596bbb265d48064180` with max `GLIBC_2.16`; `fsmeta=f4e1871db35851e21325009370a09ae7029ca48bdb8c3ea9d896e9db8c5a176c` with max `GLIBC_2.16`.
- Official redeploy/restore evidence for latest artifacts: `/tmp/official-redeploy-script-start-20260615182741.log` shows all three nodes were restarted through the official `nodes/<host_ref>/start.sh` scripts and installed `libfs_meta_runtime.so=9eae1d2a82a9a36d96d192a7948abc50e2f92f238d330c596bbb265d48064180` plus `fsmeta=f4e1871db35851e21325009370a09ae7029ca48bdb8c3ea9d896e9db8c5a176c`. Direct hand-written `nohup capanixd ...` was proven invalid because it omitted node identity/signing-key environment and failed with `node transport identity validation failed`; the official deploy skill now records the required `start.sh`/`stop.sh` method and `admin.env` CLI environment.
- Official restore evidence: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615182741-official-script-start-rerun-env` shows `cnxctl cluster status` returned three reachable cluster-ready peers, resource announce succeeded, source apps were applied, `fsmeta deploy` succeeded, the refreshed management account was written as enabled three-field `admin:plain$<password>:0`, roots preview matched 3 roots with zero unmatched, and roots apply returned `runtime_apply.mode=management_api_with_accepted_scope_followup`, `relation_target_apply=scope_only_followup`, `roots_count=3`, `app_scope_count=4`.
- Latest official raw boundary after a 300s local wait: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260615182741-post-restore-wait-300s/post-wait-sample.json` shows login 200, `/status` HTTP 200 with no partial/degraded/error, clean-log blocker counts all zero, one-to-one sink/source/scan schedules, and one-to-one sink primary owners, but manual rescan still returns HTTP 503. The error has no `route_activation_gaps`, `route_readiness_gaps`, or `route_receive_armed_gaps` and includes node-scoped ready markers for all three `source-manual-rescan.<node>:v1.req` routes, but current source-status target proof still reports `runners=0`/`last_runner=[]`; owner-local source-status fragments for `panda144` and `panda146` report `concrete_roots=0`, no published events, and no current concrete primary roots, while `panda145` has local concrete root/publication evidence only for `nfs-145`.
- Closed locally for latest target-proof boundary: fustor manual-rescan source-status target selection now accepts a single owner-scoped source/scan runtime-scope lane with node-scoped route-ready evidence as app-lane delivery proof even before source-status reports runner, concrete-root, or publication evidence. The new path is deliberately narrow: route-ready must be scoped to the owner object, the ready generation must correspond to an activate generation for the same route or bootstrap activation, later deactivate still invalidates it, multi-node runtime-scope groups are not promoted, and root-id self-scope/runtime-scope alone remains insufficient.
- Latest local validation for the target-proof fix: `cargo test -p fs-meta-runtime manual_rescan -- --nocapture --test-threads=1` passed with 207 passed / 1 ignored; `cargo fmt -p fs-meta-runtime -- --check` passed; `git diff --check` passed. The pre-existing `is_receive_polling` dead-code warnings remain unchanged.
- Closed fustor seams still relevant to this gate: `/index/rescan` current-roots request-liveness, `/status` local fallback runtime-scope subset fail-closed, retained sink route-scope, high-fanout sink tree updates, source scan/audit admission-release timeout, owner-transport fan-in gaps, roots file schema, and accepted-scope followup are closed. Reopen only with fresh raw evidence.
- Next bounded iteration: rerun the fresh complete-source aibox active-three real-NFS clean-log gate from the current worktree, then the fresh `panda145` compatible-builder gate, then clean-log official redeploy/restore and manual-rescan verification.

Governing policy for the remaining gate:

- Sink materialized tree lifecycle is hosting-bound and rebuildable; fs-meta must not require a durable full-tree snapshot to complete startup, scan/audit, or materialization.
- Scan/audit publication backpressure is valid bounded flow control, but it must recover and continue making progress. It must not become a permanent stall at a routine checkpoint.
- `/status` and diagnostics are observation surfaces. They may issue documented nonblocking single-flight wakes, but must not synchronously run retained replay, worker lifecycle repair, roots-control full replay, manual-rescan execution, scan/audit traversal, or sink materialization catch-up.
- `/status` must fail closed or explicitly mark partial/degraded evidence when source/sink owner fan-in or transport/materialization evidence is incomplete; it must not fabricate sink ownership or materialization from grants, route activation, stale cache, or status-only projections.
- A replay/apply-pending sink-status route-cache fallback may expose bounded progress telemetry, but must not claim trusted/materialized readiness from schedule-only evidence.
- Trusted/materialized readiness requires matching source publication and sink transport/materialization evidence. Source publication, route activation, grants, cached status, or schedule-only evidence must not claim trusted readiness when a sink owner has zero received/applied events.
- Source scan/audit admission release may occur only after current sink accepted-scope schedule evidence covers the same root groups or a permitted sink observation repair completes. That release is not source-to-sink delivery proof.

## Validation Order

1. Re-read this file and the governing specs before each bounded iteration.
2. Closed locally and crossed by fresh aibox: `/index/rescan` current-roots target selection no longer calls live source-worker roots/grants snapshot RPCs on the HTTP request path before preflight; cached/app-authoritative roots/grants are used and readiness remains proven by bounded current-roots/source-status/scoped-delivery evidence.
3. Passed: fresh complete-source aibox build/focused/active-three real-NFS clean-log gate for the current fustor change, `fullsrc-20260615153330`.
4. Passed: fresh `panda145` CentOS 7-compatible builder artifact gate with max required GLIBC at or below `GLIBC_2.17`.
5. Passed: clean-log active-three external-worker redeploy/restore, official roots apply, and manual rescan on `panda144`, `panda145`, and `panda146`.
6. Passed locally and crossed to aibox materialization: official post-rescan `/status` exposed a fustor source admission release owner-provenance bug; local fustor fix, focused scan/audit validation, and `fullsrc-20260615172542` materialized active-three evidence are complete.
7. Passed locally and crossed by fresh aibox: `fullsrc-20260615172542` failed only on sink worker generic timeout clean-log text; local log-rendering fix and focused validation are complete.
8. Passed: fresh complete-source aibox build/focused/active-three real-NFS clean-log gate for the current worktree, `fullsrc-20260615173742`.
9. Passed: fresh `panda145` CentOS 7-compatible builder artifact gate for `fullsrc-20260615174605`; deployable artifacts require max `GLIBC_2.16`.
10. Passed locally and crossed by fresh aibox: post-followup manual rescan exposed a fustor source-status route-generation freshness bug; local fix, focused tests, and `fullsrc-20260615182741` active-three real-NFS clean-log aibox validation are complete.
11. Passed: fresh `panda145` compatible-builder artifact gate for `fullsrc-20260615182741`; deployable artifacts require max `GLIBC_2.16`.
12. Passed locally: official `fullsrc-20260615182741` exposed a fustor manual-rescan target-proof gap when source-status has owner-scoped route-ready evidence but no runner/concrete-root/publication proof yet; local target-proof fix and focused `manual_rescan` validation are complete.
13. Next: repeat aibox, compatible-builder, official redeploy/restore, and manual rescan verification from the current worktree.
14. Final: authenticated `/api/fs-meta/v1/status` evidence must be complete/non-partial, trusted/materialized, active-three only, and show stable live-node index growth past `200,000,000`.

## Closed / Superseded

- Older small/high gate failures, five-node ownership/resource-overhead state, deleted `/root/repo/capanix/todo.md` active-boundary entries, and prior locally closed fustor/capanix sub-seams have been removed from the active TODO. Reopen only with fresh raw evidence.
- Superseded by `fullsrc-20260614230500` fresh high-NFS evidence and local validation: the `fullsrc-20260614223525` node-b `/status` id `16` liveness failure with the tight deferred `sink-generation-cutover` `tx busy` retry loop. Reopen only if fresh raw evidence again shows `/status` blocked behind immediate sink repair retries.
- Closed by `fullsrc-20260614180253` fresh real-small/high-NFS evidence: the `fullsrc-20260614170553` node-b facade/API liveness failure during materialization polling and the source-worker `OwnershipFast` retained-replay status race. Reopen only if fresh raw evidence again shows `/status` or `/session/login` liveness failure.
- Closed by `fullsrc-20260614170553` raw evidence and local validation: the `fullsrc-20260614163227` small-gate source/status fan-in `nfs-144` coverage gap. Reopen only if a fresh run again reports non-empty source or scan coverage gaps.
- Superseded by `fullsrc-20260614163227`: the `fullsrc-20260614160056` small-pass/high-fail boundary and the later node-c route-cache telemetry gap are closed by newer local/high-NFS evidence. Reopen only with fresh raw evidence.
- Closed by `fullsrc-20260614135801` small-gate progress: the prior node-b `/status` id `33` facade stall during pending materialization and the source scan/audit admission-release wait-for-observed-epoch seam. Reopen only if fresh raw evidence shows `/status` again waiting for scan/audit/materialization work.
- Closed locally and crossed by fresh aibox build/focused/high validation: sink full-tree statecell checkpoint blocking and generic sink-status replay/apply-pending no-reply. Reopen only if fresh raw evidence points back to checkpoint/statecell persistence or `/status` liveness.
- Closed: earlier facade owner-provenance loss and the prior `panda145` compatible-builder gate. Reopen only with fresh raw evidence.
- Closed: active-three artifact staging, clean-log live replacement/restart, resource announce, fs-meta app deploy, and roots apply on `panda144-146` for `20260614122654`. Reopen deploy mechanics only with fresh official-cluster evidence.
- Closed: official roots file schema repair and accepted-scope followup. Reopen only if fresh preview/config/status evidence shows incorrect root/grant matching or missing active-three `app_scopes`.
