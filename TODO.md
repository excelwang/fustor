# TODO

## Trace Authority

- This file, `/root/repo/fustor/TODO.md`, is the canonical goal trace for the current fs-meta active-three gate.
- Active owner is the repo named by the current first raw failing boundary. The periodic-audit completion timestamp fix has passed focused local validation, aibox, compatible-builder, and official clean-log redeploy. Official follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup1` proved count growth above `200000000`, but status still had all sink groups `pending-materialization`, top-level trusted readiness false, and source audit-completed timestamps null; current owner remains `fustor` until read-only follow-ups prove every final acceptance item on the deployed fix. The read-only summary analyzer was fixed after followup1 because it incorrectly reported `anomalies=none` while omitting readiness/source timestamp checks.
- Keep this file short. Move superseded iteration detail out of the active trace instead of accumulating a chronological log.
- Do not prune `Final Target` or `Final Acceptance` bullets as stale history without explicit user approval; they are invariant acceptance criteria, especially the `/status` index-entry target.

## Final Target: Active-Three fsmeta-stable

- Target cluster is exactly three active participants: `panda144` / `10.0.82.144`, `panda145` / `10.0.82.145`, and `panda146` / `10.0.82.146`.
- Active logical roots are exactly `nfs-144`, `nfs-145`, and `nfs-146`.
- Expected ownership is one-to-one:
  - `panda144::nfs-144` -> source/scan/sink owner `panda144`.
  - `panda145::nfs-145` -> source/scan/sink owner `panda145`.
  - `panda146::nfs-146` -> source/scan/sink owner `panda146`.
- Capanix is authoritative for bind/run realization and accepted scopes. fs-meta must consume that evidence and must not fabricate ownership/materialization from local grants, stale cache, or status-only projections.

## Current Boundary

- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup7` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup7.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup7`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=603000837`, per-root `nfs-144=201000279`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+20640487`, rate `20697.86/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, runtime artifact SHA `af497f65fc003ce394142795e1b80260c9b76fc21a7334ed8c297f62fa54aa71`, all sink groups `serving-trusted` / `ready` / `initial_audit_completed=true`, and top-level `trusted_observation_readiness=true`. Remaining defect: source concrete roots are active/serving with settled publication but `last_audit_completed_at_us=null` while the next periodic audit is in progress; this keeps the invariant source completed-timestamp acceptance item unproven. No `/index/rescan` was called.
- Fustor local fix for the remaining source timestamp boundary: `fs-meta/app/src/source/mod.rs` now preserves previous audit completion evidence when the reason is `periodic`, both at periodic rescan request and periodic audit start, while manual/overflow/initial repair paths still invalidate previous completion evidence. The duplicate-request audit-inflight guard now treats `started_at_us > completed_at_us` as in-flight, so preserving the old completed timestamp cannot let another request overwrite a running periodic audit.
- Local validation for the source timestamp fix passed: `cargo test -p fs-meta-runtime audit_completion_evidence -- --nocapture` (`2` passed), `cargo test -p fs-meta-runtime duplicate_pending_manual_rescan_intent_preserves_inflight_audit_evidence -- --nocapture`, `cargo test -p fs-meta-runtime materialized_status_cache -- --nocapture` (`5` passed), `cargo test -p fs-meta-runtime readiness_group_observation_ -- --nocapture` (`6` passed), `cargo test -p fs-meta-runtime trusted_observation_readiness_ -- --nocapture` (`11` passed), `cargo fmt -p fs-meta-runtime -- --check`, and `git diff --check -- fs-meta/app/src/source/mod.rs fs-meta/app/src/source/tests.rs fs-meta/app/src/query/observation.rs fs-meta/app/src/api/handlers.rs TODO.md .tmp/run-official-clean-redeploy-current.sh`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618180955` passed from the current Fustor/Capanix worktrees after the source periodic-audit timestamp fix. It ran in local foreground exec session `67851` with 300s empty `write_stdin` continuation; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618180955-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618180955`; sentinel `AIBOX_GATE_PASS fullsrc-20260618180955`. It passed Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry/trusted-readiness tests including `audit_completion_evidence`, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. The local replay final status exposed local `sink.live_nodes=42`, per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, all sink groups ready/trusted, `trusted_observation_readiness=true`, and all three source concrete roots with non-null `last_audit_completed_at_us`. It did not collect a new official authenticated `/status` sample.
- `panda145` compatible-builder gate `fullsrc-20260618180955` passed from complete source snapshots. It ran in local foreground exec session `86653` with 300s empty `write_stdin` continuations; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618180955.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618180955`; sentinel `COMPAT_BUILDER_PASS fullsrc-20260618180955`. It passed Capanix focused/full compatible tests, Capanix release build, Fustor focused compatible tests including the new `audit_completion_evidence` path, Fustor release build, artifact hash verification, and max-GLIBC checks. Deployable artifact hashes are `capanixd=227b632cf0a0d6f98916a62b4d7535f9d3476733d8174f8db582a69b8a16e9c6`, `cnxctl=602d5cf911e5b6a9d2e7e2f019236017c0b25e4338186f7704dc75155739b3ce`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=1fdc15a66c61bc20669b888f4650f5c80955b89cdb0a6fa3707df3d69f70f900`, `fsmeta=b171293fe402184cdb1d9d5c5f171d1a0f3f6bd69378a692760077c62c451b12`; all five deployable ELF files require at most `GLIBC_2.16`. It did not collect a new official authenticated `/status` sample.
- Latest official clean-log redeploy `fullsrc-20260618180955-official-20260618183549-clean1` completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618180955-official-20260618183549-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618180955-official-20260618183549-clean1-clean-redeploy`; post-wait summary `.../post-deploy-status/status-count-summary.json`. It staged and replaced compatible-builder `fullsrc-20260618180955` artifacts on `panda144`, `panda145`, and `panda146` with expected hashes, verified self-NFS mappings `144->144`, `145->145`, `146->146`, cleared logs, used node-owned start scripts, restored resources/app/roots, waited locally `900s`, and collected one authenticated `/status` sample. The sample returned HTTP 200 with reportable `sink.live_nodes=104136686`, per-root `nfs-144=39234081`, `nfs-145=21127131`, `nfs-146=43775474`, no missing per-root/per-peer live-node data, no zero-live roots, one-to-one `sink.primary_host_ref_by_group`, one-to-one source/scan schedules, and source concrete roots active/serving with no `last_error`, no `rescan_pending`, no overflow, and publication evidence. All three sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, top-level `trusted_observation_readiness=false`, source `last_audit_completed_at_us=null` during initial audit, and aggregate remains below `200000000`. The negative delta versus the pre-redeploy candidate is a clean-redeploy baseline reset comparison; next read-only sample must compare against this post-redeploy summary. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup1` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618180955-clean1-followup1`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=258335629`, per-root `nfs-144=89741352`, `nfs-145=86776874`, `nfs-146=81817403`, delta `+154198943`, rate `149831.09/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and count above `200000000`. However, direct status body inspection showed all sink groups still `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, top-level `trusted_observation_readiness=false`, source `last_audit_completed_at_us=null` for all roots, and a small publication gap on `nfs-146`; the read-only summary analyzer was stale and missed these anomalies. `.tmp/run-official-readonly-status-current.sh` is now fixed to fail-closed on sink readiness, top-level readiness, source concrete-root timestamps, and publication gaps. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup2` completed with `/status` HTTP 503 and the fixed summary analyzer reported no count: missing reportable count, missing per-root live-node fields for all three roots, missing sink primary/group/source concrete-root fields, and top-level readiness absent. Status body localized the fail-closed cause to `nfs-144` sink fan-in: `sink_schedule_gaps={"nfs-144"}`, `sink_owner_partition_gaps={"nfs-144":"missing_sink_owner"}`, and `sink_owner_transport_gaps={"nfs-144"}` while `source_route=ok`, `sink_route=ok`, and source/scan coverage gaps were empty. Cluster status in the same sample still showed all three nodes reachable/gossip-converged/cluster-ready with `active_processes=1`, and config dump retained correct self-NFS resources/scopes. Repair lanes showed `status-triggered-sink-observation-repair` inflight. No `/index/rescan` was called; next step is another read-only convergence window, then bounded node logs if the `nfs-144` sink fan-in gap persists.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup3` completed with `/status` HTTP 200 and healthy count growth: `sink.live_nodes=419680992`, per-root `nfs-144=141840519`, `nfs-145=143043653`, `nfs-146=134796820`, delta `+161345363` versus the last count-bearing followup1, rate `77929.67/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and aggregate above `200000000`. Remaining non-final state is expected high-NFS catch-up: all sink groups still `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, top-level `trusted_observation_readiness=false`, and source `last_audit_completed_at_us=null` for all roots with only `-1` publication-gap accounting offsets. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup4` completed with `/status` HTTP 200 and healthy count growth: `sink.live_nodes=491617346`, per-root `nfs-144=164571258`, `nfs-145=168050468`, `nfs-146=158995620`, delta `+71936354`, rate `74959.05/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and aggregate above `200000000`. Remaining non-final state is still high-NFS catch-up: all sink groups `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, top-level `trusted_observation_readiness=false`, and source `last_audit_completed_at_us=null` for all roots. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup6` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup6.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup6`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=582360350`, per-root `nfs-144=199419732`, `nfs-145=196194633`, `nfs-146=186745985`, delta `+73928000`, rate `72647.07/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence. Count acceptance is met, but all sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup5` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup5.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup5`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=508432350`, per-root `nfs-144=181194864`, `nfs-145=167520345`, `nfs-146=159717141`, delta `+78331120`, rate `78905.54/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence. Count acceptance is met, but all sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup4` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup4.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup4`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=430101230`, per-root `nfs-144=156557726`, `nfs-145=140123185`, `nfs-146=133420319`, delta `+74695056`, rate `74800.34/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence. Count acceptance is met, but all sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup3` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup3.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup3`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=355406174`, per-root `nfs-144=131480897`, `nfs-145=115064136`, `nfs-146=108861141`, delta `+79804205`, rate `79972.56/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence. Count acceptance is met, but all sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup2` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup2.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup2`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=275601969`, per-root `nfs-144=106656788`, `nfs-145=87156076`, `nfs-146=81789105`, delta `+89667001`, rate `88608.90/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence. Count acceptance is met, but all sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup1` completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup1`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=185934968`, per-root `nfs-144=78562470`, `nfs-145=56756958`, `nfs-146=50615540`, delta `+124987086`, rate `110565.85/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, one-to-one sink/source/scan ownership, source concrete roots active/serving with `last_error=null`, `rescan_pending=false`, publication/forwarding evidence, and all sink groups still `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`. The only summary anomaly is `below_200000000`; continue patient read-only convergence. No `/index/rescan` was called.
- Latest official clean-log redeploy `fullsrc-20260618152056-official-20260618153719-clean1` completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618152056-official-20260618153719-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618152056-official-20260618153719-clean1-clean-redeploy`; post-wait summary `.../post-deploy-status/status-count-summary.json`. It staged and replaced compatible-builder `fullsrc-20260618152056` artifacts on `panda144`, `panda145`, and `panda146` with expected hashes, verified self-NFS mappings `144->144`, `145->145`, `146->146`, cleared logs, used node-owned start scripts, restored resources/app/roots, waited locally `900s`, and collected one authenticated `/status` sample. The sample returned HTTP 200 with reportable count `sink.live_nodes=60947882`, per-root `nfs-144=22924310`, `nfs-145=21798478`, `nfs-146=16225094`, no missing per-root/per-peer live-node data, no zero-live roots, one-to-one `sink.primary_host_ref_by_group`, one-to-one source/scan schedules, and source concrete roots active/serving with no `last_error`, no `rescan_pending`, and publication evidence. All three sink groups remain `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, top-level `trusted_observation_readiness=false`, and aggregate remains below `200000000`. The negative delta versus pre-redeploy follow-up8 is a clean-redeploy baseline reset comparison; next read-only sample must compare against this post-redeploy summary. No `/index/rescan` was called.
- Current latest boundary supersedes older bullets in this section unless explicitly reopened by fresh raw evidence. Earlier superseded count-bearing official authenticated `/status` sample `official-status-anomaly-sample-20260618034954` returned HTTP 200 with reportable index count `sink.live_nodes=14688792`, per-root `nfs-144=0`, `nfs-145=14688792`, `nfs-146=0`, delta `0` over `402.97s`, rate `0/s`, no count regression, no missing per-root live-node fields, zero-live roots `nfs-144` and `nfs-146`, growth below `10000/s`, aggregate below `200000000`, and route-timeout repair-lane evidence. No `/index/rescan` was called or authorized.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-20260618054623`: HTTP 200, reportable index count `sink.live_nodes=18941398`, per-root `nfs-144=6256112`, `nfs-145=6550330`, `nfs-146=6134956`, delta `+18941398` over corrected file-mtime elapsed `311.955s` from the previous zero-count clean-log sample, rate `60718.36/s`, no missing per-root live-node fields, no count regression, no zero-live roots, no slow-growth anomaly, and still below `200000000`. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-20260618054623/status-count-summary.json`. No `/index/rescan` was called or authorized.
- The preceding clean-log sample remains the first raw failure for deploy validation: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618044930-official-2026061805-clean1-clean-redeploy/post-deploy-status/status-count-summary.json` returned HTTP 200 with `sink.live_nodes=0`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, delta `-14688792`, rate `-16320.88/s`, no missing per-root fields, count regression, slow growth, zero-live roots, and below `200000000`.
- Clean-log root cause is Fustor runtime-owned source->sink convergence using an intent-only rescan trigger while scan/audit admission stayed closed. The zero-count `/status` body had correct one-to-one sink owners and scheduled groups, but all source concrete roots had `rescan_pending=true`, `last_audit_started_at_us=null`, `last_audit_completed_at_us=null`, `emitted_event_count=0`, and `forwarded_event_count=0`; its repair lanes placed `source-scan-audit-admission-release` `inflight`. The bounded node logs under `/tmp/fsmeta-node-logs-fullsrc-20260618044930-clean1-all` show `release_source_scan_audit_after_sink_scope_ready_if_needed ... ok` only around/after that status request, and the later single read-only status sample then showed all three roots counting. Therefore the 900s deploy wait occurred before runtime-owned admission release; status was acting as the first wake rather than just observation.
- Fustor local fix: `fs-meta/app/src/runtime_app.rs` now uses `open_scan_audit_admission_and_trigger_rescan_when_ready_epoch_with_failure()` for runtime-owned source->sink convergence triggers in post-recovery initial/retry, local sink republish retrigger, deferred query-peer pretrigger, and initial mixed source/sink pretrigger paths. This preserves `/index/rescan` fail-closed policy: it does not call the dangerous API and only opens the already-authorized internal source/scan audit admission before submitting the rescan intent. `fs-meta/app/src/runtime_app/tests.rs` adds `runtime_source_to_sink_convergence_opens_scan_audit_admission`; `fs-meta/app/src/workers/source.rs` marks the retained intent-only helper as dead-code-allowed because runtime must no longer use it for convergence.
- Local validation for this Fustor fix passed: `cargo test -p fs-meta-runtime runtime_source_to_sink_convergence_opens_scan_audit_admission -- --nocapture`, `cargo test -p fs-meta-runtime scan_audit_admission -- --nocapture`, `cargo test -p fs-meta-runtime sink_recovery_machine_local -- --nocapture`, `cargo test -p fs-meta-runtime management_write_recovery -- --nocapture`, `cargo fmt -p fs-meta-runtime -- --check`, and `git diff --check -- fs-meta/app/src/runtime_app.rs fs-meta/app/src/runtime_app/tests.rs fs-meta/app/src/workers/source.rs`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618055810` failed before any new official deploy or official `/status` count sample. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618055810-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618055810`, sentinel `AIBOX_GATE_FAIL`. First raw boundary: local active-three real-NFS `apply-release` on `node-b` returned `relation_target_apply local config apply timed out after 55s` after `30505ms`; evidence `/tmp/fsm-e2e/e658d300d3619ce3/cluster5/capanix-apply-release-node-evidence-29892537409554111172444179.json`. Node-b was still alive and logs showed route activation progress through fs-meta facade/source routes. This gate exposed no new live-node/index-entry count; no `/index/rescan` was called.
- Capanix root cause: production `STARTUP_PLAN_APPLY_TIMEOUT=30s` was too tight for the active-three route-scoped startup/apply load after serial per-route activation dispatch, and relation-target apply was rewriting all inner `CtlErrorCode::Timeout` responses as a generic local apply timeout. Capanix local fix: `crates/runtime/src/engine/startup.rs` raises production startup-plan apply timeout to `60s`; `crates/runtime/src/engine/control_loop_runtime.rs` raises production relation-target local apply timeout to `100s` and only relabels true wrapper `local config apply timed out` responses, preserving specific inner `config commit failed: startup apply timed out` evidence; `crates/runtime/src/engine/control_loop_runtime/tests.rs` adds both response-mapping regressions; `crates/runtime/src/engine/startup/tests/deferred_activation_commit_config.rs` keeps the blocked activation test bounded but less scheduling-fragile.
- Local Capanix validation passed after this fix: `cargo test -p capanix-runtime relation_target_apply_config_response -- --nocapture`, `relation_target_apply_fails_closed_when_local_config_apply`, `relation_target_apply_fails_closed_when_local_activation_dispatch_blocks_after_config_set`, `cargo test -p capanix-cli relation_target_runtime_admin_client_timeout_covers_runtime_mutation_budget -- --nocapture`, `cargo test -p capanix-daemon relation_target_runtime_admin_timeout_covers_inner_quorum_and_rollback_budget -- --nocapture`, `cargo test -p capanix-runtime issue_activations -- --nocapture` (`30` passed), full `cargo test -p capanix-runtime -- --nocapture` (`289` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check` for the touched runtime files.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618061641` failed before any new official deploy or official `/status` count sample. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618061641-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618061641`, sentinel `AIBOX_GATE_FAIL`. First raw boundary: local active-three real-NFS `apply-release` on `node-b` returned `config commit failed: activation apply timed out` after `30333ms`; evidence `/tmp/fsm-e2e/9407ee4e54a4b829/cluster5/capanix-apply-release-node-evidence-29892556088882992546578451.json`. Node-b stayed alive; its stderr showed successful fs-meta activation progress through `25` unique route keys and `71` facade activation rows, with source/sink worker control frames returning ok through `source-status`. This gate exposed no live-node/index-entry count; no `/index/rescan` was called.
- Capanix root cause: the previous fix raised startup-plan and relation-target local apply budgets, but production `ACTIVATION_APPLY_TIMEOUT` still remained `30s`. Active-three fs-meta activation dispatch is route-scoped and same-pid route controls are serialized so that a stuck route still fails closed at the per-route `6s` dispatch timeout; a large set of successful route activations can legitimately exceed the old `30s` outer activation window. The outer activation timeout was therefore cutting off progressing work and masking route-level evidence as generic `activation apply timed out`.
- Capanix local fix: `crates/runtime/src/engine/startup.rs` now sets production activation apply timeout to `60s`, matching production startup-plan apply timeout while preserving the per-route dispatch timeout and blocked-route fail-closed behavior. The production timeout values are explicit constants, and `crates/runtime/src/engine/startup/tests.rs` adds `production_activation_apply_budget_covers_startup_plan_budget` so activation cannot silently regress below the startup-plan budget again.
- Local Capanix validation passed after this fix: `cargo test -p capanix-runtime production_activation_apply_budget_covers_startup_plan_budget -- --nocapture`, `cargo test -p capanix-runtime commit_config_submission_fails_closed_on_activation_route_timeout -- --nocapture`, `cargo test -p capanix-runtime issue_activations_with_timeout_does_not_leave_proc_ctl_running_after_bounded_return -- --nocapture`, `cargo test -p capanix-runtime issue_activations -- --nocapture` (`30` passed), full `cargo test -p capanix-runtime -- --nocapture` (`290` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check -- crates/runtime/src/engine/startup.rs crates/runtime/src/engine/startup/tests.rs`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618063119` failed at the wrapper/environment layer before real-NFS replay, official deploy, or any `/status` count sample. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618063119-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618063119`, sentinel `AIBOX_GATE_FAIL`. First raw boundary: `rustc-LLVM ERROR: IO failure on output stream: No space left on device` while compiling `fs-meta-runtime` tests; `/tmp` was full because stale aibox `fullsrc-*` complete build trees consumed `49G`. No live-node/index-entry count was exposed; no `/index/rescan` was called.
- Aibox environment fix: `/tmp/run-aibox-fsmeta-gate-current.sh` now removes stale `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-*` directories at startup before creating the fresh full-source preflight root. The stale complete build trees were deleted, leaving `/tmp` at `49G` available (`20%` used) and `/tmp/aibox-fsmeta-build` at `6.0M`; existing small gate logs were preserved.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618063929` passed from the current Fustor/Capanix worktrees. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618063929-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618063929`, sentinel `AIBOX_GATE_PASS`. It passed Capanix full `capanix-kernel` (`274` unit tests and `151` specs), full `capanix-runtime` (`290` unit tests, `4` route-relation tests, `121` specs, `9` config tests), focused runtime timeout/unit-tick checks, release builds, Fustor focused admission/recovery/manual-rescan/status-fan-in/retry-classifier suites including serial `manual_rescan_` (`217` passed, `1` ignored), formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618063929` artifact hashes: `capanixd=eabcee7b8a472c7adca618944bad1bf88eae68bea479ca9a8f7f757b9c61399c`, `cnxctl=65da2d542d2c8fc17c2b5d6bc1feaec0800c68eb62d76f1c6c498113ca743d0d`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=fc4c831cac976e968989f69e3e5ad6d15ff4973785f8141a15548ae3c5915c66`, `fsmeta=8af82ef3975ac15fba9499bc9fa92b0eef82a5ebab56edaa0b920a2883babc99`.
- Aibox `fullsrc-20260618063929` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node fields, no local count regression versus the prior local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; the `<10000/s` and `below_200000000` production anomaly gates remain required for official `/status` samples.
- `panda145` compatible-builder gate `fullsrc-20260618063929` failed closed before artifact verification, official deploy, or official `/status` sampling. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618063929.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618063929`, sentinel `COMPAT_BUILDER_FAIL`. First raw boundary: full `capanix-runtime` on the CentOS 7 builder failed `commit_startup_plan_with_timeout_cancels_underlying_tx_execute_after_timeout`; the wrapper returned `Timeout`, but after the test released the blocked `tx_execute`, the isolated task still completed and sent `Ok(())`. No live-node/index-entry count was exposed; no `/index/rescan` was called.
- Capanix root cause: `await_startup_owned_result_with_timeout()` split result waiting and abort into separate threads. Under builder scheduling, the public wait could return `Timeout` before the abort thread ran, leaving a cooperative but still blocked isolated startup task alive briefly after the caller had already failed closed. Releasing the test `tx_execute` during that race allowed a late completion, weakening the intended cancellation guarantee.
- Capanix local fix: `/root/repo/capanix/crates/runtime/src/engine/startup.rs` now sets an atomic cancellation flag on timeout, obtains and aborts the task handle before returning, and has the worker abort immediately if cancellation was requested before the isolated task handle was published. `/root/repo/capanix/crates/runtime/src/engine/startup/tests.rs` adds `tx_execute_entered_tx` evidence to the test kernel, and `deferred_activation_commit_config.rs` rewrites the startup timeout test to wait until `tx_execute` is truly blocked before asserting bounded timeout and no late completion.
- Local Capanix validation passed after this fix: `cargo test -p capanix-runtime commit_startup_plan_with_timeout_cancels_underlying_tx_execute_after_timeout -- --nocapture --test-threads=1`, `commit_config_submission_blocks_on_non_cooperative_tx_execute_today`, `issue_activations_with_timeout_does_not_leave_proc_ctl_running_after_bounded_return`, `commit_config_submission_fails_closed_on_activation_route_timeout`, full `cargo test -p capanix-runtime -- --nocapture` (`290` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check -- crates/runtime/src/engine/startup.rs crates/runtime/src/engine/startup/tests.rs crates/runtime/src/engine/startup/tests/deferred_activation_commit_config.rs`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618065801` passed from the current Fustor/Capanix worktrees after the Capanix startup-timeout cancellation fix. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618065801-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618065801`, terminal result `aibox gate complete`. It passed Capanix full `capanix-kernel`, full `capanix-runtime`, focused runtime timeout/unit-tick checks, release builds, Fustor focused admission/recovery/manual-rescan/status-fan-in/retry-classifier suites, formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618065801` artifact hashes: `capanixd=3900b46a81ebb6203084042f6d63e887c281079d73210ed18abd7a334ba0dbc2`, `cnxctl=edff7d0ee2e6b569850309194158049b3f92738e565d9d0d44056251ef449315`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=12d6f7c49306b97ca03f9e3625d7d4192f8ab3b9b84d1a7c97931331393a6237`, `fsmeta=fa063b45540ac7cd6d9491fbcddc11ab83c836180795ad74a139d45d8de196ff`.
- Aibox `fullsrc-20260618065801` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root or peer live-node fields, no local count regression versus the previous local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; the `below_200000000` and `<10000/s` growth anomaly gates remain required for official high-cardinality `/status` samples.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618071209` passed from complete source snapshots. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618071209.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618071209`, sentinel `COMPAT_BUILDER_PASS`. It passed the previously failing Capanix startup-timeout cancellation test on CentOS 7, adjacent Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release builds, Fustor focused admission/recovery/manual-rescan/status-fan-in/fail-closed/retry-classifier tests, Fustor release builds, and artifact verification.
- Compatible-builder `fullsrc-20260618071209` artifact hashes: `capanixd=ad4eba7cebbc1b08f4d31ca7f20150fcb23d8d885ecf240ea1ec42978fd0de85`, `cnxctl=6af24cd5fa8e600074f28e517822134bcc11c0a59131efe04ba5294326cb0ae3`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=02bd84f01ded7efd23f9421d27a7627859724375f688f88558d9ba2b1bb36286`, `fsmeta=2cb89cdcb8c1f88baa4baaefdca48a67a87d2b38cc8be22cbedab257a00e1483`; all five deployable ELF files require max `GLIBC_2.16`.
- Compatible-builder `fullsrc-20260618071209` did not collect an official authenticated `/status` sample, so it exposed no new official live-node index count. It permitted official clean-log redeploy from these artifacts. Do not call `/index/rescan`.
- Official clean-log redeploy `fullsrc-20260618071209-official-2026061807-clean1` completed with sentinel `OFFICIAL_REDEPLOY_PASS`. Log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618071209-official-2026061807-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618071209-official-2026061807-clean1-clean-redeploy`, status sample `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618071209-official-2026061807-clean1-clean-redeploy/post-deploy-status`. It staged and installed all five compatible-builder hashes on `panda144`, `panda145`, and `panda146`, cleared live logs, started each node through its own `nodes/panda14x/start.sh`, verified self-NFS mappings (`panda144 -> 10.0.82.144:/data/fustor-nfs`, `panda145 -> 10.0.82.145:/data/fustor-nfs`, `panda146 -> 10.0.82.146:/data/fustor-nfs`), restored resources/app/roots, waited locally `900s`, and collected one authenticated read-only `/status` sample. No `/index/rescan` was called.
- Latest official count-bearing authenticated `/status` is now `fullsrc-20260618071209-official-2026061807-clean1/post-deploy-status`: HTTP 200, reportable index count `sink.live_nodes=44072544`, per-root `nfs-144=14696104`, `nfs-145=14688315`, `nfs-146=14688125`, delta `+25131146` from previous official sample `official-readonly-status-20260618054623`, rate `3455.81/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, but still below `200000000` and below the `10000/s` growth floor. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618071209-official-2026061807-clean1-clean-redeploy/post-deploy-status/status-count-summary.json`.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618071209-clean1-followup1` completed in foreground exec session `70043` using three 300s empty `write_stdin` continuations after the initial command yield. Log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618071209-clean1-followup1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618071209-clean1-followup1`, summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618071209-clean1-followup1/status/status-count-summary-corrected.json`. The authenticated `/status` sample returned HTTP 200 with unchanged reportable count `sink.live_nodes=44072544`, per-root `nfs-144=14696104`, `nfs-145=14688315`, `nfs-146=14688125`, delta `0` over `1287.33s`, rate `0/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`. No `/index/rescan` was called.
- Corrected follow-up evidence shows Capanix transport is not the current boundary: `cluster-status.json` has `panda144`, `panda145`, and `panda146` all `reachable=true`, `gossip_converged=true`, `readiness_phase=cluster_ready`, `active_processes=1`, `peer_unreachable_buffered=0`, and `backpressure_events=0`; config dump still shows each NFS client mounted its own server (`10.0.82.144:/data/fustor-nfs`, `10.0.82.145:/data/fustor-nfs`, `10.0.82.146:/data/fustor-nfs`) with one-to-one root scopes.
- Fresh first raw boundary is Fustor source worker publication backpressure: all three source concrete roots are group-primary and active with scan/watch enabled, but remain `participation_state=joining`, `last_audit_completed_at_us=null`, `last_error=backpressure`, and no further count growth. Source had emitted/forwarded about `14.7M` events per root, and sink received about the same counts, but the audit epoch never completed; sink groups therefore stayed `serving-degraded`, `pending-materialization`, `initial_audit_completed=false`, and `trusted_observation_readiness=false`.
- Bounded official source-scan logs `/tmp/fsmeta-node-logs-readonly-followup1-source-scan` show source-status routes and `release_source_scan_audit_after_sink_scope_ready_if_needed ... ok` on all three nodes, with no new peer/gossip failure. This keeps the active owner on Fustor source publication behavior, not Capanix reachability or `/index/rescan`.
- Superseded Fustor local attempt: `fs-meta/app/src/workers/source_server.rs` previously treated single-event `Backpressure` as indefinitely transient and retried with the existing source-worker publish backoff. Later official `fullsrc-20260618084203` evidence proved that unbounded retry can still leave all three roots in `joining` with `last_error=backpressure` and audit epoch-end undelivered, so do not rely on that attempt as closure.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618081922` passed from the current Fustor/Capanix worktrees after the source publication backpressure fix. It ran in foreground exec session `71357`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618081922-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618081922`; sentinel `AIBOX_GATE_PASS fullsrc-20260618081922`. It passed Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry tests including `source_pump_` and `current_pub_stream_`, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. Artifact hashes: `capanixd=e60481260a03a5bedfad9492f2cfb2a947fc7b63efbc66393db92f9f79aae4b1`, `cnxctl=e411a1e8e2ec47094434e0327f1bee791eb9b0018a2e5780282e17e3e163d317`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=2f4608f640477a395355b8623ab03558a71d807d04fb093e1fac233b526eeb01`, `fsmeta=b3d6b542cb5ebe3a22ba1b428627aecdd1f5c5e62648262706a77115d935706b`.
- Aibox `fullsrc-20260618081922` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node fields, no local count regression versus the previous local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `trusted_observation_readiness=true`, and one-to-one source/scan/sink schedules. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; the official `below_200000000` and `<10000/s` anomaly gates remain required for official high-cardinality `/status` samples.
- `panda145` compatible-builder gate `fullsrc-20260618081922` failed closed before artifact verification, official deploy, or any official `/status` sample. It ran in foreground exec session `71303`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618081922.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618081922`; sentinel `COMPAT_BUILDER_FAIL fullsrc-20260618081922 rc=101`. First raw boundary: full `capanix-runtime` on the CentOS 7 builder failed `commit_config_submission_does_not_wait_on_drained_predecessor_before_activation_fail_closed`; the response was fail-closed `CtlErrorCode::Timeout` with `config commit failed: startup apply timed out`, while the test expected only `TxFailed` route-level activation evidence. No official status-returned index count was collected and no `/index/rescan` was called.
- Capanix root cause: the test used one `proc_ctl_block_ms=200` to drive both deferred predecessor deactivation and successor activation timeout while cfg(test) `STARTUP_PLAN_APPLY_TIMEOUT` is `100ms` and per-route activation dispatch timeout is `20ms`. Under slow full-suite/builder scheduling, deferred predecessor deactivate can consume the outer startup wrapper budget before successor activation route evidence is produced. This is a brittle test-budget boundary, not permission to broaden runtime fail-open behavior.
- Capanix local fix: `/root/repo/capanix/crates/runtime/src/engine/startup/tests/deferred_activation_commit_config.rs` now sets that drained-predecessor test's shared `proc_ctl_block_ms` to `40ms`, which remains above the per-route dispatch timeout but below the startup wrapper timeout. The test still proves successor activation fails closed with `activation blocked routes=1` and preserves `operation timed out`, while avoiding accidental startup-wrapper cancellation. Local validation passed: `cargo test -p capanix-runtime commit_config_submission_does_not_wait_on_drained_predecessor_before_activation_fail_closed -- --nocapture --test-threads=1`, `commit_config_submission_fails_closed_on_activation_route_timeout`, `commit_startup_plan_with_timeout_cancels_underlying_tx_execute_after_timeout`, full `cargo test -p capanix-runtime -- --nocapture` (`290` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check -- crates/runtime/src/engine/startup/tests/deferred_activation_commit_config.rs`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618084203` passed from the current Fustor/Capanix worktrees after the Capanix test-budget fix. It ran in foreground exec session `19868`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618084203-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618084203`; sentinel `AIBOX_GATE_PASS fullsrc-20260618084203`. It passed Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry tests including `source_pump_` and `current_pub_stream_`, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. Artifact hashes: `capanixd=0c35a0568269ac72df1a715e0f4c44ab416be18ea59cca66fb725e3b21f45fc1`, `cnxctl=5ad1450a0949d9a9845fc74b25af9d9fae17483b3e8762ba38648b0df47b2452`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=ade1c76cfe38646c821866050fab6b47494072b4678c11928e3e931f2ed0a5db`, `fsmeta=9d60f69365febb2fa593992796b46fcd34c0de19ee335d4d5988409ba765f193`.
- Aibox `fullsrc-20260618084203` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root or per-peer live-node fields, no local count regression versus the previous local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; the official `below_200000000` and `<10000/s` anomaly gates remain required for official high-cardinality `/status` samples. No official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618084203` passed from complete source snapshots. It ran in foreground exec session `15490`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618084203.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618084203`; sentinel `COMPAT_BUILDER_PASS fullsrc-20260618084203`. It passed Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release builds, Fustor focused admission/source/status/manual-rescan/fail-closed/retry tests including `source_pump_` and `current_pub_stream_`, Fustor release builds, and artifact verification. Compatible-builder artifact hashes: `capanixd=0ba984c0d98948e2916e82a5a72d39050f521850b463cfe8eb41224bc8399f8c`, `cnxctl=fc55d12e271245948929cfffaf512333a8b41406393cc867b8db88ed497ed760`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=9d76ea1bfa9b9bf00343e9dbb1128227d8ab628bd853c34b1de63e1b7b11c648`, `fsmeta=037c2f178e6e7dbbb153387a911cde837cbedf14ca9c290431fbd7bcddbec260`; all five deployable ELF files require max `GLIBC_2.16`. No official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Official clean-log redeploy `fullsrc-20260618084203-official-2026061809-clean1` passed in foreground exec session `34173`. Log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618084203-official-2026061809-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618084203-official-2026061809-clean1-clean-redeploy`; post-wait sample `.../post-deploy-status`. It verified all three self-NFS mounts (`panda144 -> 10.0.82.144:/data/fustor-nfs`, `panda145 -> 10.0.82.145:/data/fustor-nfs`, `panda146 -> 10.0.82.146:/data/fustor-nfs`), staged and replaced all five artifacts, started each node through its own `nodes/panda14x/start.sh`, restored resources/app/roots, waited locally 900s, and collected one authenticated `/status` sample. No `/index/rescan` was called.
- Official post-deploy `/status` count from `fullsrc-20260618084203-official-2026061809-clean1`: HTTP 200, reportable index count `sink.live_nodes=44064847`, per-root `nfs-144=14688178`, `nfs-145=14688335`, `nfs-146=14688334`, delta `-7697` over `4503.58s` vs previous count `44072544`, rate `-1.71/s`, no missing per-root or per-peer live-node data, no zero-live roots, but anomalies `count_regression`, `growth_below_10000_per_second`, and `below_200000000`. Cluster status showed all three nodes reachable, gossip-converged, `cluster_ready`, `active_processes=1`, no peer-unreachable buffer, and no backpressure events.
- Current official raw boundary after the clean redeploy remains Fustor source publication/materialization, not Capanix reachability or NFS mapping: `/status` shows one-to-one sink owners and sink/source primary maps, but all three source concrete roots are `participation_state=joining`, `last_error=backpressure`, `last_audit_started_at_us` present, `last_audit_completed_at_us=null`, and all sink groups remain `serving-degraded` / `pending-materialization` with `initial_audit_completed=false` and `trusted_observation_readiness=false`. Bounded node logs `/tmp/fsmeta-node-logs-fullsrc-20260618084203-postdeploy-source-scan` show source-status route activation and `release_source_scan_audit_after_sink_scope_ready_if_needed ... ok` on all three nodes without fresh peer/gossip failure. Do not call `/index/rescan`.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618084203-clean1-followup1` completed in foreground exec session `39737` after a local 900s wait. Log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618084203-clean1-followup1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618084203-clean1-followup1`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with unchanged reportable count `sink.live_nodes=44064847`, per-root `nfs-144=14688178`, `nfs-145=14688335`, `nfs-146=14688334`, delta `0` over `1106.66s`, rate `0/s`, no missing per-root or per-peer live-node data, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`. The count regression from the redeploy sample did not continue, but growth is still stalled.
- Persistent source backpressure stall is now the active owner: all three source concrete roots are unchanged after the read-only wait with `participation_state=joining`, `last_error=backpressure`, `last_audit_completed_at_us=null`, and `rescan_pending=false`. Event gaps remain large and stationary enough to block audit completion: `nfs-144 emitted=14789082 forwarded=14688180 gap=100902`, `nfs-145 emitted=14902691 forwarded=14688337 gap=214354`, `nfs-146 emitted=14818354 forwarded=14688336 gap=130018`. Root-fix Fustor source publication/backpressure handling before any further official deploy; do not use `/index/rescan`.
- Superseded Fustor local attempt: bounded single-event `Backpressure` fail-closed returned `CnxError::Backpressure` after `12` attempts and drove `mark_publication_output_closed` / `publish_pump_dead`; official `fullsrc-20260618102146` evidence proved this still leaves all three initial audit epochs stuck, so do not treat persistent route backpressure as terminal source-output closure.
- Current Fustor local fix: `fs-meta/app/src/workers/source_server.rs` keeps multi-event `Backpressure`/`Timeout` splitting, single-event `Timeout` fail-closed, and terminal transport-close fail-closed, but single-event route `Backpressure` now preserves the current event/window and retries under bounded per-send timeouts without returning success or closing source output. `fs-meta/app/src/workers/source.rs` mirrors the same app-local source pump rule. This is not an increased `/status` or deployment wait budget; it is publication-lane flow control, and downstream-accepted counters are still advanced only after the full batch is accepted.
- Local validation for the current Fustor fix passed: red test `cargo test -p fs-meta-runtime source_pump_does_not_close_source_output_on_route_backpressure -- --nocapture` failed before the fix because the pump exited under route backpressure, then passed after the fix. Focused green checks: `cargo test -p fs-meta-runtime source_pump_ -- --nocapture` (`7` passed), `cargo test -p fs-meta-runtime current_pub_stream_ -- --nocapture` (`5` passed), `cargo test -p fs-meta-runtime publish_pump -- --nocapture` (`4` passed), `cargo fmt -p fs-meta-runtime -- --check`, and `git diff --check -- fs-meta/app/src/workers/source_server.rs fs-meta/app/src/workers/source.rs TODO.md`. Next gate is fresh complete-source aibox; no official service change before that.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618114234` passed from the current Fustor/Capanix worktrees after the source publication flow-control fix. It ran in local foreground exec session `82022` with 300s empty `write_stdin` continuations; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618114234-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618114234`; sentinel `AIBOX_GATE_PASS fullsrc-20260618114234`. It passed Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry tests including `source_pump_` and `current_pub_stream_`, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618114234` artifact hashes: `capanixd=d91adeb012559477714d4475f9af8b047db9657dd4ec42241bd80d5fbf50401e`, `cnxctl=45481603205b419d083a3db8629aae31c21cc56fb98e4078e4b97a8e14973f8a`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=f48f31be89261195fd15486868ef0cc393ef9367cade391eb460d52866825d66`, `fsmeta=ba5b9f89c8b08d1adbc6f426301a7f6d4caa7ea04a79e1ae3932a66621f74793`.
- Aibox `fullsrc-20260618114234` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root/per-peer live-node fields, no local count regression versus previous local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `initial_audit_completed=true`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. All three source roots reached `participation_state=serving`, `last_error=null`, `last_audit_completed_at_us` set, and `forwarded_event_count=17`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; no official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618114234` passed from complete source snapshots after the aibox gate. It ran in local foreground exec session `34565` with 300s empty `write_stdin` continuations; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618114234.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618114234`; sentinel `COMPAT_BUILDER_PASS fullsrc-20260618114234`. It passed Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release builds, Fustor focused admission/source/status/manual-rescan/fail-closed/query/retry tests including `source_pump_` and `current_pub_stream_`, Fustor release builds, artifact SHA verification, `ldd`, and GLIBC verification.
- Compatible-builder `fullsrc-20260618114234` artifact hashes: `capanixd=d9597338fc6147614468c87c69a20ea2184b20ea9581e4cdde6a1fc7a87dd327`, `cnxctl=f06508371c458bcadec8d78eb81a20abc27febd4904dd29d186655cf2dbcdc15`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=eb72933b44816b0735019c0fdbdbe0d87fd3482aa348010270b7706df1847480`, `fsmeta=5fd226309c4389dd72b1f0c6d76f0a8ea623559a7cbb8e3cfc80c106792a14d8`; all five deployable ELF files require max `GLIBC_2.16`. No official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Official clean-log redeploy `fullsrc-20260618114234-official-2026061812-clean1` passed in foreground exec session `70160`. Log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618114234-official-2026061812-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618114234-official-2026061812-clean1-clean-redeploy`; post-wait sample `.../post-deploy-status`. It verified self-NFS mappings for all three nodes (`panda144 -> 10.0.82.144:/data/fustor-nfs`, `panda145 -> 10.0.82.145:/data/fustor-nfs`, `panda146 -> 10.0.82.146:/data/fustor-nfs`), staged and replaced all five compatible-builder artifacts, started each node through its node-owned script, restored resources/app/roots, waited locally `900s` while the client used only 300s empty `write_stdin` continuations, and collected one authenticated `/status` sample. No `/index/rescan` was called.
- Latest official count-bearing authenticated `/status` is now `fullsrc-20260618114234-official-2026061812-clean1/post-deploy-status`: HTTP 200, reportable index count `sink.live_nodes=83344388`, per-root `nfs-144=18922944`, `nfs-145=30788332`, `nfs-146=33633112`, delta `+39277493` versus previous official sample `official-readonly-status-fullsrc-20260618102146-clean1-followup1`, rate `9465.22/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, but still below `200000000` and below the `10000/s` growth floor. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618114234-official-2026061812-clean1-clean-redeploy/post-deploy-status/status-count-summary.json`.
- Saved post-deploy `/status` body reclassifies the remaining boundary away from persistent source backpressure: all three source concrete roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and have near-complete forwarded/emitted counts (`nfs-144` gap `1`, `nfs-145` gap `251`, `nfs-146` gap `0`). The remaining incomplete evidence is materialization/readiness: `last_audit_completed_at_us=null` on all source roots, while all sink groups remain `materialization_readiness=pending-materialization` and `initial_audit_completed=false`. This is still not an `/index/rescan` trigger.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup1`: HTTP 200, reportable index count `sink.live_nodes=199228660`, per-root `nfs-144=56115771`, `nfs-145=75146721`, `nfs-146=67966168`, delta `+115884272` versus `fullsrc-20260618114234-official-2026061812-clean1/post-deploy-status`, rate `95287.16/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and only anomaly `below_200000000`. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup1/status/status-count-summary.json`.
- Follow-up1 readiness evidence keeps the blocker open despite healthy growth: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and have forwarded all emitted events (`nfs-144`, `nfs-145`, `nfs-146` gaps all `0`), but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Continue patient read-only convergence; do not call `/index/rescan`.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup2`: HTTP 200, reportable index count `sink.live_nodes=271038214`, per-root `nfs-144=80314302`, `nfs-145=99225329`, `nfs-146=91498583`, delta `+71809554` versus follow-up1, rate `71774.11/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup2/status/status-count-summary.json`.
- Follow-up2 readiness evidence keeps the blocker open even though the count target is now satisfied: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and forwarded all emitted events at sample time, but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Because the count is still growing quickly and no topology/count anomaly remains, continue patient read-only convergence rather than calling `/index/rescan` or changing code.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup3`: HTTP 200, reportable index count `sink.live_nodes=341164354`, per-root `nfs-144=103608290`, `nfs-145=122342890`, `nfs-146=115213174`, delta `+70126140` versus follow-up2, rate `70640.95/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup3/status/status-count-summary.json`.
- Follow-up3 readiness evidence keeps the blocker open with the same shape: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and forwarded all emitted events at sample time, but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Because the count is still growing quickly and no topology/count anomaly remains, continue patient read-only convergence rather than calling `/index/rescan` or changing code.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup4`: HTTP 200, reportable index count `sink.live_nodes=409477368`, per-root `nfs-144=125209243`, `nfs-145=146241982`, `nfs-146=138026143`, delta `+68313014` versus follow-up3, rate `69353.40/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup4/status/status-count-summary.json`.
- Follow-up4 readiness evidence keeps the blocker open with the same shape: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and forwarded all emitted events at sample time, but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Because the count is still growing quickly and no topology/count anomaly remains, continue patient read-only convergence rather than calling `/index/rescan` or changing code.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup5`: HTTP 200, reportable index count `sink.live_nodes=479514533`, per-root `nfs-144=147782597`, `nfs-145=170171288`, `nfs-146=161560648`, delta `+70037165` versus follow-up4, rate `71330.28/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup5/status/status-count-summary.json`.
- Follow-up5 readiness evidence keeps the blocker open with the same shape: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and forwarded all emitted events at sample time, but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Because the count is still growing quickly and no topology/count anomaly remains, continue patient read-only convergence rather than calling `/index/rescan` or changing code.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup6`: HTTP 200, reportable index count `sink.live_nodes=549231372`, per-root `nfs-144=170272899`, `nfs-145=194091002`, `nfs-146=184867471`, delta `+69716839` versus follow-up5, rate `70787.12/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup6/status/status-count-summary.json`.
- Follow-up6 readiness evidence keeps the blocker open with the same shape: all three source roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and forwarded all emitted events at sample time, but `last_audit_completed_at_us=null` remains on every source root; all sink groups remain `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false`; top-level `trusted_observation_readiness=false`. Because the count is still growing quickly and no topology/count anomaly remains, continue patient read-only convergence rather than calling `/index/rescan` or changing code.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup7`: HTTP 200, reportable index count `sink.live_nodes=594718841`, per-root `nfs-144=192718283`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+45487469` versus follow-up6, rate `46344.99/s`, no missing per-root or per-`panda14x` live-node fields, no count regression, no zero-live roots, and no summary anomalies. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup7/status/status-count-summary.json`.
- Follow-up7 readiness evidence shows convergence is now partial-positive, not stalled: `nfs-145` and `nfs-146` sink groups reached `service_state=serving-trusted`, `materialization_readiness=ready`, and `initial_audit_completed=true`, both with `live_nodes=201000279`; only `nfs-144` remains `service_state=serving-degraded`, `materialization_readiness=pending-materialization`, and `initial_audit_completed=false` at `live_nodes=192718283`. Top-level `trusted_observation_readiness=false` remains expected until all three groups are ready. Continue patient read-only convergence; do not call `/index/rescan`.
- Latest official count-bearing authenticated `/status` is now `official-readonly-status-fullsrc-20260618114234-clean1-followup8`: HTTP 200 body, reportable index count `sink.live_nodes=603000837`, per-root `nfs-144=201000279`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+8281996` versus follow-up7, rate `8383.28/s`, no missing per-root live-node fields, no count regression, no zero-live roots, and summary anomaly `growth_below_10000_per_second`. The low final delta is reclassified as completion/stability evidence because all three roots reached the same final count and all sink groups are ready. Evidence path: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup8/status/status-count-summary.json`.
- Follow-up8 readiness evidence moves the first raw boundary from patient materialization wait to Fustor status/query readiness: all three sink groups are `service_state=serving-trusted`, `materialization_readiness=ready`, and `initial_audit_completed=true`; source concrete roots are `participation_state=serving`, `last_error=null`, `rescan_pending=false`, and have data publication/forwarding evidence, but `last_audit_completed_at_us=null` because the next source audit cycle is already in progress. Top-level `readiness_planes.trusted_observation_readiness=false` is therefore a false-negative readiness gate, not an `/index/rescan` trigger. No `/index/rescan` was called.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618100913` passed from the current Fustor/Capanix worktrees after the bounded single-event backpressure fail-closed fix. It ran in foreground exec session `42390` with 300s empty `write_stdin` continuations; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618100913-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618100913`; sentinel `AIBOX_GATE_PASS fullsrc-20260618100913`. It passed Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry tests including `source_pump_` and `current_pub_stream_`, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. Artifact hashes: `capanixd=77c819efbd1aabb184fa3e596ee23ebf74deaa32eab193c9e78cdb0f426d1e93`, `cnxctl=a58a5d7f76a03c0f394ed1b13e11b44e6fcd47b0faada7c3157f0d5be737e56e`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=1d2dc875d2ec2b16d42bc02a9a9ca976055a444389d9b25c4be492bf1b9ad82a`, `fsmeta=84b6affddeb98ba81c6e0e9ccd7e8205397eb24593118ec8afce741cea7531d3`.
- Aibox `fullsrc-20260618100913` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root or per-peer live-node fields, no local count regression versus previous local replay (`42 -> 42`), no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `initial_audit_completed=true`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; the official `below_200000000` and `<10000/s` anomaly gates remain required for official high-cardinality `/status` samples. No official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618102146` passed from complete source snapshots after the aibox gate. It ran in foreground exec session `77026` with 300s empty `write_stdin` continuations; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618102146.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618102146`; sentinel `COMPAT_BUILDER_PASS fullsrc-20260618102146`. It passed Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release builds, Fustor focused admission/source/status/manual-rescan/fail-closed/query/retry tests including `source_pump_` and `current_pub_stream_`, Fustor release builds, and artifact verification. Compatible-builder artifact hashes: `capanixd=94200f920713c1c29e1ec7685175ba3428106d4329df990197a650067e0af9d4`, `cnxctl=0895bea290c91538e8a7b22912a703fae2f2abbc8a9cbaf2f778586754fb07b2`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=c05b6c076e0286c51e0eb03102f18efaab6bcec84508a85a3c9ad6f182ec0bfa`, `fsmeta=81351363cbaf75d8db71c81d0851f747039af3ed40b8ae998ea60fd707990431`; all five deployable ELF files require max `GLIBC_2.16`. No official authenticated `/status` sample was collected and no `/index/rescan` was called.
- Long-wait skill correction after operator error: fs-meta long waits now require `yield_time_ms=300000` (300s) on empty `write_stdin` continuations, falling back only if the tool rejects 300000. This is recorded in `.codex/skills/fs-meta-cross-repo-gate/SKILL.md`, `.codex/skills/fs-meta-cross-repo-gate/references/binary-deploy.md`, `.codex/skills/official-fsmeta-stable-deploy/SKILL.md`, `.codex/skills/official-fsmeta-stable-deploy/references/deploy-restore-verify.md`, `.codex/skills/official-pending-force-find/SKILL.md`, and `.codex/skills/official-pending-force-find/references/evidence-contract.md`.
- Official reachability evidence for that same sample: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-status-anomaly-sample-20260618034954/cluster-status-summary.json` shows from `panda145` that `panda144` and `panda146` are `reachable=false`, while `panda145` has `peer_unreachable_buffered=42`, `backpressure_events=7141`, and `gossip_converged=false`; local cluster-scope `cnxctl cluster status` from `panda144` and `panda146` each failed with `kernel control request timed out after 60s`. Therefore the remote zero live-node roots are degraded/untrusted fan-in evidence, not a healthy final materialization result and not a rescan trigger.
- Bounded official daemon logs localize the current first raw boundary to Capanix peer transport/gossip under `panda145` load: `/tmp/fsmeta-node-logs-current-status-anomaly-status-fanin-20260618034954` and `/tmp/fsmeta-node-logs-current-status-anomaly-endpoints-20260618034954` show `panda145` repeatedly logging `gossip heartbeat send failed ... single-attempt liveness connect ... timed out after ~2s` to both `panda144` and `panda146` (`259/260` bounded matched lines), while `panda144`/`panda146` have local fs-meta endpoints and root tasks ready. This is a sustained condition, not an occasional `panda146` response timeout.
- Capanix local root fix for this boundary: retain the existing bounded `send_to_once_liveness()` behavior, and add real heartbeat ACK recovery. `/root/repo/capanix/crates/kernel/src/boundary/dispatcher_ops.rs` now emits a bounded `250ms` ACK on the same authorized inbound heartbeat connection; `/root/repo/capanix/crates/kernel/src/gossip/transport.rs` extends `HeartbeatAck` with `addr` and records ACKs as heartbeat reachability evidence; `/root/repo/capanix/crates/kernel/src/gossip/state.rs` exposes a local address hint for ACK payloads. Unauthorized peers and ACK frames do not receive ACKs, so there is no ACK loop or membership bypass. Validation passed: `cargo test -p capanix-kernel handle_heartbeat_ack_updates_reachability -- --nocapture`, `cargo test -p capanix-kernel authorized_gossip_heartbeat_builds_bounded_ack_payload -- --nocapture`, `cargo test -p capanix-kernel gossip::transport -- --nocapture` (`9` passed), full `cargo test -p capanix-kernel -- --nocapture` (`274` unit tests and `151` specs), `cargo fmt -p capanix-kernel -- --check`, and `git -C /root/repo/capanix diff --check -- crates/kernel/src/boundary/mod.rs crates/kernel/src/boundary/dispatcher_ops.rs crates/kernel/src/boundary/tests.rs crates/kernel/src/gossip/state.rs crates/kernel/src/gossip/transport.rs crates/kernel/src/link/manager.rs`.
- Complete-source aibox predeploy gate `fullsrc-20260618040322` passed from the current Fustor/Capanix worktrees after the heartbeat ACK recovery fix. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618040322-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618040322`, sentinel `AIBOX_GATE_PASS`. It passed Capanix focused heartbeat ACK/gossip/link/runtime tests, full `capanix-kernel` (`274` unit tests and `151` specs), full `capanix-runtime` (`286` unit tests, `4` route-relation tests, `121` specs, `9` config tests), Capanix release build, Fustor release build, focused Fustor source/status/live-node/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618040322` artifact hashes: `capanixd=bb614d63ae7084b301b778f95c08d1dd1c5b02c270729678c7ebd219c493fa58`, `cnxctl=9478852905b592c0c6f1b77be3f5b2fcae19fae7c6eafafcc8c83e927496bb96`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=47d2059e25d0ce28e084a985a951dec83abdba402b5b6717480c33e7fe0057ba`, `fsmeta=40b0c252d8a2771e2b420fda7fe2be8c162d89d648f1f75cadf765fa0990e4d0`.
- Aibox `fullsrc-20260618040322` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node data, no local count regression versus the prior local replay (`42 -> 42`), all three groups `serving-trusted`, `materialization_readiness=ready`, `trusted_observation_readiness=true`, and one-to-one source/scan/sink schedules. The aggregate is intentionally below `200000000` because this is the tiny local replay seed, so it is a local gate only.
- `panda145` compatible-builder gate `fullsrc-20260618040322` failed closed before artifact verification/deploy/status sampling. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618040322.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618040322`, sentinel `COMPAT_BUILDER_FAIL`. First raw boundary is Capanix runtime startup/deferred activation timeout test robustness on the CentOS 7 builder: `issue_activations_with_timeout_does_not_leave_proc_ctl_running_after_bounded_return` observed `Ok(())` after the bounded-return assertion, and `deferred_retire_deactivate_delivery_has_total_budget_across_many_routes` / `restart_commit_bounds_deferred_predecessor_deactivate_latency` exceeded a brittle `<100ms` wall-clock assertion by about 1-2ms. No fustor build, artifact verification, official deploy, official `/status` count sample, or `/index/rescan` happened in this gate.
- Capanix local closure for that runtime boundary: `/root/repo/capanix/crates/runtime/src/engine/activation.rs` no longer reports a blocked fresh activation `proc_ctl` timeout as delivered/activated; retryable timeout continuity is preserved only from prior active route history. `/root/repo/capanix/crates/runtime/src/engine/startup/tests.rs` and `deferred_activation_commit_config.rs` make the cancellation test deterministic by waiting until `proc_ctl` has actually entered the blocked callback, then asserting bounded blocked-route evidence and no late completion after release. `/root/repo/capanix/crates/runtime/src/engine/startup/commit.rs` replaces brittle `<100ms` wall-clock assertions with blocked-callback totals that still prove the delivery budget is not multiplied across routes. Validation passed: focused `issue_activations_with_timeout_does_not_leave_proc_ctl_running_after_bounded_return`, both deferred-retire budget tests, `issue_activations` group (`29` passed), activation route timeout fail-closed tests, full `cargo test -p capanix-runtime -- --nocapture` (`286` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check` for the touched runtime files.
- Complete-source aibox predeploy gate `fullsrc-20260618043049` failed in active-three real-NFS replay before any official deploy or official `/status` sample. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618043049-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618043049`, sentinel `AIBOX_GATE_FAIL`. First raw boundary: Capanix activation dispatch sent 37 route activations for `pid 1` as one proc_ctl batch; the per-batch 6s timeout expired at `apply-release` (`elapsed_ms=6407`) and failed all routes closed. Node evidence still showed the fs-meta app had processed facade/sink/source activation frames and spawned status endpoints, so this is a Capanix dispatch-result granularity boundary, not NFS root mismatch, pending-materialization, `/status`, or `/index/rescan` evidence.
- Capanix local closure for `fullsrc-20260618043049`: activation dispatch is now serial per pid but result-scoped per route/app-plan, so same-pid cold-start routes are not combined into one oversized proc_ctl batch; if a route dispatch times out, later same-pid routes fail closed with an explicit prior-pid-failure reason instead of missing-result ambiguity, preserving prior-holder retryable continuity where it already exists. Validation passed: `cargo test -p capanix-runtime issue_activations_route_scoped_dispatch_avoids_same_pid_cold_start_batch_timeout -- --nocapture`, `dispatch_route_plan_batches_owned_cancels_underlying_send_batch_after_timeout`, full `issue_activations` group (`30` passed), activation route timeout fail-closed tests, predecessor-before-activation fail-closed test, full `cargo test -p capanix-runtime -- --nocapture` (`287` unit tests, `4` route-relation tests, `121` specs, `9` config tests), `cargo fmt -p capanix-runtime -- --check`, and `git diff --check` for touched runtime activation files.
- Complete-source aibox predeploy gate `fullsrc-20260618044930` passed from the current Fustor/Capanix worktrees after the route-scoped activation dispatch fix. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618044930-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618044930`, sentinel `AIBOX_GATE_PASS`. It passed Capanix focused heartbeat ACK/gossip/link/runtime tests, full `capanix-kernel` (`274` unit tests and `151` specs), full `capanix-runtime` (`287` unit tests, `4` route-relation tests, `121` specs, `9` config tests), Capanix release build, Fustor release build, focused Fustor source/status/live-node/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. The previous `apply-release` boundary is closed locally: this replay's `apply-release` succeeded in `34096ms` with no node evidence counts for `operation timed out`, `route activation backpressure`, `statecell_read failed`, or worker sidecar bind failures.
- Aibox `fullsrc-20260618044930` artifact hashes: `capanixd=636b7ecc5eebd51e2c5d41f35a1b6e351a5d2b2dd0527ff1aca711e1fb61b0cb`, `cnxctl=2176c14be6ed4179dacd8f282e790754665160a79783d3fbb61bd166825e49cb`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=151fe92f6431626ab4058f0206e28283d2d44868726a04137df40cdd75561be0`, `fsmeta=f7c0fd23a1b6237e63ea54dd822b7ccc8128ed1ee74629fa7aca42994c34d386`.
- Aibox `fullsrc-20260618044930` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node data, no local count regression versus the prior local replay (`42 -> 42`), all three groups `serving-trusted`, `materialization_readiness=ready`, `initial_audit_completed=true`, `trusted_observation_readiness=true`, and one-to-one source/scan/sink schedules. The aggregate is intentionally below `200000000` because this is the tiny local replay seed, so it is a local gate only.
- `panda145` compatible-builder gate `fullsrc-20260618044930` ran from complete source snapshots and failed at the wrapper level after tests/builds, not as product evidence. Foreground exec session `3129`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618044930.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618044930`; sentinel `COMPAT_BUILDER_FAIL`. It passed focused Capanix heartbeat/gossip/link/runtime validations, full `capanix-kernel`, full `capanix-runtime`, Capanix release build, Fustor release build, focused fs-meta source/status/live-node/manual-rescan fail-closed suites, and then failed only because the artifact verification wrapper used Python `subprocess.check_output(..., text=True)`, which is unsupported by panda145's Python 3.6. No official deploy, no official `/status` count sample, and no `/index/rescan` happened.
- Corrected artifact verification retry for `fullsrc-20260618044930` passed against the same fresh remote root. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618044930-artifact-verify-retry.log`, sentinel `COMPAT_BUILDER_ARTIFACT_VERIFY_PASS`; all five deployable ELF files require max `GLIBC_2.16`.
- Compatible-builder `fullsrc-20260618044930` is closed by the complete-source tests/builds from session `3129` plus the corrected artifact verification retry. Deployable hashes: `capanixd=05082827e2ed272be73aaebffc8dbe6083771a37b8daec5c08bd94f4467dbe3e`, `cnxctl=e1ae89bc642a75a0bd4a8307fc1f5c3c47981065241555f6d8456670254a3cfc`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=37d48acdcda9087db5b315dd3ec7181218ac3f6e4d623d22ae7ccdb86428d8bd`, `fsmeta=2bb7968a8c4895c0b84d68b09212ebfa72eb2564d377008d334541100372e595`.
- Official clean-log redeploy `fullsrc-20260618044930-official-2026061805-clean1` completed from compatible-builder artifacts. Log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618044930-official-2026061805-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618044930-official-2026061805-clean1-clean-redeploy`, sentinel `OFFICIAL_REDEPLOY_PASS`. It staged and installed all five hashes on `panda144`/`panda145`/`panda146`, verified each node runs its own `nodes/panda14x/start.sh`, verified self-NFS mappings (`panda144` -> `10.0.82.144:/data/fustor-nfs`, `panda145` -> `10.0.82.145:/data/fustor-nfs`, `panda146` -> `10.0.82.146:/data/fustor-nfs`), restored resources/app/roots, waited locally `900s`, and collected one authenticated `/status` sample. No `/index/rescan` happened.
- Latest official count-bearing `/status` after that redeploy is `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618044930-official-2026061805-clean1-clean-redeploy/post-deploy-status/status-count-summary.json`: HTTP 200, `sink.live_nodes=0`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, delta `-14688792` versus the prior official count-bearing sample, rate `-16320.88/s`, no missing per-root live-node fields, count regression, growth below `10000/s`, zero-live roots `nfs-144`/`nfs-145`/`nfs-146`, and aggregate below `200000000`. This keeps the blocker open and requires clean-log root-cause analysis before any further deploy claim.
- Next allowed action: collect bounded official daemon-log evidence from the current clean-log window, focusing status fan-in, source/scan audit start, sink/source endpoint readiness, transport/gossip reachability, and worker/runtime errors. Every future official status sample must record aggregate/per-root live-node counts, delta/rate, missing peer fields, regression, growth below `10000/s`, zero-live roots, and below-`200000000` tags. Do not call `/index/rescan`.
- Complete-source aibox predeploy gate `fullsrc-20260618013122` failed at the wrapper level, not as product evidence: log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618013122-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618013122`, sentinel `AIBOX_GATE_FAIL`. It passed Capanix focused/full tests, release builds, fustor release/focused source/status/manual-rescan suites, then failed before active-three real-NFS execution because the wrapper invoked nonexistent Cargo package `fs-meta-tests`; the correct workspace package is `fustor-specs-root`.
- Corrected complete-source aibox predeploy gate `fullsrc-20260618014103` passed. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618014103-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618014103`, sentinel `AIBOX_GATE_PASS`. It passed Capanix focused liveness tests, full `capanix-kernel` (`272` unit tests and `151` specs), release builds, fustor release build, focused source/status/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), formatting, and corrected `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs`.
- Aibox `fullsrc-20260618014103` artifact hashes: `capanixd=61248d4b502c1a9a9459d1027bf28aa77d108f6841841aa1b7fc2de706ef970b`, `cnxctl=a32e0b31e05758c5e81030dfc8bd2631c415d091d74fd52b8cb4d12d00433b89`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=771a450b87accc8a0edae4c98e99f5f11f26984ab725b653ab3aa24c61e45cca`, `fsmeta=3242c136bef3fe24356b58b016a782492bc0ef6b8fb427a59251f182bd6c99a8`.
- Aibox `fullsrc-20260618014103` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, all three groups `serving-trusted`, materialization readiness `ready`, source audits started/completed for all roots, `rescan_pending=false`, and one-to-one source/scan/sink schedules. This is local validation only and not official final-acceptance count evidence.
- `panda145` compatible-builder gate `fullsrc-20260618014103` failed closed before artifact verification. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618014103.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618014103`, sentinel `COMPAT_BUILDER_FAIL`. First raw boundary: `link::manager::tests::concurrent_send_to_same_peer_reuses_single_connect_attempt` hung after its 60s libtest warning and was terminated with SIGTERM; no fustor build, official deploy, or official `/status` count sample happened.
- Capanix local closure for that builder boundary: `/root/repo/capanix/crates/kernel/src/link/manager.rs` now uses a non-lossy `watch` counter for the test's `connect_entered` gate and 5s bounded joins after releasing the connect waiter, so a scheduler race fails fast instead of hanging the builder. Validation passed: `cargo test -p capanix-kernel link::manager::tests::concurrent_send_to_same_peer_reuses_single_connect_attempt -- --nocapture`, `cargo test -p capanix-kernel link::manager -- --nocapture`, full `cargo test -p capanix-kernel -- --nocapture` (`272` unit tests and `151` specs), `cargo fmt -p capanix-kernel -- --check`, and `git diff --check -- crates/kernel/src/link/manager.rs`.
- Complete-source aibox predeploy gate `fullsrc-20260618015916` failed at the wrapper/env level, not as product evidence. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618015916-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618015916`, sentinel `AIBOX_GATE_FAIL`. It passed Capanix focused/full kernel tests, release builds, fustor release build, and focused source/status/manual-rescan suites, then real-NFS replay failed before cluster startup because `CAPANIXD_BIN` was not set and the test could not find `capanixd`; no official deploy or official `/status` sample happened.
- Complete-source aibox predeploy gate `fullsrc-20260618020814` failed before validation at snapshot time because `/tmp` was full (`/tmp/aibox-fsmeta-build` had accumulated old fullsrc targets). This is runner hygiene, not product evidence. The old aibox fullsrc directories were removed while preserving `*-gate.log` evidence, and `/tmp` recovered to about `49G` available.
- Complete-source aibox predeploy gate `fullsrc-20260618020948` passed. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618020948-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618020948`, sentinel `AIBOX_GATE_PASS`. It passed Capanix focused liveness tests, full `capanix-kernel` (`272` unit tests and `151` specs), Capanix release build, fustor release build, focused source/status/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), and corrected `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay with explicit `CAPANIXD_BIN`, `CNXCTL_BIN`, and `CAPANIX_WORKER_HOST_BINARY`.
- Aibox `fullsrc-20260618020948` artifact hashes: `capanixd=b28ce329f952a41f0a516760dc9c107338fc9bf320659d198f21acc8cf53c129`, `cnxctl=70e48aabdff9b07ff20f5f8cfbeef763ab522cc544942e0822873d67ddbefd53`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=57b23e548a9bd9c25955ea9bf904afbd5f4a6e2f3b878fbd2fed5eb66a290a90`, `fsmeta=4e0b44c565d0137532d2b469ba7c72b983656140f97c67c686dbd9ba3760f80b`.
- Aibox `fullsrc-20260618020948` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node data, no local count regression, all three groups `serving-trusted`, materialization readiness `ready`, `rescan_pending=false`, and one-to-one source/scan/sink schedules. This is local validation only and not official final-acceptance count evidence.
- `panda145` compatible-builder gate `fullsrc-20260618020948` failed closed in Capanix runtime tests before artifact build. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618020948.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618020948`, sentinel `COMPAT_BUILDER_FAIL`. First raw boundary: `engine::activation::tests::renew_due::renew_due_activations_does_not_shrink_current_root_scope_from_stale_bootstrap_manifest` failed in `cargo test -p capanix-runtime -- --nocapture`; expected zero stale bootstrap reissues, actual one control send. No fustor build, official deploy, or official `/status` count sample happened.
- Capanix local closure for that runtime boundary: `/root/repo/capanix/crates/runtime/src/engine/activation.rs` now separates `due_renew_route_keys()` from `pending_reissue_route_keys()` in `renew_due_activations_internal`; non-active/pending routes still reissue, while active due routes extend retained lease bookkeeping without using a stale manifest to shrink accepted current-root scopes. `/root/repo/capanix/crates/runtime/src/engine/unit_tick.rs` also removed a domain-specific test string that violated the runtime domain-neutral specs. Validation passed: focused failing renew-due test, full renew-due group, `cargo test -p capanix-runtime issue_activations_ -- --nocapture`, full `cargo test -p capanix-runtime -- --nocapture` (`286` unit tests, `4` route-relation tests, `121` specs, `9` config tests), full `cargo test -p capanix-kernel -- --nocapture` (`272` unit tests and `151` specs), `cargo fmt -p capanix-runtime -- --check`, `cargo fmt -p capanix-kernel -- --check`, and `git diff --check` for touched Capanix files.
- Complete-source aibox predeploy gate `fullsrc-20260618022729` failed at the wrapper/env level, not as product evidence. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618022729-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618022729`, sentinel `AIBOX_GATE_FAIL`. It passed Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, formatting, and Capanix/Fustor release builds, then Fustor focused test `runtime_app::tests::worker_source_status_release_opens_scan_audit_admission_while_source_state_pending` failed before app start because the wrapper did not pass `CAPANIX_WORKER_HOST_BINARY` and the test fallback looked for a nonexistent debug worker-host under the fustor snapshot. No real-NFS replay, official deploy, or official `/status` count sample happened.
- Corrected complete-source aibox predeploy gate `fullsrc-20260618023536` passed. Log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618023536-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618023536`, sentinel `AIBOX_GATE_PASS`. It passed Capanix focused liveness tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release build, Fustor release build, Fustor focused source/status/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay with explicit `CAPANIXD_BIN`, `CNXCTL_BIN`, and `CAPANIX_WORKER_HOST_BINARY`.
- Aibox `fullsrc-20260618023536` artifact hashes: `capanixd=76402dd057438be4c67cb29822b97ba3a002b8b965440abe19a8112cb8c2923a`, `cnxctl=ef239800efc7b8db3b8200aaf372b4f3b07e9722b2660f51b08996d1f383a4ce`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=2dac4c79327c76c7ba72e94aa499b6cb32b3312d01f22f36ecf53b2b9240b991`, `fsmeta=b20c2f833eca1859957c924542cad65234f75e354efe230ae4f8d22f4ea83b41`.
- Aibox `fullsrc-20260618023536` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no missing per-root live-node data, no local count regression versus the prior local replay (`42 -> 42`), all three groups `serving-trusted`, materialization readiness `ready`, `rescan_pending=false`, and one-to-one source/scan/sink schedules. The aggregate remains below `200000000` because this is the intentionally tiny local replay seed, so it is local gate evidence only and not official final-acceptance count evidence.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618023536` passed from complete source snapshots. Log `/tmp/fsmeta-compatible-builder-fullsrc-20260618023536.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618023536`, sentinel `COMPAT_BUILDER_PASS`. It passed Capanix focused liveness/runtime tests, full `capanix-kernel`, full `capanix-runtime`, release builds, Fustor release build, Fustor focused source/status/manual-rescan suites including serial `manual_rescan_` (`217` passed, `1` ignored), and artifact verification.
- Compatible-builder hashes for `fullsrc-20260618023536`: `capanixd=663bdb1100e388e7700e8a8f8a80f115b160e43d42cdb432996d2aaf362682b5`, `cnxctl=858d88c9786dac419cd3644e220e60ba3d515b5d9ed7a4213d9b5fd58ef4ecea`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=b3272b6c28697b70c99798dd0cd8525f655e119e6da04cdd278db4f18747daa1`, `fsmeta=42870afa57e402fd764cb012dab417773816cf8d20c753b336456abccb356c6c`; all deployable ELF max GLIBC values are `GLIBC_2.16`.
- Official clean-log redeploy attempt `fullsrc-20260618023536-official-2026061804-clean1` failed before restore/status sampling, so it collected no new official `/status` count. The binaries were already replaced with the `fullsrc-20260618023536` compatible-builder hashes on all three nodes, each node started through its own `nodes/panda14x/start.sh`, and self-NFS mapping was verified. First raw boundary is an operator restore-env assumption: the effective `/home/wanghuajin/fsmeta-stable/run/admin.env` has the management signing key but does not define `CAPANIX_SOCKET` or `FSMETA_CONFIG`; previous successful restore context used socket `/home/wanghuajin/fsmeta-stable/run/nodes/panda145/home/core.sock` and config `/home/wanghuajin/fsmeta-stable/run/fs-meta-embedded.yaml`. This is not product `/status` evidence and does not authorize `/index/rescan`.
- Official clean-log restart/restore attempt `fullsrc-20260618023536-official-2026061804-clean2` completed with `OFFICIAL_RESTART_RESTORE_PASS`; log `/tmp/fsmeta-official-clean-restart-restore-fullsrc-20260618023536-official-2026061804-clean2.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618023536-official-2026061804-clean2-clean-restart-restore`. It used the explicit stable socket/config, restored resources/app/roots, waited locally `900` seconds, then sampled authenticated `/status` once. Corrected count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618023536-official-2026061804-clean2-clean-restart-restore/post-deploy-status/status-count-summary-corrected.json`: HTTP 200, `sink.live_nodes=0`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, delta `0`, rate `0/s`, no count regression, no missing per-root live-node data, growth below `10000/s`, aggregate below `200000000`. The first clean2 summary artifact falsely marked per-root data missing because the operator parser did not recognize `sink.groups[].group_id`; the status body did expose all three per-root counts.
- Clean2 source/sink evidence: `/status` had one-to-one `primary_host_ref_by_group` and `scheduled_groups_by_node`, but all source concrete roots still had `rescan_pending=true`, `last_audit_started_at_us=null`, `last_audit_completed_at_us=null`, `emitted_event_count=0`, and `forwarded_event_count=0`. The same status response placed `source-scan-audit-admission-release` into `inflight`; bounded clean2 node logs `/tmp/fsmeta-node-logs-clean2-zero-source-scan-20260618023536` show `release_source_scan_audit_after_sink_scope_ready_if_needed ... ok` on each `panda14x`. Therefore the clean2 900s window again occurred before the status-triggered admission release became observable; it is not yet proof that post-release scanning stayed stalled for 900s. Do not call `/index/rescan`.
- Next allowed action: one read-only post-release convergence wait after clean2, then one authenticated `/status` sample that records aggregate/per-root counts, delta/rate, missing peer live-node data, regression, slow-growth, and below-`200000000` tags. Do not call `/index/rescan`.
- The retry2 raw cause was source audit admission releasing without waking the worker-side scan after the reply. Local fustor fix in `fs-meta/app/src/workers/source_server.rs`: `OpenScanAuditAdmissionAndTriggerRescanWhenReadyEpoch` now records the epoch with `begin_rescan_request_epoch()` and returns the deferred rescan signal instead of submitting directly before the worker reply. Focused source/status/manual-rescan validation passed locally.
- The previous deploy-gate aibox `fullsrc-20260617232753` is superseded because Capanix source changed afterward. The Capanix compatible-builder hang was a test-harness race in `crates/kernel/src/link/manager.rs`; the release gate now uses `watch` state instead of lossy `Notify::notify_waiters()`. Local Capanix validation passed, including full `capanix-kernel`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618000204` passed after the Capanix link-manager fix. Log: `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618000204-gate.log`; root: `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618000204`; sentinel: `AIBOX_GATE_PASS`. It passed Capanix focused/full tests, release builds, fustor focused suites, serial `manual_rescan_`, formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618000204` artifact hashes: `capanixd=fd0b9bd5bfaa8c624fb501b43187434e393d13f0b73fe607f6f6f99caebcce41`, `cnxctl=1e146b78e3139901cb595c94515c1a95ee28529bf30ca273efa0e7011d88c795`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=fb34ebe035845353a9e565c169d872a6db04233f0e05cb03b4a517585abbfdd4`, `fsmeta=8b6d4c7dfeecce82aa9d1d8bc92c6bc1fbed33c8210beee23f11fc16dc239219`.
- Aibox `fullsrc-20260618000204` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, all three groups `serving-trusted`, source audits started/completed for all roots, `rescan_pending=false`, and one-to-one source/scan/sink schedules. This is local validation only and not official final-acceptance count evidence.
- `panda145` compatible-builder gate `fullsrc-20260618001339` failed closed before artifact verification. Session `23446`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618001339.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618001339`. Capanix focused/full tests and release build passed, then fustor failed in `workers::source_server::tests::bootstrap_start_rebinds_publish_pump_to_latest_boundary_when_previous_pump_is_alive`: replacement boundary saw two control-only origin batches and the test asserted before data events arrived.
- Local fustor closure for that builder boundary: the failing test now waits for both origin evidence and `sent_data_events_for_route(&event_route) > 0` before asserting replacement-boundary data publication. This is inside `#[cfg(test)] mod tests` and does not change release runtime behavior. Validation passed: `cargo test -p fs-meta-runtime workers::source_server::tests::bootstrap_start_rebinds_publish_pump_to_latest_boundary_when_previous_pump_is_alive -- --nocapture`, `cargo test -p fs-meta-runtime workers::source_server::tests -- --nocapture`, and `git diff --check -- fs-meta/app/src/workers/source_server.rs TODO.md`.
- `panda145` compatible-builder gate `fullsrc-20260618002246` failed closed in Capanix full `capanix-kernel` before artifact build. Session `32993`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618002246.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618002246`. First raw failure: `boundary::dispatcher_ops::tests::tx_execute_proc_spawn_blocking_registered_start_does_not_hang_result` timed out under its outer `120ms` wrapper while testing a `BlockingStartApp` that sleeps `200ms`; the production helper already uses a test `APP_START_TIMEOUT=100ms` plus `10ms` cancel grace and a separate app-start thread, so the outer wrapper was too close to the internal deadline for loaded full-suite scheduling.
- Local Capanix closure for that builder boundary: `/root/repo/capanix/crates/kernel/src/boundary/dispatcher_ops.rs` now uses a `350ms` outer timeout for the blocking registered-start test, matching the adjacent pre-commit blocking-start test, while preserving the semantic assertion that elapsed time is `<180ms` and therefore below the blocking app's `200ms` sleep. Validation passed: `cargo test -p capanix-kernel boundary::dispatcher_ops::tests::tx_execute_proc_spawn_blocking_registered_start_does_not_hang_result -- --nocapture`, `cargo test -p capanix-kernel boundary::dispatcher_ops -- --nocapture`, full `cargo test -p capanix-kernel -- --nocapture` (`271` unit tests and `151` specs), `cargo fmt -p capanix-kernel -- --check`, and `git diff --check -- crates/kernel/src/boundary/dispatcher_ops.rs`.
- Fresh `panda145` compatible-builder gate `fullsrc-20260618002757` passed from complete source snapshots. Log: `/tmp/fsmeta-compatible-builder-fullsrc-20260618002757.log`; remote root: `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618002757`; sentinel: `COMPAT_BUILDER_PASS`. It passed Capanix focused tests, full `capanix-kernel`, release builds, fustor focused source/status/manual-rescan suites, release builds, and artifact verification.
- Compatible-builder hashes for `fullsrc-20260618002757`: `capanixd=6eaf497fcb2d5ddcba8aab394bc5b654e0c809b775853400c211c18a3996cedc`, `cnxctl=e3bad61f532180d85dd5c3eb3a07fd948d4ed41c06b9ae6986045b687d0a6910`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=5c77d3e751e875496a179fe253bf7fda94e0840a92b3f5d2d4096d0588b19105`, `fsmeta=1c0c7170905ab0c4c2dd4dd0e9854befbcc2562a471b45c540a12b4087e7b096`; all deployable ELF max GLIBC values are `GLIBC_2.16`.
- Next allowed action: official clean-log redeploy from `fullsrc-20260618002757` compatible-builder artifacts, then a local long wait followed by one authenticated `/status` sample. The sample must record aggregate `sink.live_nodes`, per-root live nodes, comparable delta/rate, whether any active `panda14x` peer lacks live-node data, whether any count regressed, whether growth is below `10000/s`, and whether aggregate remains below `200000000`.
- Planned official redeploy wait contract: attempt `fullsrc-20260618002757-official-2026061800-clean1` will write log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618002757-official-2026061800-clean1.log`, validation state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618002757-official-2026061800-clean1-clean-redeploy`, expected terminal sentinel `OFFICIAL_REDEPLOY_PASS` or `OFFICIAL_REDEPLOY_FAIL`, command shape is one foreground `exec_command` that stages compatible-builder artifacts, stops all three official node-owned daemons, clears logs, deletes/replaces live binaries, starts each node with its own `nodes/panda14x/start.sh`, restores resources/app/roots, sleeps locally for `900` seconds, and collects one authenticated `/status` sample through `panda145`. Continue any yielded session only with empty `write_stdin`.
- Official clean-log redeploy `fullsrc-20260618002757-official-2026061800-clean1` completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618002757-official-2026061800-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618002757-official-2026061800-clean1-clean-redeploy`. It staged/replaced all five artifacts on `panda144`/`panda145`/`panda146`, verified each node starts through its own node-owned script, and re-verified self-NFS: `10.0.82.144:/data/fustor-nfs -> /mnt/fustor-peers/nfs144`, `10.0.82.145:/data/fustor-nfs -> /mnt/fustor-peers/nfs145`, `10.0.82.146:/data/fustor-nfs -> /mnt/fustor-peers/nfs146`.
- Post-wait authenticated `/status` for `fullsrc-20260618002757-official-2026061800-clean1` returned HTTP 200. Reportable index count is still `sink.live_nodes=0`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, delta `0`, rate `0/s`; no active `panda14x` peer lacks live-node data, but growth is below `10000/s` and aggregate remains below `200000000`. No `/index/rescan` was called.
- Clean-log status/log diagnosis: cluster status had `peer_count=3`, all `panda144`/`panda145`/`panda146` reachable and `cluster_ready`; `/status` had one-to-one sink/source/scan maps and all three per-root live-node fields. Source concrete roots remained `rescan_pending=true`, `last_audit_started_at_us=null`, `last_audit_completed_at_us=null`, `emitted_event_count=0`, `forwarded_event_count=0`. The `/status` response itself scheduled `source-scan-audit-admission-release` and left that repair lane `inflight`; bounded clean-log excerpts under `/tmp/fsmeta-node-logs-zero-after-clean1-*` and `/tmp/fsmeta-node-logs-worker-open-audit-clean1-*` show source/sink roots and routes ready plus `release_source_scan_audit_after_sink_scope_ready_if_needed source-status ok` on each node, but no `manual completed` or published data events before that status response. Therefore the first 900s deploy wait happened before the first status-triggered admission release; it is not proof of 900s post-release scanning failure.
- Next allowed action: one read-only post-release convergence wait, then one authenticated `/status` sample that records aggregate/per-root counts and anomaly tags. Do not call `/index/rescan`.
- Planned read-only post-release wait contract: attempt `official-post-release-readonly-clean1-2026061801` will write log `/tmp/fsmeta-official-post-release-readonly-clean1-2026061801.log`, validation state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-post-release-readonly-clean1-2026061801`, expected terminal sentinel `OFFICIAL_POST_RELEASE_STATUS_PASS` or `OFFICIAL_POST_RELEASE_STATUS_FAIL`, command shape is one foreground `exec_command` that sleeps locally for `900` seconds and then collects one authenticated `/status` sample through `panda145`; continue any yielded session only with empty `write_stdin`.
- Read-only post-release wait `official-post-release-readonly-clean1-2026061801` completed with `OFFICIAL_POST_RELEASE_STATUS_PASS`; log `/tmp/fsmeta-official-post-release-readonly-clean1-2026061801.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-post-release-readonly-clean1-2026061801`. Authenticated `/status` returned HTTP 503, so no reportable `sink.live_nodes` or per-root live-node count was exposed in that response. Summary: `missing_sink_status`, `missing_aggregate_sink_live_nodes`, `status_http_not_200`; no `/index/rescan` was called.
- Next allowed action: read the 503 body and collect bounded `status-fanin` official node logs from the clean-log window to classify the first raw post-release boundary.
- `/index/rescan` remains disallowed for pending materialization, zero count, missing explicit global alias, or impatience. It is allowed only on explicit user authorization or fresh first-boundary evidence proving stale/incorrect accepted-scope/source/scan repair state with this TODO naming rescan as the next repair.
- Every future official authenticated `/status` sample must record aggregate `sink.live_nodes`, per-root live nodes, comparable delta/rate, whether any active `panda14x` peer lacks live-node data, whether any count regressed, whether growth is below `10000/s`, and whether aggregate remains below `200000000`.

- Latest official clean-log redeploy attempt `fullsrc-20260617221254-official-20260617223721-retry2` completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617221254-official-20260617223721-retry2.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617221254-official-20260617223721-retry2-clean-redeploy`. Authenticated `/status` returned HTTP 200 and exposed one-to-one source/scan/sink topology plus all three per-root live-node fields, but reportable index count was `sink.live_nodes=0`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, delta `0`, rate `0/s`, and aggregate remained below `200000000`. No `/index/rescan` was called.
- Fresh retry2 raw boundary is not Capanix reachability: post-sample `cnxctl cluster status` showed `peer_count=3`, `panda144`/`panda145`/`panda146` all reachable, gossip-converged, cluster-ready, and `active_processes=1`. `/status` showed source/sink orchestration accepted one-to-one roots, but all source roots had `rescan_pending=true`, `last_audit_started_at_us=null`, `last_audit_completed_at_us=null`, `emitted_event_count=0`, `forwarded_event_count=0`, and source/sink published/received debug maps were empty. Bounded logs under `/tmp/fsmeta-node-logs-source-audit-not-started-20260617223721` showed `release_source_scan_audit_after_sink_scope_ready_if_needed source-status ok ... epoch=1` without any initial/manual scan start.
- Local root cause and fix: `fs-meta/app/src/workers/source_server.rs` handled `OpenScanAuditAdmissionAndTriggerRescanWhenReadyEpoch` by calling `submit_rescan_request_epoch_with_failure()` directly, unlike `SubmitRescanRequestEpoch` and targeted submit paths that record the epoch and defer the scan signal until after the worker reply. The fixed action now calls `begin_rescan_request_epoch()` and returns `deferred_manual_rescan_signal(source, root_keys)` so `/status` still returns without waiting for materialization while the worker sidecar reliably wakes local primary scan roots after the reply.
- Red/green proof for the retry2 boundary: new test `open_scan_audit_admission_trigger_rescan_defers_scan_signal_until_after_reply` failed before the fix because the admission-release action returned no deferred signal, then passed after the fix and observed post-reply baseline event publication. Additional validation passed: `cargo test -p fs-meta-runtime scan_audit_admission -- --nocapture`, `cargo test -p fs-meta-runtime source_worker_when_ready_requests_submit_intent_without_waiting_for_scan_readiness -- --nocapture`, `cargo test -p fs-meta-runtime status_sink_scope_ready_releases_scan_audit_admission_on_worker_backed_source_owners -- --nocapture`, `cargo test -p fs-meta-runtime workers::source_server::tests -- --nocapture` (`35` passed), `cargo fmt -p fs-meta-runtime -- --check`, and `git -C /root/repo/fustor diff --check -- fs-meta/app/src/workers/source_server.rs`.
- Fresh complete-source aibox predeploy gate after the source-worker fix completed as `fullsrc-20260617232753`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617232753-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617232753`; sentinel `AIBOX_GATE_PASS`.
- The valid aibox gate passed the Capanix focused suites (`gossip::transport`, `send_to_once`, link/gossip, full `capanix-kernel`, `capanix-cli` scaffold, runtime timeout-label), release builds, fustor source/status/manual-rescan/API focused suites, formatting, and `CAPANIX_REAL_NFS_E2E=1` `fs_meta_active_three_external_worker_replay_real_nfs`. The earlier `fullsrc-20260617232535` attempt is invalid because `/tmp` ENOSPC broke the run while the wrapper could have printed a false pass; do not use it as product evidence.
- Active-three local real-NFS replay final `/status` for `fullsrc-20260617232753` showed all three sink groups `serving-trusted` with per-group `live_nodes=14` and aggregate `sink.live_nodes=42`, one-to-one `primary_host_ref_by_group`, one-to-one source/scan schedules for `nfs-144`, `nfs-145`, and `nfs-146`, `trusted_observation_readiness=true`, source audit started/completed for all three roots, and `rescan_pending=false`. This is local aibox evidence only; no new official authenticated `/status` sample has been collected after the fix, so the current official status-returned index count remains the retry2 value `sink.live_nodes=0`, latest official rate `0/s`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`.
- Aibox artifact hashes for `fullsrc-20260617232753`: `capanixd=358db9cd9e2755c5668331307732ff26ef77598147f5deabd5f183c7b7c1372f`, `cnxctl=6d32a30db01d211df0770d6b77ea332a561b49062ecb2642127e4a3988452678`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=9d55e0dae2df936b8c6fcf43a0d2e149e34dba3420e5d55594a90accad40f867`, `fsmeta=7949f6d52dfbd732f3d6f97f648187215efda67b017202cef85fe486c332a03b`.
- Compatible-builder attempt from `fullsrc-20260617232753` failed closed before artifact verification: session `54535`, log `/tmp/fsmeta-compatible-builder-fullsrc-20260617232753.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617232753`. The full `capanix-kernel` suite hung after `link::manager::tests::concurrent_send_to_same_peer_reuses_single_connect_attempt has been running for over 60 seconds`; the command was cancelled and remote cargo/test processes were cleared. The same test passed independently under a bounded 180s rerun, proving a test-harness race rather than an official `/status` index-count sample.
- Capanix root fix for that builder boundary: `/root/repo/capanix/crates/kernel/src/link/manager.rs` changed the test release gate from lossy `Notify::notify_waiters()` to a `watch` state gate so a release cannot be lost if the connect future is delayed between `connect_entered.notify_waiters()` and awaiting release. Local validation passed: `cargo test -p capanix-kernel link::manager::tests::concurrent_send_to_same_peer_reuses_single_connect_attempt -- --nocapture`, `cargo test -p capanix-kernel link::manager -- --nocapture`, `cargo fmt -p capanix-kernel -- --check`, and full `cargo test -p capanix-kernel -- --nocapture` (`271` unit tests and `151` specs).
- Because Capanix source changed after the `fullsrc-20260617232753` aibox pass, that aibox result is now superseded as a deploy gate. Next allowed action: fresh complete-source aibox predeploy gate from the current fustor+capanix worktrees, then fresh `panda145` compatible-builder gate, then clean-log official redeploy. Do not deploy to official `panda144`/`panda145`/`panda146` until the refreshed compatible-builder gate passes and exact artifact GLIBC/hash evidence is recorded.
- Fresh `panda145` compatible-builder gate `fullsrc-20260617221254` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617221254`; local log `/tmp/fsmeta-compatible-builder-fullsrc-20260617221254.log`; remote focused capanix/fustor tests passed, including the Capanix heartbeat timeout non-mutating guard, `send_to_once`, gossip/link/scaffold tests, fs-meta API/status/manual-rescan suites, release builds for all five deployable artifacts, and all deployable ELF max GLIBC values were `GLIBC_2.16`.
- Compatible-builder hashes for `fullsrc-20260617221254`: `capanixd=a93e99b0b3de92193dd1d8e5c62d1ae38db33ea2ea64343fcb379db285bd93b6`, `cnxctl=1dcc10e707a892ace7c6976a59c91789b8525414a3a7a9df4dba958968dcbfcf`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=65d93ca365bc6f5959faa72aadc9421d358685508ed583fda7f6ace3337f5702`, `fsmeta=ec955f1c13f78dc3e61333f11eeae9ab6cbf49ea16990530689c5b5d9d8a7717`.
- Previous `panda145` compatible-builder gate `fullsrc-20260617205449` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617205449`; local log `/tmp/fsmeta-compatible-builder-fullsrc-20260617205449.log`; remote focused capanix/fustor tests passed, including the Capanix heartbeat-ticker skip/gossip/link/scaffold tests and fs-meta API/status/manual-rescan suites, and all deployable ELF max GLIBC values were `GLIBC_2.16`.
- Compatible-builder hashes for `fullsrc-20260617205449`: `capanixd=f376661d03b2caafe7e8f5d4e71987f889fc234fc4936adfe794aee355fb3b71`, `cnxctl=db8679366785768a815752fc937880ed5569d0d3a1925bf30566d2864fe5cc0e`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=4ea66211037be218d71ac46b4734c2edac432a0c4cce92a65c0f049e970fdef7`, `fsmeta=6c6fd8f1af6f0522ad8c45a39b50a3aaf4dee5621a08fc5967e4f2fb5c7a4ab7`.
- Official clean-log redeploy `fullsrc-20260617205449-official-20260617212156` completed from compatible-builder artifacts `fullsrc-20260617205449`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617205449-official-20260617212156.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617205449-official-20260617212156-clean-redeploy`. The post-wait authenticated `/status` sample returned HTTP 200 but reportable count regressed from the previous comparable `sink.live_nodes=58754377` to `sink.live_nodes=0`, delta `-58754377` over 900s, rate `-65282.64/s`, with per-root `nfs-144=0`, `nfs-145=0`, and `nfs-146=0`. This is an active count-regression, slow-growth, and below-`200000000` anomaly, not a patience-only `pending-materialization` state; no `/index/rescan` was called or authorized.
- Fresh post-redeploy raw boundary remains Capanix reachability/heartbeat transport: `/status` still exposed one-to-one source/sink/scan scheduling and all three per-root live-node fields, but only `panda145` published a small event count while sink received no events; post-wait `cnxctl cluster status` from `panda145` showed `panda144` and `panda146` `reachable=false` despite `peer_count=3` and `cluster_ready`. Bounded logs `/tmp/fsmeta-node-logs-post-heartbeat-redeploy-zero-20260617212156` and `/tmp/fsmeta-node-logs-post-heartbeat-redeploy-transport-20260617212156` show local root readiness on peers plus heartbeat/QUIC timeouts from `panda145` to `10.0.82.144:19401` and `10.0.82.146:19401`.
- Previous deployed-candidate `panda145` compatible-builder gate `fullsrc-20260617175625` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617175625`; local log `/tmp/fsmeta-compatible-builder-fullsrc-20260617175625.log`; remote focused capanix/fustor tests passed, including the Capanix diagnostic-read/heartbeat/runtime timeout-label tests and fs-meta API/status/manual-rescan suites, and all deployable ELF max GLIBC values were `GLIBC_2.16`.
- Compatible-builder hashes: `capanixd=9a4bf959015fb24864fa3c8f2b5d91d4e51519d0c179d495d7f80ff95c0bf71a`, `cnxctl=e17512f995c756b14f62433cd5a0ba5ecb413d21f4cfc1925634570f40db7b24`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=5f9dfdc2f3c9cddbd7ce3f3e534c25493c5bd2ff82d21724500f44702f343c0b`, `fsmeta=01e26746d558af29cadbb00825e7a3fe8e02f3bea6d730461025c9a420de545e`.
- Official clean-log redeploy `fullsrc-20260617175625-official-20260617182416` completed from compatible-builder artifacts `fullsrc-20260617175625`: all five artifacts were staged and hash-verified on `panda144`, `panda145`, and `panda146`; old live hashes were recorded; live binaries were replaced after node-owned stop plus clean-log cleanup; each node restarted through its own `nodes/<host_ref>/start.sh`; and self-NFS was verified as `10.0.82.144:/data/fustor-nfs -> /mnt/fustor-peers/nfs144`, `10.0.82.145:/data/fustor-nfs -> /mnt/fustor-peers/nfs145`, and `10.0.82.146:/data/fustor-nfs -> /mnt/fustor-peers/nfs146`.
- Official restore evidence for `fullsrc-20260617175625-official-20260617182416`: resource announce, `fsmeta deploy` with redacted evidence plus refreshed management shadow, roots preview/apply, config dump, and post-restore `cnxctl cluster status` all succeeded. State: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617175625-official-20260617182416-clean-redeploy`; log: `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617175625-official-20260617182416.log`.
- Post-deploy official evidence after the built-in 900s local wait: authenticated `/status` through `panda145` returned HTTP 200 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617175625-official-20260617182416-clean-redeploy/post-deploy-status/status-body.json`. `cnxctl cluster status` showed `peer_count=3` with `panda144`, `panda145`, and `panda146` all `reachable=true`, `gossip_converged=true`, and `readiness_phase=cluster_ready`.
- The post-deploy `/status` sample has correct one-to-one topology but has not reached final materialization: `facade.state=serving`, `rollout.state=stable`, `sink.primary_host_ref_by_group={"nfs-144":"panda144","nfs-145":"panda145","nfs-146":"panda146"}`, `source.debug.scheduled_source_groups_by_node` and `source.debug.scheduled_scan_groups_by_node` are one-to-one, `source.debug.source_primary_by_group={"nfs-144":"panda144::nfs-144","nfs-145":"panda145::nfs-145","nfs-146":"panda146::nfs-146"}`, and source concrete roots for all three groups are active group primaries. The remaining state is expected high-NFS catch-up: all three sink groups are `service_state=selected-pending`, `materialization_readiness=pending-materialization`, `initial_audit_completed=false`, `sink.live_nodes=0`, and `readiness_planes.trusted_observation_readiness=false`.
- Index count for the post-deploy `/status` sample: the status body exposed aggregate `sink.live_nodes=0` with all three per-group `live_nodes=0`; the generated summary file marked `global_count_exposed=false` only because it looked for an explicitly named global field. `sink.live_nodes` is the current status-returned live-node index count for reporting, even during `pending-materialization`. This is not final acceptance and does not authorize `/index/rescan` because topology and reachability are healthy while materialization remains pending.
- Follow-up read-only convergence observation `official-readonly-convergence-20260617184616` completed a 900s local wait in exec session `52514` and collected one authenticated `/status` sample through `panda145`. The sample returned HTTP 200 with `sink.primary_host_ref_by_group={"nfs-144":"panda144","nfs-145":"panda145","nfs-146":"panda146"}`, one-to-one source/scan schedules, and cluster status `peer_count=3` with all three nodes reachable/cluster-ready. Current status-returned live-node index count is `sink.live_nodes=14688097`; per-group breakdown is `nfs-144=0`, `nfs-145=14688097` (`total_nodes=14688098`, `service_state=serving-degraded`), and `nfs-146=0`. The sink groups remain `materialization_readiness=pending-materialization` with `initial_audit_completed=false`.
- Index count for `official-readonly-convergence-20260617184616`: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-convergence-20260617184616/index-entry-summary.json` includes `sink.live_nodes=14688097` and 49 candidate count fields, while `global_live_node_index_entry_count=null` only reflects the absence of an explicitly named global alias. Treat `sink.live_nodes` as the reportable current index-entry count until the API exposes a dedicated alias. Final acceptance still requires stable/increasing count above `200000000`.
- New live-node anomaly gate: every authenticated `/status` sample must record aggregate count, per-root count, and rate against the previous comparable sample when possible. Treat these as active anomalies requiring root-cause analysis and repair, not as ordinary pending-materialization: count regression, growth below `10000/s` over a completed observation window, any active `panda14x` root/peer lacking live-node data or stuck at zero while another peer advances, and aggregate count below `200000000`.
- Read-only convergence observation `official-readonly-convergence-20260617191218` completed a 900s local wait in exec session `65649`; log `/tmp/fsmeta-official-readonly-convergence-official-readonly-convergence-20260617191218.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-convergence-20260617191218`. Authenticated `/status` returned HTTP 200 with reportable `sink.live_nodes=14688097`, unchanged from the prior comparable sample: delta `0` over 900s, rate `0/s`, below the `10000/s` anomaly threshold and far below final `200000000`. Per-root breakdown remained `nfs-144=0`, `nfs-145=14688097`, `nfs-146=0`. Source debug showed only `panda145` produced materialized events (`panda144=3`, `panda145=14688099`, `panda146=2`), and sink debug received only from `panda145`, so this is not just patient pending-materialization.
- Environment root-cause fragment found and repaired: the actual local daemon environments on `panda144` and `panda146` lacked the high-NFS source scan tuning already present on `panda145` (`FS_META_SOURCE_SCAN_WORKERS=16`, `FS_META_SOURCE_HOST_FS_OP_WORKERS=64`, `FS_META_SOURCE_THROTTLE_INTERVAL_MS=50`, `FS_META_SOURCE_HOST_FS_OP_TIMEOUT_SECS=15`). The local `node.env` files were backed up and updated on their own hosts, then `panda144` and `panda146` were restarted through their node-owned scripts; new process environments verified the four variables. No binaries changed and `/index/rescan` was not called.
- Post-tuning read-only observation `official-readonly-post-scan-tuning-20260617193507` completed a bounded sample after the restart; log `/tmp/fsmeta-official-readonly-post-scan-tuning-official-readonly-post-scan-tuning-20260617193507.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-post-scan-tuning-20260617193507`. Authenticated `/status` still returned `sink.live_nodes=14688097`, delta `0`, rate `0/s`, with `nfs-144=0`, `nfs-145=14688097`, and `nfs-146=0`; source published events remained `panda144=3`, `panda145=14688099`, `panda146=2`. The env fix was necessary but insufficient.
- Fresh first raw boundary after the env repair: post-tuning `cnxctl cluster status` succeeded, but from the `panda145` facade view `result.cluster.nodes` reported `panda144` and `panda146` as `reachable=false`, while `panda145` had `gossip_converged=false`, `reachable_peers=0`, `peer_unreachable_buffered=34`, and high backpressure. `/status` also retained old source audit start timestamps after the remote restarts. The next bounded action is official node-log evidence for reachability/backpressure/status-source fan-in before any further repair. Do not call `/index/rescan`.
- Bounded node logs `/tmp/fsmeta-node-logs-post-tuning-reachability-20260617193507` proved the first raw boundary was not local fs-meta root scheduling: `panda144` and `panda146` both logged local `root task stream ready`, local source/sink scheduling for their own roots, and `source-status ok`, while `panda145` repeatedly logged QUIC heartbeat timeouts to `10.0.82.144:19401` and `10.0.82.146:19401`. Direct process checks then showed `panda144` and `panda146` `daemon.pid` files pointed at exited processes, so `panda145` was correctly timing out against dead peer daemons.
- Operational repair completed: `panda144` and `panda146` were restarted through their node-owned scripts and verified alive (`panda144 pid=34142`, `panda146 pid=110474`) with self bind addresses and the four high-NFS `FS_META_SOURCE_*` tuning variables in process env. A follow-up authenticated sample `official-restart-recover-status-20260617200112` had no reportable count because `/status` returned HTTP 503 with `sink_owner_transport_gaps={"nfs-144","nfs-146"}`; cluster status already showed all three peers reachable, but serve announcements initially existed only from `panda145`.
- App/roots recovery after the peer daemon restart: resource announce succeeded for all three nodes. A full `fsmeta deploy` retry hit `remote_execution/tx_busy` after leaving the app in empty-roots bootstrap/listener state, then direct `roots preview/apply` using `/home/wanghuajin/fsmeta-stable/run/fs-meta-embedded.yaml` succeeded at `official-roots-apply-after-peer-restart-20260617200700`. Cluster status then showed `panda144`, `panda145`, and `panda146` reachable, gossip-converged, cluster-ready, and `active_processes=1`; authenticated `/status` returned HTTP 200 with `sink.live_nodes=14688097`, delta `0`, rate `0/s`, per-root `nfs-144=0`, `nfs-145=14688097`, `nfs-146=0`.
- Capanix root-fix for the dead-pid false-positive class: `/root/repo/capanix/crates/cli/src/scaffold.rs` now generates `start.sh` with a `CAPANIX_START_GRACE_SECS` post-start liveness check. If `capanixd` exits immediately, the script removes `daemon.pid`, prints the daemon log tail, and exits nonzero instead of reporting a dead pid as started. Existing official `nodes/panda144/start.sh`, `nodes/panda145/start.sh`, and `nodes/panda146/start.sh` were patched equivalently with backups `*.failclosed-202606172008.bak`. Validation passed: `cargo test -p capanix-cli scaffold -- --nocapture`, `cargo fmt -p capanix-cli -- --check`, `git -C /root/repo/capanix diff --check -- crates/cli/src/scaffold.rs`, and `git -C /root/repo/fustor diff --check -- TODO.md`.
- Post-recovery read-only convergence wait `official-post-recovery-readonly-20260617201107` completed in foreground exec session `19556`; log `/tmp/fsmeta-official-post-recovery-readonly-official-post-recovery-readonly-20260617201107.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/__RUN_ID__` due to an operator-script naming bug. Authenticated `/status` returned HTTP 200 with `sink.live_nodes=58754377`, delta `44066280` from the prior `14688097`, rate `48962.53/s`, and per-root `nfs-144=14689714`, `nfs-145=29368874`, `nfs-146=14695789`. This closes the zero-peer and slow-growth anomaly for the latest window, but final count remains below `200000000`. Cluster status still had all three nodes reachable/cluster-ready with `active_processes=1`, while `gossip_converged=false` persisted for `panda144`/`panda145`.
- Follow-up read-only convergence wait `official-post-recovery-readonly2-20260617202740` completed in foreground exec session `30972`; log `/tmp/fsmeta-official-post-recovery-readonly2-official-post-recovery-readonly2-20260617202740.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-post-recovery-readonly2-20260617202740`. Authenticated `/status` returned HTTP 200 but `sink.live_nodes` remained `58754377`, delta `0`, rate `0/s`, with unchanged per-root counts `nfs-144=14689714`, `nfs-145=29368874`, and `nfs-146=14695789`. This reopens the slow-growth anomaly after the initial catch-up burst. Cluster status still showed all three `active_processes=1` and cluster-ready, but `panda146` was `reachable=false` from the `panda145` sample and `panda144`/`panda145` still had `gossip_converged=false`. Next boundary is post-recovery transport/gossip or source scan progress stall, not `/index/rescan`.
- Bounded post-recovery transport logs `/tmp/fsmeta-node-logs-post-recovery-stall-20260617202740` localize the first raw boundary to Capanix inter-node gossip/QUIC churn, not missing local fs-meta root scheduling: `panda144` and `panda146` each logged local root task readiness and `source-status ok`, but the same window had heartbeat failures `panda144 -> panda145=44`, `panda144 -> panda146=24`, `panda145 -> panda144=3`, `panda145 -> panda146=13`, `panda146 -> panda144=29`, `panda146 -> panda145=57`; inbound accept failures were `panda144=128`, `panda145=364`, `panda146=148`, mostly `QUIC accept: aborted by peer`, plus accept timeouts on `panda145=23` and `panda146=41`. The persisted `/status` body still exposed one-to-one topology and per-root live-node data, but `source.concrete_roots` had `panda146::nfs-146 last_error=backpressure`.
- Bounded source-scan logs `/tmp/fsmeta-node-logs-source-scan-stall-20260617202740` show all three nodes repeatedly reapplying facade/query-peer routes and source/sink orchestration without new audit completion evidence: `apply_facade_activate` counts were `panda144=180`, `panda145=356`, `panda146=180`; source orchestration remained one-to-one (`nfs-144`, `nfs-145`, `nfs-146`) and no per-node root was absent. This keeps the current owner on Capanix transport/reconcile amplification rather than `/index/rescan` or missing root config.
- Capanix local root-fix for the current gossip burst class: `/root/repo/capanix/crates/kernel/src/gossip/transport.rs` now constructs the heartbeat ticker with `MissedTickBehavior::Skip`, so a slow/timeout heartbeat round does not burst-submit every missed tick and amplify QUIC reconnect storms under high-NFS load. Guard test `heartbeat_ticker_skips_missed_ticks_instead_of_bursting` covers the behavior. Validation passed: `cargo test -p capanix-kernel gossip::transport -- --nocapture`, `cargo test -p capanix-kernel gossip -- --nocapture`, `cargo fmt -p capanix-kernel -- --check`, and `git diff --check -- crates/kernel/src/gossip/transport.rs`.
- Capanix local root-fix for the fresh post-redeploy heartbeat budget class: `/root/repo/capanix/crates/kernel/src/link/manager.rs` now exposes `send_to_once` for liveness traffic, and `/root/repo/capanix/crates/kernel/src/gossip/transport.rs` uses it for gossip heartbeats with a heartbeat-interval bounded timeout capped at 2s. The first aibox attempt for this fix (`fullsrc-20260617215332`) proved the timeout branch was still too destructive: it closed peer pools on timeout, then local active-three replay hung in `/index/rescan` source-status readiness and later login timed out while node-a/node-c reported repeated 2s heartbeat send timeouts to the facade node. Timeout is only a liveness sample, not proof that an inbound/outbound session is corrupt, so the local correction now stops closing peer pools from the heartbeat timeout branch while retaining explicit `send()` error cleanup in `send_to_once`. Guard test `heartbeat_timeout_does_not_close_tracked_inbound_peer_session` covers this regression class. Validation passed: `cargo test -p capanix-kernel gossip::transport -- --nocapture`, `cargo test -p capanix-kernel send_to_once -- --nocapture`, `cargo test -p capanix-kernel link::manager -- --nocapture`, `cargo test -p capanix-kernel gossip -- --nocapture`, full `cargo test -p capanix-kernel -- --nocapture` (`271` unit tests and `151` specs passed), `cargo fmt -p capanix-kernel -- --check`, `git -C /root/repo/capanix diff --check`, and `git -C /root/repo/fustor diff --check -- TODO.md`.
- The previous `official-readonly-convergence-20260617164627` transport/`tx_busy` boundary is superseded by the current clean-log redeploy evidence: the latest cluster status is reachable/cluster-ready for all three nodes. Do not reopen that Capanix transport seam without fresh raw evidence from this candidate.
- Previous official read-only observation `official-readonly-status-20260617150325` completed a 900s local wait in exec session `71622`; the first remote sample wrapper failed before `/status` because of an operator token-parse script bug after login HTTP 200, then corrected retry evidence `official-readonly-status-20260617150325-retry1` collected one authenticated `/status` sample through `panda145`.
- That previous official `/status` sample `official-readonly-status-20260617150325-retry1` returned HTTP 503 with fail-closed reason: `sink_route=ok source_route=ok expected_groups={"nfs-144","nfs-145","nfs-146"}`, `source_health_gaps={}`, `source_coverage_gaps={"nfs-146"}`, `scan_coverage_gaps={"nfs-146"}`, `sink_report_gaps={}`, `sink_schedule_gaps={}`, `sink_owner_partition_gaps={"nfs-146":"missing_source_owner"}`, and `sink_owner_transport_gaps={}`. Evidence: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-20260617150325-retry1`; this boundary is superseded by the current redeployed HTTP 200 status sample above.
- Index count for that previous official `/status` sample: `index-entry-summary.json` recorded neither an explicitly named global alias nor `sink.live_nodes` / `index/live/count/entries` candidates, so no reportable index count was available from that 503 response.
- Bounded node logs `/tmp/fsmeta-node-logs-official-readonly-status-20260617150325-retry1` show `panda146` local source/sink state is present (`root task stream ready` for `nfs-146`, `source_groups=Some({"nfs-146"})`, `scan_groups=Some({"nfs-146"})`, sink scheduled groups/targets for `nfs-146`, and `source-status ok node=panda146 route=source-status.panda146:v1.req`). The `panda145` facade log still failed closed with `nfs-146` omitted from `last_source` after a `manual rescan scoped source-status target probe failed node=panda146 err=operation timed out`. This raw boundary is fustor status/source-status fan-in evidence retention under owner-route timeout; it is locally addressed below, is not pending-materialization, and does not authorize `/index/rescan`.
- Runtime environment diagnostic for the repeated `panda146` boundary found no artifact/config/start mismatch: all three official daemons were node-owned `capanixd` processes with the expected bind/config pattern, while `panda146` differed by runtime load. Its `capanixd` was around `90%` CPU with three hot `capanix-unit-br` threads, RSS was much lower than `panda144`/`panda145`, and its active root `nfs-146` mounted `10.0.82.147:/data/fustor-nfs` at `/mnt/fustor-peers/nfs147`; that backing `/data` was `99%` used with about `2.4T` free, unlike the `nfs145`/`nfs146` backends at about `73-74%` used. Latest node logs still showed `panda146` local worker/control success (`update_logical_roots`, `refresh_cached_scheduled_groups`, `observability_snapshot ok=true`), so the product boundary remains facade fan-in/source-status proof timing rather than missing local worker startup.
- User clarified the official NFS topology requirement: each active client must mount its own NFS server. The previous run configuration violated this (`panda144->10.0.82.145`, `panda145->10.0.82.146`, `panda146->10.0.82.147`), so the current first raw boundary is a run-configuration defect, not a code-level owner-cache requirement.
- Self-NFS correction completed on the official active-three nodes: `/etc/fstab` was backed up and corrected on `panda144`, `panda145`, and `panda146`; active mounts now are `10.0.82.144:/data/fustor-nfs -> /mnt/fustor-peers/nfs144`, `10.0.82.145:/data/fustor-nfs -> /mnt/fustor-peers/nfs145`, and `10.0.82.146:/data/fustor-nfs -> /mnt/fustor-peers/nfs146`. The old wrong `fustor-peers` mounts to `nfs145`, `nfs146`, and `nfs147` were unmounted without force.
- Fresh self-NFS verification at `20260617125714+0800`: active raw `findmnt` entries under `/mnt/fustor-peers` contain exactly `10.0.82.144:/data/fustor-nfs -> /mnt/fustor-peers/nfs144` on `panda144`, `10.0.82.145:/data/fustor-nfs -> /mnt/fustor-peers/nfs145` on `panda145`, and `10.0.82.146:/data/fustor-nfs -> /mnt/fustor-peers/nfs146` on `panda146`; `/etc/fstab`, `monitoring-roots-3group.local.json`, and `fs-meta-all-resources.yaml` all match the same self-NFS mapping.
- Fresh self-NFS recheck at `20260617130651+0800`: `findmnt` again shows `panda144` mounted `10.0.82.144:/data/fustor-nfs` at `/mnt/fustor-peers/nfs144`, `panda145` mounted `10.0.82.145:/data/fustor-nfs` at `/mnt/fustor-peers/nfs145`, and `panda146` mounted `10.0.82.146:/data/fustor-nfs` at `/mnt/fustor-peers/nfs146`. `/etc/fstab` keeps only the matching active self-NFS lines for those mount points, with the old wrong-peer entries commented as `fsmeta-selfnfs-disabled`; `monitoring-roots-3group.local.json` and `fs-meta-all-resources.yaml` still point `nfs-144`, `nfs-145`, and `nfs-146` to the matching self servers and mount hints.
- Official run config files were updated on all three nodes: `monitoring-roots-3group.local.json` and `fs-meta-all-resources.yaml` now point `nfs-144`, `nfs-145`, and `nfs-146` at the matching local host/server/mount. `cnxctl config resource-announce` returned `status=ok` with `announced=4`; roots preview then reported `unmatched_roots=[]`, one matched grant per root, and monitor paths `/mnt/fustor-peers/nfs144`, `/mnt/fustor-peers/nfs145`, `/mnt/fustor-peers/nfs146`; roots apply returned `status=ok`, `roots_count=3`. Evidence state: `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-resource-20260617111148` and `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-apply-20260617111210`.
- Self-NFS post-correction `/status` sample after a 900s bounded wait returned HTTP 503 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-status-20260617112344/status-body.json`; `index-entry-summary.json` recorded neither an explicitly named global alias nor `sink.live_nodes` / `index/live/count/entries` candidates, so no reportable index count was available from that 503 response.
- The new post-self-NFS raw boundary is not the old missing-source coverage shape: `source_coverage_gaps={}` and `scan_coverage_gaps={}`, but `sink_owner_partition_gaps={"nfs-146": "primary_host_ref=panda146 source_owner=panda144"}` plus transient `sink_owner_transport_gaps={"nfs-145"}`. Bounded node logs at `/tmp/fsmeta-node-logs-selfnfs-status-20260617112344` show `panda145` current source proof still had only `nfs-144`/`nfs-145` concrete roots and `panda146` did not log a fresh post-self-NFS roots update, so the next operator action is to re-run the official resource announce, `fsmeta deploy`, and roots preview/apply sequence to force runtime accepted-scope replay before changing code.
- Accepted-scope replay initially hit repeat `fsmeta deploy` `tx_busy` after resource announce succeeded, then config dump showed the failed deploy had left `apps=[]` while corrected announced resources remained present. Recovery used the official node-owned `stop.sh/start.sh` scripts on all three nodes, then redeployed `fs-meta` from empty app state and restored roots. Evidence: failed deploy dirs `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-scope-replay-20260617114654` and `selfnfs-scope-replay-retry-20260617114847`; API-down/empty-app evidence `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-api-down-config-20260617120000`; successful recovery `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-redeploy-from-empty-20260617120456`.
- Successful recovery details: `resource-announce=ok`, `deploy=ok`, `roots-preview=ok`, `roots-apply=ok`, `config-dump=ok`; deploy route plan node IDs were exactly `["panda144","panda145","panda146"]`; preview matched one local grant per root with `10.0.82.144:/data/fustor-nfs -> /mnt/fustor-peers/nfs144`, `10.0.82.145:/data/fustor-nfs -> /mnt/fustor-peers/nfs145`, and `10.0.82.146:/data/fustor-nfs -> /mnt/fustor-peers/nfs146`; roots apply returned `status=ok`, `roots_count=3`, `mode=management_api_with_accepted_scope_followup`, `tx_busy_retries=0`; config dump app scopes now include exactly `nfs-144`, `nfs-145`, `nfs-146`, and `fs-meta-tcp-listener`.
- Post-restore `/status` after a 900s bounded wait returned HTTP 200 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-postrestore-status-20260617121428`: sink primary map is one-to-one for all three roots, source debug maps show one-to-one scheduled source/scan groups and `source_primary_by_group` for all three roots, and source concrete roots include `panda144::nfs-144`, `panda145::nfs-145`, and `panda146::nfs-146`. The remaining state is materialization pending: all three sink groups have `materialization_readiness=pending-materialization`, `initial_audit_completed=false`, `sink.live_nodes=0`, and `trusted_observation_readiness=false`.
- Index count for the post-restore `/status` sample: `index-entry-summary.json` recorded no explicitly named global live-node alias (`global_live_node_index_entry_count=null`, `global_count_exposed=false`), but it did extract aggregate `sink.live_nodes=0` and per-group `live_nodes=0`. Under the current reporting rule, `sink.live_nodes=0` is the status-returned reportable index count for that pending-materialization sample; it was not final acceptance.
- Explicit `/index/rescan` after the HTTP 200 pending sample returned HTTP 503 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-rescan-20260617123134`: `manual rescan current roots runtime-scope readiness failed` with `coverage_gaps={"nfs-144","nfs-146"}`, no route activation/readiness gaps, and `probe_state=direct-source-status-probe-incomplete`. The response scheduled `source-repair` for `manual-rescan-source-repair`, so the next step is a bounded post-repair wait and one status sample before relocalizing.
- Post-rescan repair wait `/status` returned HTTP 503 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-postrescan-status-20260617123331`: `sink_route=timeout`, `source_route=timeout`, `source_scope_gaps={"nfs-144","nfs-146"}`, and `scan_scope_gaps={"nfs-144","nfs-146"}`. Index summary exposed neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that 503 response.
- Fresh node logs `/tmp/fsmeta-node-logs-selfnfs-postrescan-status-20260617123331` show the remote nodes were locally ready despite facade timeout: `panda144` and `panda146` each had `root task stream ready`, source/scan groups for their own root, sink scheduled groups for their own root, and `release_source_scan_audit_after_sink_scope_ready_if_needed source-status ok`. The facade-side failure on `panda145` was `manual rescan scoped source-status target probe failed node=panda144 err=operation timed out` and the same for `panda146`, after which only `nfs-145` local proof remained. Current boundary is therefore status/manual-rescan fan-in evidence retention or retry behavior under remote target timeout, not NFS topology.
- Fresh relocation sample `status-reloc-20260617130255` returned authenticated `/status` HTTP 503 with the same fail-closed shape: `sink_route=timeout`, `source_route=timeout`, `source_scope_gaps={"nfs-144","nfs-146"}`, and `scan_scope_gaps={"nfs-144","nfs-146"}`. Index summary recorded neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that 503 response. Bounded node logs `/tmp/fsmeta-node-logs-status-reloc-20260617130255` show `panda144` and `panda146` still have local `root task stream ready` and `source-status ok` evidence, while `panda145` logs repeated `gossip heartbeat send failed` / `QUIC connect ... timed out after 5s` to both `10.0.82.144:19401` and `10.0.82.146:19401` before the `/status` fail-closed line. Current first raw boundary is now Capanix inter-node QUIC transport/gossip connectivity from `panda145` to peer owners, not fustor status evidence synthesis.
- Capanix local root cause identified and fixed: `GossipState::heartbeat_targets()` emitted seed targets before learned peer targets, so a known peer address was shadowed by `NodeId("<ip>:19401")` instead of the stable node id (`panda144`/`panda146`). Official evidence `/tmp/fsmeta-node-logs-capanix-quic-20260617131125` shows repeated address-alias heartbeat targets and no stable `target_id=panda...` heartbeat targets, and sanitized `cnxctl cluster status` from `panda145` listed `panda144`/`panda146` as `reachable=false` despite correct `management_peers` config and correct QUIC UDP buffer provisioning. The capanix fix now prioritizes learned stable peer targets and maps seed addresses back to known stable peer ids while preserving seed reconnect for unreachable peers.

## Current Local Closure

- Latest local Fustor fix for the follow-up8 false-negative: `fs-meta/app/src/query/observation.rs` now allows trusted/materialized readiness to remain open while the next source audit is in progress only when the same group has source logical service-ready evidence, a concrete group-primary source root with no rescan/overflow/error and settled nonzero data publication/forwarding evidence, and a live materialized ready sink group. It keeps fail-closed behavior for first audit in-flight, rescan pending, missing source coverage, unready sink, missing sink ingress, and overflow-pending sink groups. `fs-meta/app/src/api/handlers.rs` adds the matching `/status` trusted-readiness regression.
- Breadth-first static review kept the boundary on `fustor`: the duplicate roots-control replay branch violated the manual-rescan spec by reopening the roots-control send cache and returning `retry will reissue` before observing owner-scoped source-status/readiness evidence.
- Current local fix: when same-generation roots-control replay is skipped by the duplicate-send cache, manual rescan first reuses a complete local receive-armed current-roots snapshot when available; otherwise it continues through the existing bounded owner-scoped source-status readiness path without reissuing roots-control. Complete owner-scoped source-status proof returns scoped delivery targets. Missing receive-armed proof still fails closed with retryable source-status non-ready/route-gap evidence and keeps the roots-control cache so the next retry observes readiness instead of timer-replaying roots-control.
- Latest local fix: the retryable non-ready preflight predicate no longer returns fast-503 when the merged current-roots source-status evidence proves current roots, source/scan runtime scope, scoped rescan route activation, route readiness, receive-armed evidence, and non-empty scoped delivery targets. Cache/pending provenance still cannot become owner-origin delivery proof by itself; missing health or missing receive-armed route evidence still fails closed as retryable non-ready/route-gap evidence.
- Adjacent closed seams remain closed and should not be revisited without fresh raw evidence: current-roots source/scan evidence survives later partial target probes, target-proof `Timeout`/`TxBusy` retry stays bounded, status fan-in uses owner-scoped source/sink evidence without fabricating ownership, source-observability retention drops removed roots, and initial scan/audit admission can release owners once sink schedule plus source owner scope is closed.
- Active-three status fan-in closure: long runtime node IDs from the status path are pinned by `management_status_source_fanin_recovers_long_node_owner_scope_from_status_path`, and the focused real-NFS active-three replay now passes without debug fan-in logging.
- Latest local fix: when `/status` exceeds `STATUS_RESPONSE_TIMEOUT`, the handler now schedules cancellation of the internal `status_impl` task off the response path so route/collection guards are released without waiting for cancellation cleanup. A test-only blocking-drop hook still proves the HTTP response does not wait for cancellation destructor work.
- Latest local fix: management `/status` owner-scoped source-status now retries transient timeout/`operation timed out` errors within the owner-scoped deadline, using a 1s per-attempt cap so existing bounded slow-owner probes still pass. This closes the `nfs-146` missing-source-owner gap without fabricating ownership from grants or sink rows.
- Latest local fix: serving `/status` short-budget source owner fan-in now uses the same grant fallback as the actual owner-scoped source-status collect. This prevents a generic source-health reply with missing source/scan runtime scope from skipping the owner route only because the generic snapshot lacks source-primary/debug route maps.
- Latest local fix: source-status/manual-rescan readiness now treats a node-scoped `source-manual-rescan.<node>:v1.req` route as owned by the node named in the route key even when its active scope resource is the logical root id (`nfs-146=>nfs-146`) rather than a host object ref. This closes the official root-id scoped-route false negative where `panda146` spawned `source-manual-rescan.panda146:v1.req` but source-status reported it as not receive-armed.
- Adjacent breadth-first fix: scoped source-rescan proxy ready marker refresh now uses current `is_receive_armed()` endpoint state, not historical receive-poll observation, before recording current-generation readiness. Existing ready-generation markers still preserve already observed source-owned receive-armed proof across identical route refreshes.
- Local status fan-in remains fail-closed for source-only ownership: source owner partition proof still requires source and scan runtime-scope ownership on the same node, and focused status tests reject missing scan ownership instead of treating it as complete evidence.
- Fresh compatible-builder `fullsrc-20260617095202` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617095202`; local log `/tmp/fsmeta-compatible-builder-fullsrc-20260617095202.log`; remote focused tests passed and all deployable ELF max GLIBC values were `GLIBC_2.16`.
- Compatible-builder hashes: `capanixd=51ab562bfabdc804cf529b3af9ead5eb34bd32fd66ac97f2f74fb9dabe5b2dbe`, `cnxctl=1f14d4de420436c88e2c4d510ef5a09db515d835c3b98552251efdae88d14b47`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=546071c9dd62172c6600652b0cde58685f78954cd30e0eb25bf1fc62e02f97bb`, `fsmeta=fcaf0c02b02e7f5aa9c6fab5c252b5a4af796f375c34d58541d92614d09c8784`.
- Current local target is closed for the capanix address-alias heartbeat target class through local red/green tests, fresh complete-source aibox evidence, fresh `panda145` compatible-builder evidence, and official clean-log redeploy evidence. Do not reopen capanix unless fresh official raw evidence shows a new capanix boundary.
- Index-count reporting rule: whenever an authenticated `/status` sample is collected, record and report the status-returned live-node index entry count. If an explicitly named global field is absent, use aggregate `sink.live_nodes` as the current index-entry count and `sink.groups[].live_nodes` as the per-root breakdown; report count absence only when neither an explicit global field nor `sink.live_nodes` is present.
- System API fail-closed audit after the `/index/rescan` pending-only misuse fix found no additional immediate fail-open route in the exposed API planes: route policy keeps `PUT /monitoring/roots` on FullManagementWrite, `POST /index/rescan` on SourceRepair, query reads on query-key/facade drain only, and `/status` out of facade drain by spec; query trusted-materialized root/stats gates still fail closed on partial, missing, or untrusted materialization evidence; management/query auth remains separated.
- Follow-up `/index/rescan` gate audit kept the gate fail-closed rather than broadening it: pending-only sink materialization is rejected before scoped source delivery, and current-roots preflight rejects missing scoped rescan route activation/readiness or cache/degraded-only source evidence. A side-effect-free front-door rescan gate was not added because the current status route helpers are not a harmless peek on all test/production boundaries; do not use `/index/rescan` as a pending-materialization accelerator.
- Latest local fix for the official `official-readonly-status-20260617150325-retry1` `/status` boundary: `release_source_scan_audit_after_sink_scope_ready` now records owner source-status snapshots returned by owner routes into a roots/grants/sink-owner signed evidence cache, and management `/status` merges only matching cached owner snapshots before authoritative fail-closed. This reuses real owner-route source-status evidence when the later current-owner-health probe times out; it still fails closed if the signature mismatches or no owner-proving source-status snapshot exists.
- Capanix local target-id alias closure: red test `cargo test -p capanix-kernel heartbeat_targets_prefer_stable_peer_id_over_seed_addr_alias -- --nocapture` failed before the fix with target `NodeId("10.0.0.2:5000")` shadowing `NodeId("n2")`, then passed after the fix. Adjacent guard `heartbeat_targets_keep_stable_peer_id_for_seed_reconnect_when_peer_unreachable` proves reconnect still uses the stable peer id. Passed validation: `cargo test -p capanix-kernel heartbeat_targets -- --nocapture`, `cargo test -p capanix-kernel gossip::state::tests -- --nocapture`, `cargo test -p capanix-kernel gossip -- --nocapture`, `cargo test -p capanix-kernel link::manager -- --nocapture`, full `cargo test -p capanix-kernel -- --nocapture`, `cargo fmt -p capanix-kernel -- --check`, and `git diff --check -- crates/kernel/src/gossip/state.rs`.
- Latest Capanix local diagnostic-read closure for the fresh `panda144` `tx_busy` facet: `cluster_state_get` and `membership_summary_get` still require `ClusterRead` authorization, but no longer take the global control-visibility read gate because they read gossip/link evidence rather than route/config mutation state. Other privileged reads still fail closed with `TxBusy` under visibility contention. This should keep `cnxctl cluster status` available during runtime cutover pressure so the next official sample can expose `panda144`'s own link view instead of hiding it behind local `tx_busy`.
- Latest Capanix log-classifier cleanup: unit-tick dispatch now logs `CnxError::Timeout` as `timeout` instead of the generic `operation timed out` text so clean-log blocker classifiers do not confuse unrelated unit tick retries with fs-meta transport/source-status boundary evidence.

## Latest Local Validation

- Fresh `panda145` compatible-builder gate `fullsrc-20260618152056` passed from complete source snapshots after aibox `fullsrc-20260618150853`. It ran in local foreground exec session `28324` with 300s empty `write_stdin` continuations; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618152056.log`; remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618152056`; sentinel `COMPAT_BUILDER_PASS fullsrc-20260618152056`. It passed Capanix focused tests, full `capanix-kernel`, full `capanix-runtime`, Capanix release builds, Fustor focused admission/source/status/manual-rescan/fail-closed/query/trusted-readiness/retry tests, Fustor release builds, artifact SHA verification, `ldd`, and GLIBC verification.
- Compatible-builder `fullsrc-20260618152056` artifact hashes: `capanixd=e34bf26b218f6d29eba207d730583820707d74a49b14aa39f9d4f4382e6017e2`, `cnxctl=0c3f93833a4b0c962ac5629dcb9b941ecf5391a8b39eca84e67185a2242c2497`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=af497f65fc003ce394142795e1b80260c9b76fc21a7334ed8c297f62fa54aa71`, `fsmeta=5a0493d8c161cb4a87dd1ab3553a68fca6f5e9caad632a7b4210fa9b6550dbe5`; all five deployable ELF files require max `GLIBC_2.16`.
- Fresh complete-source aibox predeploy gate `fullsrc-20260618150853` passed from the current Fustor/Capanix worktrees after the follow-up8 readiness-gate fix. It ran in local foreground exec session `98688` with 300s empty `write_stdin` continuations; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618150853-gate.log`; root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618150853`; sentinel `AIBOX_GATE_PASS fullsrc-20260618150853`. It passed complete-snapshot Capanix full/focused tests and release builds, Fustor focused admission/source-worker/manual-rescan/status/retry/trusted-readiness tests, formatting checks, and `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay.
- Aibox `fullsrc-20260618150853` artifact hashes: `capanixd=a48b9224f110fc920db1b47b77b788261a7bbeaa9a202587601d06b7588f8d17`, `cnxctl=a98e3dca1dfde79e895b48e8077d359068a78cfd078bb8ca18bbf54a274459ca`, `capanix_worker_host=abc0386d8f855348212c0a9516d0ab6d17181b1fe84c09a682a61a9488529569`, `libfs_meta_runtime.so=013173de6d9756ab835a833b67fa80406ddd92f27abc2d184f7a14783ac13202`, `fsmeta=a57cb2f8a74ae0bbf3ecccbfef8228b1fae45013f21ed0dfb97d82809605949f`.
- Aibox `fullsrc-20260618150853` local real-NFS final `/status`: aggregate `sink.live_nodes=42`, per-root `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, no local count regression, no zero-live roots, all three groups `serving-trusted`, `materialization_readiness=ready`, `initial_audit_completed=true`, `trusted_observation_readiness=true`, one-to-one source/scan/sink schedules, and self-mount fixture paths `node-a/nfs-144`, `node-b/nfs-145`, `node-c/nfs-146`. The aggregate is intentionally below `200000000` because this is the tiny local replay seed; official high-cardinality count gates remain required.
- Latest Fustor readiness-gate validation passed after tightening the follow-up8 fix: `cargo test -p fs-meta-runtime materialized_status_cache -- --nocapture` (`5` passed, restoring fail-closed for primary rescan pending and first audit in-flight), `cargo test -p fs-meta-runtime readiness_group_observation_ -- --nocapture` (`6` passed), and `cargo test -p fs-meta-runtime trusted_observation_readiness_ -- --nocapture` (`11` passed). An attempted `status_stats_cached_readiness` filter matched zero tests and is not counted as coverage.
- Latest fustor red/green test for the official retry2 boundary passed: `open_scan_audit_admission_trigger_rescan_defers_scan_signal_until_after_reply` failed before the source worker admission-release fix because no post-reply deferred scan signal was returned, then passed after the fix and observed baseline event publication. Adjacent validation passed: `scan_audit_admission`, `source_worker_when_ready_requests_submit_intent_without_waiting_for_scan_readiness`, `status_sink_scope_ready_releases_scan_audit_admission_on_worker_backed_source_owners`, `workers::source_server::tests` (`35` passed), `cargo fmt -p fs-meta-runtime -- --check`, and `git -C /root/repo/fustor diff --check -- fs-meta/app/src/workers/source_server.rs`.
- Red/guard tests for the latest heartbeat budget boundary pass: `send_to_once_does_not_retry_active_connect_failure`, `send_to_once_reuses_tracked_inbound_peer_session_before_active_connect`, `heartbeat_send_timeout_is_bounded_by_interval_and_cap`, and `heartbeat_loop_times_out_slow_send_and_reaches_next_tick`.
- Passed focused and broad Capanix validation after the heartbeat budget fix: `cargo test -p capanix-kernel send_to_once -- --nocapture`, `cargo test -p capanix-kernel gossip::transport -- --nocapture`, `cargo test -p capanix-kernel link::manager -- --nocapture`, `cargo test -p capanix-kernel gossip -- --nocapture`, full `cargo test -p capanix-kernel -- --nocapture` (`270` unit tests and `151` specs passed), `cargo fmt -p capanix-kernel -- --check`, `git -C /root/repo/capanix diff --check`, and `git -C /root/repo/fustor diff --check -- TODO.md`.
- Capanix cluster-read red test `cargo test -p capanix-kernel cluster_reads_remain_available_during_visibility_write_contention -- --nocapture` failed before the gate change with `TxBusy`, then passed after `cluster_state_get`/`membership_summary_get` bypassed only the unnecessary visibility gate while retaining `ClusterRead` authorization.
- Passed Capanix adjacent visibility/read validation after the cluster-read gate fix: `cargo test -p capanix-kernel visibility -- --nocapture` (`9` unit tests plus `3` spec tests), `cargo test -p capanix-kernel cluster_state_get -- --nocapture`, `cargo test -p capanix-kernel heartbeat_targets -- --nocapture`, full `cargo test -p capanix-kernel` (`265` unit tests, `151` spec tests), `cargo fmt -p capanix-kernel -p capanix-runtime -- --check`, `git -C /root/repo/capanix diff --check`, and `git -C /root/repo/fustor diff --check`.
- Passed Capanix runtime log-label validation: `cargo test -p capanix-runtime unit_tick_dispatch_timeout_log_label_avoids_generic_blocker_text -- --nocapture`.
- The local Capanix diagnostic-read fix is now covered by official redeploy/read-only evidence. The latest official sample is `official-readonly-convergence-20260617184616`, which returned HTTP 200 and exposed current status-returned index count `sink.live_nodes=14688097`.
- Red test for the latest official read-only `/status` boundary failed before the fix and passed after it: `cargo test -p fs-meta-runtime management_status_reuses_release_owner_source_status_when_current_owner_probe_times_out -- --nocapture --test-threads=1`.
- Passed adjacent validation after the release-owner evidence cache fix: `status_sink_scope_ready`, `management_status_source_fanin` (`10 passed`), `management_status_sink_hint_source_owner_probe` (`2 passed`), `status_authoritative_fanin` (`15 passed`), and `source_scan_audit` (`4 passed`).
- Passed formatting/diff gates after the release-owner evidence cache fix: `cargo fmt -p fs-meta-runtime -p fustor-specs-root -- --check` and `git diff --check`.
- No new official authenticated `/status` sample was collected for this local-only fix, so no new official status-returned index count was collected.
- Fresh complete-source aibox predeploy gate `fullsrc-20260617175625` passed from the current fustor+capanix worktrees with the Capanix diagnostic-read/timeout-label fixes and release-owner source-status evidence cache fix included. It passed capanix focused diagnostic-read/visibility, heartbeat, gossip/link, runtime timeout-label, full `capanix-kernel`, capanix release build, fustor release build, focused fs-meta API fail-closed/status/manual-rescan tests including `management_status_reuses_release_owner_source_status_when_current_owner_probe_times_out`, formatting, and `CAPANIX_REAL_NFS_E2E=1` active-three replay. The local replay exposed reportable local status count `sink.live_nodes=42` and per-group `live_nodes=14`; the gate log did not include a separately named global alias.
- Fresh `panda145` compatible-builder gate `fullsrc-20260617160400` passed from complete source snapshots with the release-owner source-status evidence cache fix included. It passed focused capanix gossip/link tests, focused fs-meta status/source-scan tests including `management_status_reuses_release_owner_source_status_when_current_owner_probe_times_out`, release builds for deployable capanix and fs-meta artifacts, and SHA/ldd/GLIBC verification for all five deployable ELF files. No official authenticated `/status` sample was collected, so no new official status-returned index count was collected.
- API fail-closed audit validation passed: `cargo test -p fs-meta-runtime api_request_route_policy -- --nocapture --test-threads=1` (`4 passed`), `rescan_rejects_pending_only_sink_materialization_before_scoped_delivery`, `trusted_materialized_stats_groups`, `trusted_materialized_root_tree_session_missing_expected_groups`, `trusted_materialized_root_readiness_gap_uses_authoritative_roots_when_source_status_is_partial`, and `query_key_round_trip_works`. The only warnings were the unchanged `EndpointReceiveState::is_receive_polling` / `ManagedEndpointTask::is_receive_polling` dead-code warnings.
- Additional rescan audit probe: `cargo test -p fs-meta-runtime rescan_with_runtime_boundary_uses_runtime_delivery_without_duplicate_local_signal -- --nocapture` currently fails closed with `manual rescan current roots source-status returned only cache/degraded/non-ready evidence or route-gap evidence before scoped delivery target proof`; this old delivery-success fixture lacks the current scoped route proof and should be migrated before using it as a passing gate.
- Red test for the official 14343 boundary failed before the fix and passed after it: `cargo test -p fs-meta-runtime manual_rescan_duplicate_current_roots_uses_owner_scoped_source_status_proof -- --nocapture --test-threads=1`.
- Passed focused duplicate/current-roots suites: `manual_rescan_duplicate_current_roots_`, `rescan_current_roots_duplicate_control_returns_retryable_for_not_receive_armed_source_status`, `manual_rescan_current_roots_readiness_`, and `manual_rescan_after_preflight_`.
- Red test for the official 17034928 boundary is `manual_rescan_preflight_does_not_fast_503_when_current_roots_scoped_routes_are_ready`; it passed with the local fix.
- Passed current focused preflight guards: `cargo test -p fs-meta-runtime manual_rescan_preflight -- --nocapture --test-threads=1` (`6 passed`), `manual_rescan_current_roots_preflight`, `manual_rescan_retryable_nonready`, and `manual_rescan_schedule_only_pending_source_state_cache_is_retryable_preflight_evidence`.
- Passed latest focused duplicate/current-roots suite after the 17034928 fix: `cargo test -p fs-meta-runtime manual_rescan_duplicate_current_roots_ -- --nocapture --test-threads=1` (`2 passed`).
- Passed full focused manual-rescan suite after the 17034928 fix: `cargo test -p fs-meta-runtime manual_rescan_ -- --test-threads=1` (`214 passed`, `1 ignored`).
- Passed status fan-in focused tests, including `management_status_source_fanin_recovers_long_node_owner_scope_from_status_path`, `management_status_source_fanin_queries_owner_when_global_health_lacks_owner_scope`, `status_authoritative_fanin_accepts_live_concrete_source_owner_when_schedule_map_is_sparse`, and `management_status_source_fanin_uses_sink_owner_route_as_probe_hint_when_source_candidates_are_sparse`.
- Passed retry-classifier checks: `management_retry_includes_duplicate_roots_control_observation_readiness` and `source_repair_retry_classifier_treats_duplicate_roots_control_observation_as_transient_readiness`.
- Passed focused real-NFS active-three replay without debug fan-in logging: `/tmp/fsmeta-active3-nodebug-20260617033539.log`, `1 passed`, blocker log counts for `operation timed out`, `worker sidecar bind failed`, `statecell_read failed`, and `route activation backpressure` all `0`.
- Red test for the latest aibox `/status` timeout liveness boundary failed before the fix and passed after it: `cargo test -p fs-meta-runtime status_response_timeout_releases_internal_remote_collection_guard -- --nocapture --test-threads=1`.
- Passed adjacent status timeout tests after the fix: `cargo test -p fs-meta-runtime status_response_timeout -- --nocapture --test-threads=1` (`2 passed`) and `cargo test -p fs-meta-runtime status_returns_service_unavailable_when_internal_status_phase_never_releases -- --nocapture --test-threads=1`.
- Passed local status fan-in suites after the fix: `management_status_sink_fanin` (`14 passed`), `management_status_source_fanin` (`10 passed`), and `status_authoritative_fanin` (`15 passed`).
- Passed formatting/diff gates after the fix: `cargo fmt -p fs-meta-runtime -p fustor-specs-root -- --check` and `git diff --check`.
- Passed the previously failing active-three real-NFS replay after the fix: `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1` (`1 passed`, final status showed all three sink groups `serving-trusted` with `materialization_readiness=ready`, one-to-one `primary_host_ref_by_group`, and complete source/scan ownership).
- Superseded aibox predeploy `fullsrc-20260617051143` failed at active-three real-NFS with final `/status` missing `nfs-146` source/scan owner evidence despite sink route success; that boundary is closed by the owner-scoped source-status timeout retry fix.
- Red test for the latest aibox `nfs-146` source-owner gap is `management_status_sink_hint_source_owner_probe_retries_timeout_before_missing_owner_gap`; it passed after the management owner-scoped source-status timeout retry fix.
- Passed adjacent status fan-in suites after the latest fix: `management_status_sink_hint_source_owner_probe` (`2 passed`), `management_status_source_fanin` (`10 passed`), `management_status_sink_fanin` (`14 passed`), and `status_authoritative_fanin` (`15 passed`).
- Passed latest local real-NFS active-three replay after the timeout retry fix: `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1` (`1 passed`, final status showed all three sink groups `serving-trusted`, one-to-one sink/source/scan ownership, and trusted observation readiness).
- Passed latest formatting/diff gates after the timeout retry fix: `cargo fmt -p fs-meta-runtime -p fustor-specs-root -- --check` and `git diff --check`.
- Fresh complete-source aibox predeploy `fullsrc-20260617054503` passed after clearing stale `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-*` build artifacts that had filled `/tmp`.
- Fresh complete-source aibox predeploy `fullsrc-20260617071336` passed from the current worktree with the latest short-budget source owner fan-in boundary included in the status suite.
- Fresh `panda145` compatible-builder gate `fullsrc-20260617055614` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617055614`; remote focused tests passed and all ELF max GLIBC values were `GLIBC_2.16`.
- Compatible-builder hashes: `capanixd=297f6c6fb2e6e8ab45ec1037513e62149f861233759f684bf1f8d39c5f0f23dd`, `cnxctl=46d0c3d8ba647e5e64fbc3a8b49c2de5c4a5a67cb2a1694be16f0871b2ff399d`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=bd4b839183e0b4780aee678c5352f0f5b48f03291d4bf525ce1657666b0c11ae`, `fsmeta=fa05918f1f86a8e8c47686a8923ad7e4d2c6ccd6bda3fa9ee5fa0576a7568bcc`.
- Fresh `panda145` compatible-builder gate `fullsrc-20260617072319` passed from complete source snapshots at `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617072319`; it included the latest short-budget source owner fan-in boundary and all ELF max GLIBC values were `GLIBC_2.16`.
- Official clean redeploy `fullsrc-20260617072319` replaced all five live binaries on `panda144`, `panda145`, and `panda146`, then failed final validation with `/index/rescan` HTTP 503 from `route_receive_armed_gaps={"nfs-146": {"panda146"}}` and final `/status` HTTP 503 from `missing_source_owner` on `nfs-146`.
- Node-log sample `/tmp/fsmeta-node-logs-fullsrc-20260617072319-status-503` showed `panda146` repeatedly spawning `source-manual-rescan.panda146:v1.req` but `source-status` still reporting that route as not receive-armed, keeping the rescan readiness proof fail-closed.
- Red test for the official root-id scoped-route false negative is `node_scoped_source_rescan_route_targets_named_node_when_scope_resource_is_root_id`; it failed before the fix and passed after it.
- Worker-backed source-status regression for the same class is `worker_manual_rescan_status_uses_node_scoped_root_id_route_as_local_ready_proof`; it passes after the fix and proves root-id scoped route evidence returns same-node ready proof without worker delivery acceptance or live observability precheck.
- Passed adjacent source-status/scoped-rescan validations after the fix: `worker_manual_rescan_status_` (`4 passed`), `manual_rescan_source_status` (`21 passed`), `receive_armed` (`3 passed`), `worker_scoped_rescan` (`2 passed`), `management_status_source_fanin` (`10 passed`), and `status_authoritative_fanin` (`15 passed`).
- Passed full focused manual-rescan suite after the fix: `cargo test -p fs-meta-runtime manual_rescan_ -- --test-threads=1` (`215 passed`, `1 ignored`).
- Passed latest local active-three real-NFS replay after the fix: `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1`, sentinel `REAL_NFS_ACTIVE3_PASS`, log `/tmp/fsmeta-active3-root-id-ready-20260617084011.log`.
- Official clean redeploy `fullsrc-20260617055614-rerun1` replaced all five live binaries on `panda144`, `panda145`, and `panda146`, then failed final validation with `/index/rescan` HTTP 200 but `/status` HTTP 503: `source_coverage_gaps={"nfs-144"}`, `scan_coverage_gaps={"nfs-144"}`, `sink_owner_partition_gaps={"nfs-144": "missing_source_owner"}`.
- Node-log sample `/tmp/fsmeta-node-logs-fullsrc-20260617055614-status-503` showed `panda144` local `source-status ok` and local source/scan/sink scheduling for `nfs-144`, proving the API fan-in skipped recoverable owner-scoped source evidence.
- Red test for the official short-budget fan-in boundary is `management_status_short_budget_source_fanin_uses_grants_when_runtime_scope_is_missing`; it passed after the grant fallback fix.
- Passed adjacent local status suites after this fix: `management_status_source_fanin` (`10 passed`), `management_status_sink_hint_source_owner_probe` (`2 passed`), and `status_authoritative_fanin` (`15 passed`).
- Passed latest local active-three real-NFS replay after the grant fallback fix: `CAPANIX_REAL_NFS_E2E=1 cargo test -p fustor-specs-root --test fs_meta_api_e2e fs_meta_active_three_external_worker_replay_real_nfs -- --ignored --nocapture --test-threads=1` (`1 passed`, final status showed all three sink groups `serving-trusted`, one-to-one sink/source/scan ownership, and trusted observation readiness).
- Passed latest formatting/diff gates after the grant fallback fix: `cargo fmt -p fs-meta-runtime -p fustor-specs-root -- --check` and `git diff --check`.
- Previous complete-source aibox pass `fullsrc-20260617033918`, compatible-builder pass `fullsrc-20260617034928`, and official attempt `fullsrc-20260617034928` are stale failure-context evidence because the worktree changed after them.
- Known unchanged warning: `EndpointReceiveState::is_receive_polling` / `ManagedEndpointTask::is_receive_polling` dead-code warnings.

## Next Validation Order

1. Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup7` passed the count, sink readiness, and top-level readiness acceptance items, but exposed the remaining source concrete-root `last_audit_completed_at_us=null` timestamp boundary during periodic next-audit in-progress.
2. Current local source timestamp fix has passed focused local validation, fresh complete-source aibox predeploy gate `fullsrc-20260618180955`, `panda145` compatible-builder gate `fullsrc-20260618180955`, official clean-log redeploy `fullsrc-20260618180955-official-20260618183549-clean1`, and count-bearing followup1 above `200000000`.
3. Next gate is read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup5` using the fixed fail-closed summary analyzer.
4. For every official sample, record and report `sink.live_nodes`, per-root live-node counts, delta/rate, missing per-root/per-peer data, regressions, growth below `10000/s`, below-`200000000`, source concrete root audit/publication state, sink group readiness, and top-level readiness. Do not call `/index/rescan` for readiness/count/pending-materialization symptoms without fresh stale or incorrect source/scan repair evidence and explicit authorization in this trace.

## Active Wait

- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup5` is the active planned foreground wait. Reason: use the fixed fail-closed analyzer after another patient `900s` read-only convergence window, then collect one authenticated `/status` sample against followup4 as the count baseline. Command shape: `OFFICIAL_READONLY_ATTEMPT=official-readonly-status-fullsrc-20260618180955-clean1-followup5 .tmp/run-official-readonly-status-current.sh`. Session id is pending until `exec_command` yields. Log path: `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup5.log`. Expected terminal sentinel: `OFFICIAL_READONLY_STATUS_PASS attempt=official-readonly-status-fullsrc-20260618180955-clean1-followup5` or `OFFICIAL_READONLY_STATUS_FAIL attempt=official-readonly-status-fullsrc-20260618180955-clean1-followup5`. `write_stdin` wait budget after any yield: up to `8` empty continuations, each exactly `yield_time_ms=300000`; no side probes between continuations.

## Completed Wait

- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup4` ran in local foreground exec session `69657` and completed with `/status` HTTP 200; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup4.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618180955-clean1-followup4`; summary `.../status/status-count-summary.json`. It exposed reportable status count `sink.live_nodes=491617346`, per-root `nfs-144=164571258`, `nfs-145=168050468`, `nfs-146=158995620`, delta `+71936354`, rate `74959.05/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and count above `200000000`; sink groups and source completed timestamps remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup3` ran in local foreground exec session `46028` and completed with `/status` HTTP 200; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup3.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618180955-clean1-followup3`; summary `.../status/status-count-summary.json`. It exposed reportable status count `sink.live_nodes=419680992`, per-root `nfs-144=141840519`, `nfs-145=143043653`, `nfs-146=134796820`, delta `+161345363`, rate `77929.67/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and count above `200000000`; sink groups and source completed timestamps remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup2` ran in local foreground exec session `53167` and completed collection with `/status` HTTP 503; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup2.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618180955-clean1-followup2`; summary `.../status/status-count-summary.json`. It exposed no reportable status count; fail-closed gaps were `nfs-144` sink schedule/owner/transport fan-in while cluster/config remained healthy. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618180955-clean1-followup1` ran in local foreground exec session `38725` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618180955-clean1-followup1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618180955-clean1-followup1`; summary `.../status/status-count-summary.json`. It exposed reportable status count `sink.live_nodes=258335629`, per-root `nfs-144=89741352`, `nfs-145=86776874`, `nfs-146=81817403`, delta `+154198943`, rate `149831.09/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and count above `200000000`; direct status inspection still showed sink groups pending, top-level trusted readiness false, source completed timestamps null, and an `nfs-146` publication gap, so final acceptance remains open. No `/index/rescan` was called.
- Official clean-log redeploy `fullsrc-20260618180955-official-20260618183549-clean1` ran in local foreground exec session `98312` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618180955-official-20260618183549-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618180955-official-20260618183549-clean1-clean-redeploy`; post-wait sample `.../post-deploy-status/status-count-summary.json`. It exposed reportable status count `sink.live_nodes=104136686`, per-root `nfs-144=39234081`, `nfs-145=21127131`, `nfs-146=43775474`, no missing per-root/per-peer live-node data, no zero-live roots, one-to-one sink/source/scan ownership, source concrete roots active/serving with no error/rescan/overflow, all sink groups still `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, source `last_audit_completed_at_us=null`, and top-level `trusted_observation_readiness=false`. No `/index/rescan` was called.
- `panda145` compatible-builder gate `fullsrc-20260618180955` ran in local foreground exec session `86653` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618180955.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618180955`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`; hashes are `capanixd=227b632cf0a0d6f98916a62b4d7535f9d3476733d8174f8db582a69b8a16e9c6`, `cnxctl=602d5cf911e5b6a9d2e7e2f019236017c0b25e4338186f7704dc75155739b3ce`, `capanix_worker_host=bb85370ad6ec5ebcee3f9a4ea53911656d1c288617839fe7bdce8cd7717decdd`, `libfs_meta_runtime.so=1fdc15a66c61bc20669b888f4650f5c80955b89cdb0a6fa3707df3d69f70f900`, `fsmeta=b171293fe402184cdb1d9d5c5f171d1a0f3f6bd69378a692760077c62c451b12`.
- Complete-source aibox predeploy gate `fullsrc-20260618180955` ran in local foreground exec session `67851` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618180955-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618180955`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed local count `sink.live_nodes=42`, per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, `trusted_observation_readiness=true`, all sink groups `serving-trusted` / `ready` / `initial_audit_completed=true`, and all three source concrete roots with completed audit timestamps.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup7` ran in local foreground exec session `64394` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup7.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup7`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=603000837`, per-root `nfs-144=201000279`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+20640487`, rate `20697.86/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, no count summary anomalies, all sink groups `serving-trusted` / `ready` / `initial_audit_completed=true`, and top-level `trusted_observation_readiness=true`. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and settled publication evidence, but `last_audit_completed_at_us=null` because the next periodic audit was in progress. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup6` ran in local foreground exec session `41453` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup6.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup6`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=582360350`, per-root `nfs-144=199419732`, `nfs-145=196194633`, `nfs-146=186745985`, delta `+73928000`, rate `72647.07/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no count summary anomalies. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup5` ran in local foreground exec session `79424` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup5.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup5`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=508432350`, per-root `nfs-144=181194864`, `nfs-145=167520345`, `nfs-146=159717141`, delta `+78331120`, rate `78905.54/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no count summary anomalies. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup4` ran in local foreground exec session `50484` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup4.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup4`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=430101230`, per-root `nfs-144=156557726`, `nfs-145=140123185`, `nfs-146=133420319`, delta `+74695056`, rate `74800.34/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no count summary anomalies. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup3` ran in local foreground exec session `14962` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup3.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup3`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=355406174`, per-root `nfs-144=131480897`, `nfs-145=115064136`, `nfs-146=108861141`, delta `+79804205`, rate `79972.56/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no count summary anomalies. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup2` ran in local foreground exec session `37778` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup2.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup2`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=275601969`, per-root `nfs-144=106656788`, `nfs-145=87156076`, `nfs-146=81789105`, delta `+89667001`, rate `88608.90/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no count summary anomalies. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618152056-clean1-followup1` ran in local foreground exec session `87987` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618152056-clean1-followup1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618152056-clean1-followup1`; summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=185934968`, per-root `nfs-144=78562470`, `nfs-145=56756958`, `nfs-146=50615540`, delta `+124987086`, rate `110565.85/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and only summary anomaly `below_200000000`. Source roots were active/serving with `last_error=null`, `rescan_pending=false`, and publication/forwarding evidence; sink groups remained pending. No `/index/rescan` was called.
- Official clean-log redeploy `fullsrc-20260618152056-official-20260618153719-clean1` ran in local foreground exec session `24632` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618152056-official-20260618153719-clean1.log`; state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618152056-official-20260618153719-clean1-clean-redeploy`; post-wait sample `.../post-deploy-status/status-count-summary.json`. It exposed reportable status count `sink.live_nodes=60947882`, per-root `nfs-144=22924310`, `nfs-145=21798478`, `nfs-146=16225094`, no missing per-root/per-peer live-node data, one-to-one sink/source/scan ownership, source concrete roots active/serving with publication evidence and no rescan/error/overflow, all sink groups still `serving-degraded` / `pending-materialization` / `initial_audit_completed=false`, and top-level `trusted_observation_readiness=false`. No `/index/rescan` was called.
- Complete-source aibox predeploy gate `fullsrc-20260618114234` ran in local foreground exec session `82022` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618114234-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618114234`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`.
- Complete-source aibox predeploy gate `fullsrc-20260618150853` ran in local foreground exec session `98688` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618150853-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618150853`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`, with `trusted_observation_readiness=true`.
- `panda145` compatible-builder gate `fullsrc-20260618152056` ran in local foreground exec session `28324` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618152056.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618152056`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`.
- `panda145` compatible-builder gate `fullsrc-20260618114234` ran in local foreground exec session `34565` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618114234.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618114234`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`.
- Official clean-log redeploy `fullsrc-20260618114234-official-2026061812-clean1` ran in local foreground exec session `70160` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618114234-official-2026061812-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618114234-official-2026061812-clean1-clean-redeploy`, post-wait sample `.../post-deploy-status`. It verified all three self-NFS mappings, replaced all five artifacts with compatible-builder `fullsrc-20260618114234` hashes, started each node through its node-owned script, restored resources/app/roots, waited locally `900s`, and collected one authenticated `/status` sample. The sample returned HTTP 200 with reportable `sink.live_nodes=83344388`, per-root `nfs-144=18922944`, `nfs-145=30788332`, `nfs-146=33633112`, delta `+39277493`, rate `9465.22/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup1` ran in local foreground exec session `20970` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup1`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=199228660`, per-root `nfs-144=56115771`, `nfs-145=75146721`, `nfs-146=67966168`, delta `+115884272`, rate `95287.16/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and only anomaly `below_200000000`. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup2` ran in local foreground exec session `82266` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup2.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup2`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=271038214`, per-root `nfs-144=80314302`, `nfs-145=99225329`, `nfs-146=91498583`, delta `+71809554`, rate `71774.11/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup3` ran in local foreground exec session `15076` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup3.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup3`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=341164354`, per-root `nfs-144=103608290`, `nfs-145=122342890`, `nfs-146=115213174`, delta `+70126140`, rate `70640.95/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup4` ran in local foreground exec session `45637` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup4.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup4`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=409477368`, per-root `nfs-144=125209243`, `nfs-145=146241982`, `nfs-146=138026143`, delta `+68313014`, rate `69353.40/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup5` ran in local foreground exec session `86931` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup5.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup5`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=479514533`, per-root `nfs-144=147782597`, `nfs-145=170171288`, `nfs-146=161560648`, delta `+70037165`, rate `71330.28/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup6` ran in local foreground exec session `52491` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup6.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup6`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=549231372`, per-root `nfs-144=170272899`, `nfs-145=194091002`, `nfs-146=184867471`, delta `+69716839`, rate `70787.12/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. Source roots were serving with `last_error=null`, `rescan_pending=false`, and emitted/forwarded gaps closed, but source `last_audit_completed_at_us` and sink trusted readiness were still absent. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup7` ran in local foreground exec session `43979` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup7.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup7`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=594718841`, per-root `nfs-144=192718283`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+45487469`, rate `46344.99/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and no summary anomalies. `nfs-145` and `nfs-146` reached `serving-trusted` / `ready` / `initial_audit_completed=true`; `nfs-144` remained pending at `192718283`. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618114234-clean1-followup8` ran in local foreground exec session `79025` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618114234-clean1-followup8.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618114234-clean1-followup8`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=603000837`, per-root `nfs-144=201000279`, `nfs-145=201000279`, `nfs-146=201000279`, delta `+8281996`, rate `8383.28/s`, no missing per-root live-node data, no count regression, no zero-live roots, and summary anomaly `growth_below_10000_per_second`; this low final delta is completion/stability evidence because all three groups are now `serving-trusted` / `ready` / `initial_audit_completed=true`. Top-level `readiness_planes.trusted_observation_readiness=false` remains the current Fustor readiness-gate boundary. No `/index/rescan` was called.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618102146-clean1-followup1` ran in local foreground exec session `42927` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618102146-clean1-followup1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618102146-clean1-followup1`, summary `.../status/status-count-summary.json`. The authenticated `/status` sample returned HTTP 200 with unchanged reportable `sink.live_nodes=44066895`, per-root `nfs-144=14689051`, `nfs-145=14689620`, `nfs-146=14688224`, delta `0` over `1147.05s`, rate `0/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`. Source audit evidence remained stalled on all roots: `participation_state=joining`, `last_error=backpressure`, `last_audit_started_at_us` set, `last_audit_completed_at_us=null`, emitted counts ahead of forwarded counts, and `rescan_pending=false`. No `/index/rescan` was called.
- Official clean-log redeploy `fullsrc-20260618102146-official-2026061810-clean1` ran in local foreground exec session `83985` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618102146-official-2026061810-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618102146-official-2026061810-clean1-clean-redeploy`, post-wait sample `.../post-deploy-status`. It verified self-NFS mappings for `panda144`, `panda145`, and `panda146`, replaced all five artifacts with compatible-builder `fullsrc-20260618102146` hashes, started each node through its node-owned script, restored resources/app/roots, waited locally `900s`, and collected one authenticated `/status` sample. The sample returned HTTP 200 with reportable `sink.live_nodes=44066895`, per-root `nfs-144=14689051`, `nfs-145=14689620`, `nfs-146=14688224`, delta `+2048` over `4234.64s`, rate `0.48/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`. No `/index/rescan` was called.
- `panda145` compatible-builder gate `fullsrc-20260618102146` ran in local foreground exec session `77026` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618102146.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618102146`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`.
- Complete-source aibox predeploy gate `fullsrc-20260618100913` ran in local foreground exec session `42390` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618100913-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618100913`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`.
- Official read-only wait `official-readonly-status-fullsrc-20260618071209-clean1-followup1` ran in local foreground exec session `70043` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618071209-clean1-followup1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618071209-clean1-followup1`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=44072544`, per-root `nfs-144=14696104`, `nfs-145=14688315`, `nfs-146=14688125`, delta `0`, rate `0/s`, no missing per-root/per-peer live-node data, no count regression, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`.
- Official read-only follow-up `official-readonly-status-fullsrc-20260618084203-clean1-followup1` ran in local foreground exec session `39737` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-status-fullsrc-20260618084203-clean1-followup1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-fullsrc-20260618084203-clean1-followup1`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=44064847`, per-root `nfs-144=14688178`, `nfs-145=14688335`, `nfs-146=14688334`, delta `0`, rate `0/s`, no missing per-root/per-peer live-node data, no zero-live roots, and anomalies `below_200000000` plus `growth_below_10000_per_second`.
- Official clean-log redeploy `fullsrc-20260618084203-official-2026061809-clean1` ran in local foreground exec session `34173` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618084203-official-2026061809-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618084203-official-2026061809-clean1-clean-redeploy`, post-wait sample `.../post-deploy-status`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=44064847`, per-root `nfs-144=14688178`, `nfs-145=14688335`, `nfs-146=14688334`, delta `-7697`, rate `-1.71/s`, no missing per-root/per-peer live-node data, no zero-live roots, and anomalies `count_regression`, `growth_below_10000_per_second`, and `below_200000000`.
- `panda145` compatible-builder gate `fullsrc-20260618084203` ran in local foreground exec session `15490` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618084203.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618084203`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`.
- Complete-source aibox predeploy gate `fullsrc-20260618084203` ran in local foreground exec session `19868` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618084203-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618084203`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`.
- `panda145` compatible-builder gate `fullsrc-20260618081922` ran in local foreground exec session `71303` and completed with `COMPAT_BUILDER_FAIL`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618081922.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618081922`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. First raw boundary is Capanix test-budget robustness in `commit_config_submission_does_not_wait_on_drained_predecessor_before_activation_fail_closed`, now locally fixed.
- Complete-source aibox predeploy gate `fullsrc-20260618081922` ran in local foreground exec session `71357` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618081922-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260618081922`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`.
- Official clean-log redeploy `fullsrc-20260618044930-official-2026061805-clean1` ran in local foreground exec session `26933` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260618044930-official-2026061805-clean1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260618044930-official-2026061805-clean1-clean-redeploy`. The authenticated `/status` sample returned HTTP 200 with reportable `sink.live_nodes=0`, delta `-14688792`, rate `-16320.88/s`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`, no missing per-root fields, and anomalies `count_regression`, `growth_below_10000_per_second`, `zero_live_roots:nfs-144,nfs-145,nfs-146`, `below_200000000`.
- Corrected artifact verification retry for `fullsrc-20260618044930` completed with `COMPAT_BUILDER_ARTIFACT_VERIFY_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618044930-artifact-verify-retry.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618044930`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. Artifact max GLIBC evidence is `GLIBC_2.16` for all five deployable files.
- `panda145` compatible-builder gate `fullsrc-20260618044930` ran in local foreground exec session `3129` and completed with `COMPAT_BUILDER_FAIL`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260618044930.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260618044930`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. First raw boundary is wrapper compatibility: panda145 Python 3.6 rejected `subprocess.check_output(..., text=True)` during artifact verification after tests/builds had already passed.
- Official clean-log redeploy `fullsrc-20260617221254-official-20260617223721-retry2` ran in foreground exec session `85152` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617221254-official-20260617223721-retry2.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617221254-official-20260617223721-retry2-clean-redeploy`. The authenticated `/status` sample returned HTTP 200 with one-to-one topology and reportable `sink.live_nodes=0`, delta `0`, rate `0/s`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`. This sample exposed the current fustor worker admission-release/source-audit-not-started boundary.
- `panda145` compatible-builder gate `fullsrc-20260617221254` ran in local foreground exec session `25774` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260617221254.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617221254`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected. All five deployable artifacts require at most `GLIBC_2.16`.
- Complete-source aibox predeploy gate `fullsrc-20260617221254` ran in local foreground exec session `49635` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617221254-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617221254`. It did not collect a new official authenticated `/status` sample, so no new official final-acceptance count was exposed. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `nfs-144=14`, `nfs-145=14`, `nfs-146=14`.
- Complete-source aibox predeploy gate `fullsrc-20260617215332` ran in local foreground exec session `92974` and completed with `AIBOX_GATE_FAIL`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617215332-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617215332`. It passed focused Capanix heartbeat/link tests, full `capanix-kernel`, Capanix release builds, fustor release builds, and focused fs-meta API/status/manual-rescan fail-closed suites, but failed `CAPANIX_REAL_NFS_E2E=1` active-three real-NFS replay. First raw failure: local facade login did not recover within 90s after `/index/rescan` request id 5 began but never completed; node-a/node-c logs showed repeated 2s gossip heartbeat send timeouts to node-b. This is local replay evidence that heartbeat timeout must be non-mutating with respect to peer pools; it is not authorization to call official `/index/rescan`.
- Complete-source aibox predeploy gate `fullsrc-20260617205449` ran in local foreground exec session `46161` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617205449-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617205449`. It did not collect a new official authenticated `/status` sample, so no new official final-acceptance count was exposed. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `live_nodes=14`.
- `panda145` compatible-builder gate `fullsrc-20260617205449` ran in local foreground exec session `33648` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260617205449.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617205449`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected.
- Official clean-log redeploy `fullsrc-20260617205449-official-20260617212156` ran in local foreground exec session `83386` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617205449-official-20260617212156.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617205449-official-20260617212156-clean-redeploy`. The authenticated post-wait `/status` sample returned HTTP 200 and exposed `sink.live_nodes=0`, delta `-58754377`, rate `-65282.64/s`, per-root `nfs-144=0`, `nfs-145=0`, `nfs-146=0`.
- Complete-source aibox predeploy gate `fullsrc-20260617131850` ran in local foreground exec session `46791` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617131850-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617131850`. It did not collect a new official authenticated `/status` sample, so no new official final-acceptance count was exposed. The local real-NFS replay final status exposed reportable local count `sink.live_nodes=42` and per-group `live_nodes=14`.
- Complete-source aibox predeploy gate `fullsrc-20260617154953` ran in local foreground exec session `84159` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617154953-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617154953`. It did not collect a new official authenticated `/status` sample, so no new official final-acceptance count was exposed. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `live_nodes=14`.
- Complete-source aibox predeploy gate `fullsrc-20260617175625` ran in local foreground exec session `75018` and completed with `AIBOX_GATE_PASS`; log `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617175625-gate.log`, root `/tmp/aibox-fsmeta-build/panda145/current/fullsrc-20260617175625`. It did not collect a new official authenticated `/status` sample, so no new official final-acceptance count was exposed. The local active-three replay final status exposed reportable local count `sink.live_nodes=42` and per-group `live_nodes=14`.
- `panda145` compatible-builder gate `fullsrc-20260617175625` ran in local foreground exec session `51203` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260617175625.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617175625`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected.
- Official clean-log redeploy `fullsrc-20260617175625-official-20260617182416` ran in local foreground exec session `59726` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617175625-official-20260617182416.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617175625-official-20260617182416-clean-redeploy`, post-wait sample `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617175625-official-20260617182416-clean-redeploy/post-deploy-status`. The authenticated post-wait `/status` sample returned HTTP 200 with one-to-one topology but remained `pending-materialization`; current status-returned index count was `sink.live_nodes=0`.
- Official read-only convergence wait `official-readonly-convergence-20260617184616` ran in local foreground exec session `52514` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-convergence-official-readonly-convergence-20260617184616.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-convergence-20260617184616`. The authenticated `/status` sample returned HTTP 200 with one-to-one topology and `pending-materialization`; current status-returned index count is `sink.live_nodes=14688097`, with per-group `nfs-144=0`, `nfs-145=14688097`, and `nfs-146=0`.
- Official read-only convergence wait `official-readonly-convergence-20260617191218` ran in local foreground exec session `65649` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-convergence-official-readonly-convergence-20260617191218.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-convergence-20260617191218`. The authenticated `/status` sample exposed `sink.live_nodes=14688097`, unchanged over 900s (`0/s`), with per-root `nfs-144=0`, `nfs-145=14688097`, `nfs-146=0`.
- Official post-tuning read-only sample `official-readonly-post-scan-tuning-20260617193507` completed after updating and restarting the actual `panda144`/`panda146` local `node.env` files. It still exposed `sink.live_nodes=14688097`, rate `0/s`, per-root `nfs-144=0`, `nfs-145=14688097`, `nfs-146=0`, so the tuning mismatch was closed as a real run-config defect but not as the final root cause.
- Official roots-apply recovery sample `official-roots-apply-after-peer-restart-20260617200700` completed after restarting dead `panda144`/`panda146` daemons and restoring active roots. It returned authenticated `/status` HTTP 200 with reportable `sink.live_nodes=14688097`, delta `0`, rate `0/s`, and per-root `nfs-144=0`, `nfs-145=14688097`, `nfs-146=0`; cluster status showed all three nodes reachable/cluster-ready with `active_processes=1`.
- Official post-recovery read-only wait `official-post-recovery-readonly-20260617201107` ran in foreground exec session `19556` and completed with `OFFICIAL_POST_RECOVERY_STATUS_PASS`; log `/tmp/fsmeta-official-post-recovery-readonly-official-post-recovery-readonly-20260617201107.log`. The authenticated `/status` sample returned HTTP 200 and exposed `sink.live_nodes=58754377`, delta `44066280`, rate `48962.53/s`, per-root `nfs-144=14689714`, `nfs-145=29368874`, `nfs-146=14695789`.
- Official post-recovery read-only wait `official-post-recovery-readonly2-20260617202740` ran in foreground exec session `30972` and completed with `OFFICIAL_POST_RECOVERY_STATUS2_PASS`; log `/tmp/fsmeta-official-post-recovery-readonly2-official-post-recovery-readonly2-20260617202740.log`. The authenticated `/status` sample returned HTTP 200 but exposed `sink.live_nodes=58754377`, delta `0`, rate `0/s`, per-root `nfs-144=14689714`, `nfs-145=29368874`, `nfs-146=14695789`.
- `panda145` compatible-builder gate `fullsrc-20260617160400` ran in local foreground exec session `6149` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260617160400.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617160400`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected.
- Official clean-log redeploy `fullsrc-20260617160400-official-20260617162145-retry1` ran in local foreground exec session `27858` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617160400-official-20260617162145-retry1.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617160400-official-20260617162145-retry1-clean-redeploy`, post-wait sample `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617160400-official-20260617162145-retry1-clean-redeploy/post-deploy-status`. The authenticated post-wait `/status` sample returned HTTP 200 with one-to-one topology but remained `pending-materialization`; index summary recorded neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that historical response.
- Official read-only convergence wait `official-readonly-convergence-20260617164627` ran in local foreground exec session `93920` and completed with `OFFICIAL_READONLY_STATUS_PASS`; log `/tmp/fsmeta-official-readonly-convergence-20260617164627.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-convergence-20260617164627`. The authenticated `/status` sample returned HTTP 503 with only `sink_owner_transport_gaps={"nfs-144"}` among the authoritative fan-in gaps; index summary recorded neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that 503 response.
- Operator-script retry note: initial official redeploy attempt `fullsrc-20260617160400-official-20260617161931` failed before replacement/status sampling because the stale-process check matched its own `bash -s` command line; no authenticated `/status` sample was collected and no index count was exposed. Retry `fullsrc-20260617160400-official-20260617162145-retry1` fixed the check to match only `$run_root/bin/{capanixd,capanix_worker_host}` and passed.
- `panda145` compatible-builder gate `fullsrc-20260617133101` ran in local foreground exec session `9519` and completed with `COMPAT_BUILDER_PASS`; log `/tmp/fsmeta-compatible-builder-fullsrc-20260617133101.log`, remote root `/home/wanghuajin/fsmeta-build/current/fullsrc-20260617133101`. It did not collect a new official authenticated `/status` sample, so no new official status-returned index count was collected.
- Official clean-log redeploy `fullsrc-20260617133101-official-20260617134241` ran in local foreground exec session `79817` and completed with `OFFICIAL_REDEPLOY_PASS`; log `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617133101-official-20260617134241.log`, state `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617133101-official-20260617134241-clean-redeploy`. The authenticated post-wait `/status` sample returned HTTP 200 but remained `pending-materialization`; index summary had no separately named global alias and extracted 49 count fields. Under the current rule, any exposed `sink.live_nodes` in that evidence is the reportable pending-state count; the evidence is superseded by the current `sink.live_nodes=14688097` sample.
- Official read-only `/status` observation `official-readonly-status-20260617150325` ran in local foreground exec session `71622`; log `/tmp/fsmeta-official-readonly-status-20260617150325.log`; first wrapper evidence `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-20260617150325` failed before status due to an operator token-parse script bug after login HTTP 200, so it exposed no status-returned index count. Corrected post-wait sample evidence `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/official-readonly-status-20260617150325-retry1` returned authenticated `/status` HTTP 503 with `nfs-146` source/scan coverage missing and no reportable `sink.live_nodes` count. Bounded node logs are `/tmp/fsmeta-node-logs-official-readonly-status-20260617150325-retry1`.
- Operator correction: an extra authenticated `POST /index/rescan` was triggered as `fullsrc-20260617133101-official-20260617134241-rescan-20260617140635` and returned HTTP 200, but the follow-up wait/status sample was cancelled because `pending-materialization` alone is normal under high-NFS convergence and is not sufficient reason to submit manual-rescan repair work. The local foreground session `35880` was terminated before the planned post-rescan `/status` sample; no new status-returned index count was collected from that aborted window.
- Self-NFS post-rescan repair status wait `selfnfs-postrescan-status-20260617123331` slept 900s and then collected one authenticated `/status` sample through `panda145`; it returned HTTP 503 with facade route timeouts and local fallback gaps for `nfs-144` and `nfs-146`. Index count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-postrescan-status-20260617123331/index-entry-summary.json` recorded neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that 503 response.
- Self-NFS post-restore status wait `selfnfs-postrestore-status-20260617121428` slept 900s and then collected one authenticated `/status` sample through `panda145`; it returned HTTP 200 with one-to-one source/scan/sink ownership but all three sink groups still `pending-materialization`. Index count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-postrestore-status-20260617121428/index-entry-summary.json` had no separately named global alias but did expose reportable pending-state count `sink.live_nodes=0` with per-group `live_nodes=0`.
- Self-NFS app restore from empty config completed at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-redeploy-from-empty-20260617120456`: deploy succeeded, then after a 180s local wait roots preview/apply succeeded and config dump showed app scopes for `nfs-144`, `nfs-145`, `nfs-146`, and `fs-meta-tcp-listener`.
- Self-NFS post-correction status wait `selfnfs-status-20260617112344` slept 900s and then collected one authenticated `/status` sample through `panda145`; it returned HTTP 503 with `sink_owner_partition_gaps={"nfs-146": "primary_host_ref=panda146 source_owner=panda144"}` and `sink_owner_transport_gaps={"nfs-145"}`. Index count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/selfnfs-status-20260617112344/index-entry-summary.json` recorded neither an explicitly named global alias nor `sink.live_nodes` candidates, so no reportable index count was available from that 503 response.
- Official clean redeploy `fullsrc-20260617095202` failed before restore validation because the operator script assumed `CAPANIX_SOCKET` was exported by `admin.env`; all five artifacts had already been replaced and all three node-owned daemons were live with correct hashes. This is an operator-script failure, not product evidence.
- Official clean redeploy `fullsrc-20260617095202-rerun1` failed in the restore wrapper because an unquoted local heredoc expanded the Python shadow-file `${password}` expression before remote execution. This is an operator-script failure, not product evidence.
- Official clean redeploy `fullsrc-20260617095202-rerun2` failed in the initial post-wait sample wrapper because remote Python 3.6 does not support `subprocess.run(text=...)`; restore had completed first (`resource-announce`, `fsmeta deploy`, roots preview/apply, config dump all OK). A bounded follow-up authenticated `/status` sample returned HTTP 200 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617095202-rerun2-clean-redeploy/status-index-sample.json`; index count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617095202-rerun2-clean-redeploy/index-entry-sample-summary.json` had no separately named global alias but exposed reportable pending-state count `sink.live_nodes=0` and per-group `live_nodes=0`.
- Follow-up `/index/rescan` for `fullsrc-20260617095202-rerun2` returned HTTP 200, then `/status` returned HTTP 503 at `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617095202-rerun2-clean-redeploy/status-after-rescan-followup.json`. Index count summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617095202-rerun2-clean-redeploy/index-entry-after-rescan-followup-summary.json` found neither an explicitly named global alias nor `sink.live_nodes` / `index/live/count/entries` candidates in the 503 response.
- Post-rescan final sample for `fullsrc-20260617095202-rerun2` returned `/status` HTTP 503 after 300s; summary `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/fullsrc-20260617095202-rerun2-clean-redeploy/index-entry-final-followup-summary.json` found neither an explicitly named global alias nor `sink.live_nodes` candidates. Final error: `source_coverage_gaps={"nfs-146"}`, `scan_coverage_gaps={"nfs-146"}`, `sink_owner_partition_gaps={"nfs-146": "missing_source_owner"}`, `sink_owner_transport_gaps={}`.

- Post-redeploy convergence wait for `fullsrc-20260616205604`: start `2026-06-16 21:28:40 CST`, deadline `2026-06-16 21:43:40 CST`, local command session `10369`, sample directory `/home/wanghuajin/fsmeta-stable/run/.fsmeta-state/validation/20260616205604-official-redeploy/post-deploy-wait-20260616212840`; completed with login/status HTTP 200 and stalled sink materialization evidence above.
- Post-redeploy convergence wait for `fullsrc-20260616223828`: `/tmp/fsmeta-official-redeploy-fullsrc-20260616223828-deploy-wait2.log`; completed with resource announce, fs-meta deploy, roots apply, login HTTP 200, `/status` HTTP 200, and retryable `/index/rescan` HTTP 503.
- Full cold-restart convergence wait for `fullsrc-20260616223828`: `/tmp/fsmeta-official-cold-restart-deploy-fullsrc-20260616223828.log`; completed with three reachable/gossip-converged cluster-ready nodes, login/status HTTP 200, and `/index/rescan` HTTP 503.
- Official clean redeploy wait for `fullsrc-20260617004202`: `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617004202-resume2.log`; completed with three reachable/gossip-converged cluster-ready nodes, `/status` before rescan HTTP 200, `/index/rescan` HTTP 503 duplicate roots-control replay failure, and post-rescan `/status` timeout.
- Official clean redeploy wait for `fullsrc-20260617014343`: `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617014343-resume1.log`; completed with official starts, resource/app/roots restore, cluster-ready sample, login HTTP 200, `/index/rescan` HTTP 503 duplicate replay failure, and final `/status` HTTP 503. This is failure evidence for the current local fix, not a passing deploy.
- Official clean redeploy wait for `fullsrc-20260617034928`: `/tmp/fsmeta-official-clean-redeploy-fullsrc-20260617034928.log`; completed with official starts, resource/app/roots restore, cluster-ready sample, login HTTP 200, initial `/status` HTTP 200, `/index/rescan` HTTP 503 from the retryable-nonready fast-503 bug, and final `/status` HTTP 503 with no final index count.

## Final Acceptance

- `cnxctl cluster status` shows exactly `panda144`, `panda145`, and `panda146` active and cluster-ready.
- `cnxctl config dump` shows `fs-meta.__cnx_runtime.app_scopes` includes exactly the three active NFS roots plus legitimate bootstrap/listener scopes.
- Authenticated `/api/fs-meta/v1/status` returns HTTP 200 only for complete evidence, or otherwise carries explicit degraded/partial/error evidence.
- `/status` shows one-to-one sink/source/scan scheduled groups and one-to-one `sink.primary_host_ref_by_group`.
- Source concrete roots for all three groups have current owner evidence and completed current-audit timestamps.
- Sink groups for all three roots leave `pending-materialization` and become `serving-trusted` with `initial_audit_completed=true`, `materialization_readiness=ready`, and `trusted_observation_readiness=true`.
- Final complete/non-partial authenticated `/api/fs-meta/v1/status` evidence must show the status-returned live-node index entry count is stable/increasing and exceeds `200,000,000` (`200000000`), using an explicit global alias if present or aggregate `sink.live_nodes` otherwise.

## Superseded

- Older official/aibox/builder iteration details have been removed from the active trace. Reopen them only with fresh raw evidence that reproduces the same boundary.
