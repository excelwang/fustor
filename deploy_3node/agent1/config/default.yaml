sources:
  nfs-source:
    driver: fs
    uri: "/data/fustor-benchmark-run/data"

senders:
  fusion-main:
    driver: fusion
    uri: "http://fusion-node:28888"
    credential:
      key: "fs-sync-secret-key"

pipes:
  sync-job:
    source: nfs-source
    sender: fusion-main
