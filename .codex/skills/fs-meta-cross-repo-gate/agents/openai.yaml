interface:
  display_name: "Fs-meta Cross-Repo Gate"
  short_description: "Shared blocker-state, 300s waits, node logs"
  default_prompt: "Use $fs-meta-cross-repo-gate to run the current fustor-capanix blocker workflow through one shared blocker-state document, direct bounded iterations, exec_command plus 300s empty write_stdin long waits, bounded official per-node log evidence, a production-parity aibox predeploy gate, and the safe clean-log binary build/deploy gate when changing cluster daemons or app runtimes."
