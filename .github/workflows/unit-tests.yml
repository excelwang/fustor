# .github/workflows/unit-tests.yml
# Unit test workflow for Fustor packages

name: Unit Tests

on:
  push:
    branches: [main, develop, 'refactor/*', 'feature/*']
    paths:
      - 'agent/**'
      - 'fusion/**'
      - 'benchmark/**'
      - 'packages/**'
      - '.github/workflows/unit-tests.yml'
      - 'pyproject.toml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'agent/**'
      - 'fusion/**'
      - 'benchmark/**'
      - 'packages/**'
      - 'pyproject.toml'

env:
  PYTHON_VERSION: '3.11'

jobs:
  test:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.5.14"
          cache: true

      - name: Set up Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-packages

      - name: Run unit tests
        run: |
          uv run pytest --ignore=it/ -v --tb=short --junit-xml=test-results.xml

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: test-results.xml

  lint:
    name: Code Quality
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "0.5.14"
          cache: true

      - name: Set up Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-packages

      - name: Run ruff check
        run: |
          uv run ruff check .

      - name: Run ruff format check
        run: |
          uv run ruff format --check .

      - name: Run mypy type check
        run: |
          # Check core, SDKs and V2 drivers
          uv run mypy packages/core/src packages/agent-sdk/src packages/fusion-sdk/src \
                      packages/source-fs/src packages/view-fs/src fusion/src agent/src
