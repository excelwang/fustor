# Example: datastores-config.yaml
# Location: $FUSTOR_HOME/datastores-config.yaml

datastores:
  research-data:
    session_timeout_seconds: 30
    allow_concurrent_push: true
    api_key: fk_example_api_key_replace_me
  
  archive-data:
    session_timeout_seconds: 60
    allow_concurrent_push: false
    api_key: fk_archive_api_key_replace_me
