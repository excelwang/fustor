[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fustor-schema-fs"
version = "0.1.0"
description = "Fustor File System Schema - Contract between source-fs and view-fs"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [
    {name = "Fustor Team"}
]
keywords = ["fustor", "schema", "filesystem"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

# No runtime dependencies - schema is pure data definition
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/fustor_schema_fs"]
