# Gate Discovery Shim

This repository keeps the formal fs-meta specification tree under `fs-meta/specs/`.
The gate discovery entrypoints below delegate to the canonical repo-owned validator
and contract workflow for that tree.

./scripts/validate.py specs/
./scripts/test-workflow.sh contracts
